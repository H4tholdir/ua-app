# Referto — T9 · ondata (b) · `FoglioCategoria` (§5.41, C1)

**Data:** 01/08/2026 · **Ramo:** `ondata-b-schermate`
**Legge:** spec v3 rev. 3.4 **§5.41** · **Prove e ragioni:** `docs/superpowers/specs/allegati/2026-07-30-ds-v3-sezioni-album.md` §5.41 e **§1.9 (B-6)**
**Piano:** `docs/superpowers/plans/2026-07-30-album-foto-scheda-lavoro.md` → **Task 9**, col riquadro «MANDATO CORRETTO» che vince sul resto
**Da dove vengo:** `docs/roadmap/2026-08-01-t8-referto.md` (T8, la tendina) · **Dove va:** T9-bis (`FoglioConferma`, §5.42).

**Esito:** ✅ fatto. Nasce `src/components/ds/FoglioCategoria.tsx` con
`tests/unit/ds-v3/componenti/FoglioCategoria.test.tsx` (**35 prove**). Non è montato da nessuna parte — è
**T11** (lo scatto) e **T10/T12** (la correzione).

⚠️ **Nota di processo, dichiarata:** questo task è stato eseguito **dallo stesso esecutore di T8**, contro
R-E1 (un esecutore fresco per compito), su richiesta esplicita di Francesco. La compensazione adottata è
stata **leggere le fonti non ancora lette** invece di fidarsi di quanto «già saputo» a T8 — ed è servita:
**§1.9 (B-6) prescrive per questo componente una forma di «riduci movimento» diversa da quella di §5.40**, ed
è esattamente il punto in cui T8 aveva sbagliato copiando dal vicino.

---

## 1. FASE 7, con l'output vero

```
npx tsc --noEmit     → 0 errori
npx eslint <i due file nuovi> → pulito
npx vitest run       → Test Files  367 passed | 3 skipped (370)
                       Tests  4140 passed | 19 skipped (4159)
npx next build       → ✓ Compiled successfully in 4.9s
```

Guardie: `check-ds-compliance.sh` ✅ · `guardia-reduced-motion.mjs` ✅ · `guardia-coerenza-documenti.mjs` ✅
(18 documenti vivi).

**Il conto torna: +36 = 35 mie + 1 che non ho scritto.** Riferimento a T8 chiuso: **366 | 3** file e
**4104 | 19** prove. Ora **367 | 3** e **4140 | 19**. La prova in più è di
`tests/unit/home-style-parsabile.test.ts`, che genera un caso per ogni sorgente con un blocco `<style>`:
passa da **54 a 55** (`provato:` eseguito da solo). È la terza ondata di fila in cui questa previsione regge.

🛑 **`scripts/guardia-navigazione-overlay.mjs` non è stata lanciata:** il componente non è montato da nessuna
parte e alla guardia serve l'app accesa con un lavoro preparato. Va lanciata quando questo foglio entra in
una schermata (T10/T11).

---

## 2. R-P4 — quanto mordono le prove: **33 su 35**

Abbozzo inerte (il componente rende `null`, `variantePannello` torna sempre la coreografia di casa): **33
prove si accendono**. Le due verdi:

| prova verde sull'abbozzo | perché |
|---|---|
| «chiuso: niente a schermo e il corpo non si tocca» | prova di **assenza** |
| «a preferenza SPENTA la coreografia è quella di casa, non una copia» | l'abbozzo torna **davvero** `coreografie.sheetSu`, cioè soddisfa per caso metà del contratto. ⚠️ Dichiarata come **debole**: la sua gemella (preferenza accesa) è rossa, ed è quella che porta il peso |

### Le mutazioni — sei, tutte mordono

| mutazione | atteso | misurato |
|---|---|---|
| l'uscita chiude **senza** portare la categoria (via `onScegli('altro')`) | rosso | **3 rosse** |
| l'ordine si inverte (`onChiudi()` prima di `onScegli('altro')`) | rosso | **3 rosse** |
| `y` tolta dal bersaglio ridotto (il difetto D1/D2) | rosso | **1 rossa** |
| a preferenza accesa si usa la coreografia intera | rosso | **1 rossa** |
| allo scatto si accende «Impronta» (il mockup) | rosso | **2 rosse** |
| il focus si posa sulla prima pastiglia invece che sul pannello | rosso | **2 rosse** |

🔑 **Le prime due sono la stessa riga di spec vista da due lati** (D74), e servono entrambe: la prima prova
che la categoria arriva, la seconda che arriva **prima** della chiusura. Un `onChiudi()` che parte per primo
smonterebbe il chiamante prima che la categoria sia scritta.

### Le forme d'ingresso enumerate — **19**, quindici coperte, quattro col loro perché

Coperte: chiuso · aperto senza anteprime · `quante = 1` · `quante = 5` con **3** anteprime (il plurale viene
da `quante`, mai dal numero di anteprime) · `scelta` assente (lo scatto) · `scelta` presente (la correzione) ·
tap su una pastiglia · tap sul velo · click orfano (ghost click) · `Escape` · `Escape` con uno `Sheet`
fratello sotto · «indietro» · `Tab` ripetuto oltre l'ultimo elemento · àncora dichiarata · àncora vuota.

| non coperta | perché |
|---|---|
| **swipe giù** | jsdom non simula pan e velocità in modo affidabile — è la ragione per cui `deveChiudere` **esiste come funzione pura** ed è provata a parte (`Sheet.tsx:40`). Qui si importa, non si riscrive |
| **text-zoom 200% a 390 e 768** | è una prova di **browser**, non un'asserzione unitaria: la porta il collaudo (§5.41). Qui la rete unitaria è che **nessuna pastiglia porta `whiteSpace: nowrap`**, che è la causa del taglio |
| `quante = 0` | forma incoerente per costruzione: il foglio si apre **dopo** che almeno una foto esiste |
| più di tre anteprime | coperto dal codice (`slice(0, 3)`) ma non da un caso proprio: la prova con `quante=5` e tre anteprime copre già la dissociazione fra le due grandezze |

---

## 3. Le scelte che portano una ragione

- **Ogni uscita porta una categoria** (D74): velo, `Escape`, «indietro» e swipe chiamano `onScegli('altro')`
  **e poi** `onChiudi()`. E **non è un errore**: nessun suono, nessuna vibrazione, nessun avviso — provato in
  positivo (`suona` e `vibra` mai chiamati su un'uscita).
- **Allo scatto nessuna pastiglia è accesa**, contro il mockup: pre-selezionare «Impronta» darebbe due
  default in contraddizione con D74, e quello affermato a schermo sarebbe il più sbagliato. La prop `scelta`
  serve **solo** al secondo momento (v. §4 ②).
- **Il focus si posa sul PANNELLO**, non sulla prima pastiglia: sulla tastiera è la stessa ragione di D74.
  `trappolaFocus` senza `focusIniziale` fa già esattamente questo, e il suo commento cita §5.41.
- **`Tab` TRATTENUTO** — ed è **l'opposto** di quanto fatto ieri in `TendinaMenu`: lì `role="menu"` senza
  `aria-modal` e il `Tab` chiude; qui `role="dialog"` + `aria-modal="true"`, e la trappola è la metà mancante
  di quella promessa (D85). Provato col gemello di `SkipToContent` fuori dal pannello, come T5-ter.
- 🔴 **«Riduci movimento» — la forma è DIVERSA da quella di §5.40, ed è il punto delicato di questo task.**
  `coreografie.sheetSu` porta la transizione **dentro** la variante (`v3/motion.ts:85-86`): una `transition`
  passata come prop non ci arriverebbe mai, e il foglio si muoverebbe lo stesso. Serve la **variante ridotta
  esplicita** di §1.9 (B-6), con `y` che **resta nel bersaglio** e ci arriva istantanea.
  🛑 E la via che si troverebbe guardando la casa è peggio: `SheetRidotto` (`Sheet.tsx:448-458`) ha un
  pannello **senza `y`**, cioè il bersaglio-tolto che §1.9 esiste per vietare, scritto dentro il componente
  che fa da modello.
  🔑 **La variante è esportata** (`variantePannello`) per la stessa ragione per cui `Sheet` esporta
  `deveChiudere`: è un dato puro, e in un ambiente dove Motion non applica mai l'`animate`
  (`skipAnimations`) l'unico modo onesto di provare «`y` finale = 0 e nessun tween su `y`» è guardare
  l'oggetto vero. **Meglio della prova testuale sul sorgente usata a T8**, ed è il modo giusto quando il
  dato si può isolare.
- **La geometria si dichiara** (`width:'100%'`, `maxWidth: 480`), e il pannello **scorre davvero**
  (`overflowY:'auto'`), o l'`overscrollBehavior:'contain'` sarebbe una riga decorativa.
- **Le emoji sono un `Record<CategoriaFoto, string>`** sull'elenco importato, non un secondo elenco: se una
  categoria si aggiunge, questo file **non compila** finché non ha la sua. È l'opposto della copia locale che
  quel modulo vieta a chiare lettere.

---

## 4. 🔴 Ritrovamenti — R-E2: riferiti, non corretti

**① Il piano porta ancora due righe ANNULLATE dal suo stesso riquadro, ed erano prove da scrivere.** Il
Passo 1 chiede la prova che «Guida colore» **non va a capo** perché «la pastiglia utile è 148,5 px», e il
Passo 4 chiede la mutazione «porta il testo a 15,5 px → atteso rosso sulla prova della larghezza». Ma il
MANDATO CORRETTO ai punti 2-3 dichiara **VOID** quella misura (era presa dentro la cornice di telefono del
mockup, 342 px) e **vieta** il `nowrap` che la rendeva sensata. **Non le ho scritte.** ⚠️ È lo **stesso
difetto** trovato ieri nel Task 8 — un riquadro corregge la testa del task e il corpo resta a dire il
contrario: **due task su due**. Chi legge un Passo senza il riquadro scrive la prova sbagliata.

**② 🔴 La firma del piano non può rendere metà del componente che la spec descrive.** «Interfacce → Produce»
elenca `{ aperto, quante, anteprime, ancoraFocus, onScegli, onChiudi }` — **manca la categoria corrente**.
Ma §5.41 dichiara due momenti, e nel secondo (la correzione, D70) «una categoria c'è già» e la sua pastiglia
è accesa con `aria-pressed="true"`. Senza una prop, quel momento **non è rappresentabile**. Aggiunta
`scelta?: CategoriaFoto`, provata su entrambi i momenti (zero accese allo scatto, esattamente una nella
correzione). **Scostamento dichiarato dall'interfaccia del piano.**

**③ Divergenza dichiarata sul tipo di `ancoraFocus`.** Il piano dice `ancoraFocus?: HTMLElement | null`
(opzionale, elemento nudo); qui è **`RefObject<HTMLElement | null>` obbligatoria**, come in `VisoreFoto`
(F-12). Ragioni: né §5.41 né §1.6/C-12 impongono un contenitore — impongono che l'àncora sia **dichiarata**;
e il chiamante vero (T10) terrà **già** un ref al ⋯ per `VisoreFoto.ancoraFocus`, quindi passa lo stesso
oggetto. Obbligatoria perché un focus che non torna **non si vede in nessuno screenshot**: se il `.current` è
vuoto all'apertura, si avvisa in sviluppo.

**④ «Il manico» è elencato fra le uscite, ma in casa non è un comando.** §5.41 scrive «velo, manico, swipe
giù, `Escape`, «indietro»». In `Sheet` (`:369-373`) il manico è `aria-hidden="true"`, **non** un bottone, e
serve solo ad avviare il trascinamento — e a 36×4 px sarebbe comunque **sotto il bersaglio minimo di 44**.
Qui è stato fatto come in casa: innesco del gesto, non uscita per tap. **Se invece deve chiudere al tocco,
gli serve un'area sensibile allargata**, e allora è una modifica che riguarda anche `Sheet`.

**⑤ Un limite d'ondata, non di questo task: l'uscita non è animata.** Nessuno dei tre strati nuovi
(`VisoreFoto`, `TendinaMenu`, `FoglioCategoria`) monta `AnimatePresence`, quindi la chiave `exit` delle
varianti non gioca: si esce di colpo. La variante `exit` è comunque scritta e provata, come prescrive §1.9
(B-6). **Va deciso insieme per tutti e tre al gate estetico L2 (T13)** — se lo si facesse qui per uno solo,
il foglio uscirebbe animato e i due strati sopra no.

---

## 5. Che cosa resta aperto (nessuno è mio)

- **`ROADMAP-UFFICIALE.md`** descrive ancora l'ondata (b) come «*da pianificare*» mentre è eseguita fino a
  T9: da sistemare **prima del merge** (già riferito da T8).
- **`MenuVoce.tsx:77-79`** mostra il chevron anche sulla variante distruttiva, contro la sua legge visiva
  (F-6) · **`TastoTondo`** senza `aria-haspopup`/`aria-expanded` (**T10**) · **`sopraFoto.facciaAttiva`**
  ancora a zero usi (**T10**, non è un difetto).
- **R27** (`tsc` non protegge le query) · **R29 + D81** (foto rotte su uachelab.com fino al merge, **T13**) ·
  **FM-8** · **tredici** overlay di `src/components/features/**` che promettono `aria-modal` senza
  mantenerlo.

**Nessuna decisione nuova di Francesco in questo task:** il verbale resta a **novanta**, la prossima è D91.
