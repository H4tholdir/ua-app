# Handoff — Sessione N13 (guard `lab.stato` nelle API) — percorso GRANDE
**Data:** 17 luglio 2026, sera · **Da eseguire in sessione NUOVA a contesto pulito** (decisione Francesco)

## Contesto (30 secondi)

Punto (1) della sequenza ratificata post-P0-PERF. In questa sessione: **decisioni N14/N13/N11-bis ratificate da Francesco** (panel advisor §0C completati — 5 pareri, tutti CONFERMATA CON RISERVE o convergenza piena), poi **N11-bis e N14 implementati e verificati** (TDD, tsc 0, vitest 2096 pass, build OK, verifica browser).

⚠️ **Working tree NON committato** (Francesco non ha chiesto commit). Contiene N11-bis + N14 pronti:
- N11-bis: `.is('deleted_at', null)` in `api/admin/labs/[id]/impersonate/route.ts` + `admin/labs/[id]/live/page.tsx` + `tests/unit/n11bis-impersonate.test.ts`
- N14: `login-form.tsx` (prefetch + 250ms + arm prompt, fix skip cosmetico), `src/lib/auth/passkey-prompt.ts`, `PasskeyPromptOnDashboard.tsx` (montato in dashboard), `tests/unit/passkey-prompt.test.ts`
- Prima di iniziare N13: decidere con Francesco se committare questi due item (consigliato: 2 commit separati `fix(admin)` + `feat(auth)`) — poi worktree dedicato per N13.

## N13 — Decisione RATIFICATA (non ridiscutere, implementare)

**Documento completo (LEGGE per questa ondata):** `docs/design/decisions/2026-07-17-N13-N14-N11bis-ratifiche.md`

Sintesi operativa:
- Helper **`assertLabOperativo(ctx, method)`** (nuovo `src/lib/supabase/lab-guard.ts`), chiamato a inizio handler. NON negli helper contesto, NON in middleware. Early-return della 403 pronta, niente throw. **Kill-switch** env per rollback istantaneo.
- Matrice: trial/attivo ok · trial-scaduto/sospeso/scaduto → mutazioni 403 · **blacklist → tutto 403, GET inclusi (ratificato)** · stato sconosciuto → 403 fail-closed · `lab===null` non-admin → 403 fail-closed · admin_sistema bypass · **admin_rete = come titolare (ratificato)**.
- Contratto: `403 + { error, code }`, codici distinti `UA_LAB_TRIAL_SCADUTO`/`UA_LAB_SOSPESO`/`UA_LAB_SCADUTO`/`UA_LAB_BLACKLIST`. `Cache-Control: no-store`.
- **Scope pieno (ratificato):** guard su handler mutanti (~65 file) + 28 GET categoria A + **guardia portale token** (`api/portale/[token]/*`: blacklist → 404 generico, no info-leak; sospeso/scaduto read ok) + riga trial-scaduto. ~95 edit one-line + ~3 wrapper (`verifyTitolare` ecc.: esporre `lab` — oggi il tipo di ritorno non lo porta).
- **Anti-drift:** test di guardia statico gemello di `tests/unit/lab-context-guard.test.ts` + allowlist esenzioni esportata (`lab-guard-exempt-routes.ts`): Stripe checkout/portal (GET side-effecting, aperti a sospeso/scaduto), `stripe/webhook`, `auth/*`, portale (guardia propria), export GDPR.
- **Rollout:** shadow mode log-only 24-48h → enforcing.
- Accessorie da includere/tracciare: fetch-wrapper client PWA (map `UA_LAB_*` → `/impostazioni/abbonamento`) — senza, il code è inerte; revoca sessioni al passaggio blacklist (hook in `admin/labs/[id]/stato`); `timingSafeEqual` in `internal/pec-verify`; processo GDPR out-of-band documentato in `docs/security/` PRIMA del deploy del blocco-GET-blacklist (azione Francesco, bloccante solo per il deploy).

## Percorso (§0C — GRANDE, tocca auth)

FASE 2 (brainstorm) = **già assorbita dai panel** (5 advisor, riserve integrate nel doc decisione). Partire da: FASE 3 validazione arch (5 domande) → FASE 4 `writing-plans` → FASE 5 worktree → FASE 6 TDD → FASE 7 verifiche → review → shadow → merge → enforcing → BP-1.

## Azioni SOLO di Francesco

- Conferma commit N11-bis + N14 (working tree attuale) · processo GDPR out-of-band (per deploy blocco-GET-blacklist) · collaudo login reale ≤2s + abbassare `PERF_BUDGET_LOGIN` · secrets PERF_* · deferral N14 (restyling modal ai token → gate L2; cap lifetime + voce Impostazioni→Sicurezza).

## Regole di ingaggio (invariate)

CLAUDE.md §0C (12 fasi + regola advisor) · lab E2E `00000000-…-0001` per QA, MAI lab Filippo · BP-0/BP-1 obbligatori · budget: rapporto P0-PERF §7.
