# UÀ — Roadmap Ufficiale
**Ultimo aggiornamento:** 28 luglio 2026 (27) — 📐 **ONDATA (b) APERTA: OTTO DECISIONI RATIFICATE DA FRANCESCO, nessuna riga di codice.** Verbale: **`docs/design/decisions/2026-07-28-wizard-ondata-b-decisioni.md`**. **Perimetro: il solo wizard** (D1) — le tre eredità della scheda del lavoro e i due difetti della home restano fuori, tracciati. 🛑 **Il catalogo dei colori è CHIUSO** (D3, parole di Francesco: «non esiste poter inserire un colore che non esiste»): **decade la "quinta eredità"** e con essa la richiesta di panel; il panel era stato fatto lo stesso e ha **falsificato entrambe le gambe** della premessa (sul POST un rifiuto non perde il lavoro; sul PUT rifiutare non è gratis — blocca l'intero salvataggio). 🆕 **`pazienti` diventa un'anagrafica vera** (D4), ma **solo per la parte wizard** (D5): si cerca prima di creare, riconoscimento **per cognome** disambiguato da dentista + data dell'ultimo lavoro (D6, **la data di nascita NON entra**); unione delle schede doppie e pagina `/pazienti` in scrittura restano **fuori, voce propria**. 🔓 **Deroga concessa e regola del DS riscritta** (D7): la riga «il nome paziente non compare MAI in UI» dichiarava un invariante che l'app **già** non rispettava (parete cassette, D8 del 27/07) — ora dice «pseudonimo per difetto, deroghe esplicite e datate», con notifiche e WhatsApp **senza eccezioni**. 📷 **Un passo foto sempre presente** (D8), altrimenti i tipi senza prescrizione restavano senza fotocamera. **Due caselle nome/cognome, facoltative, e via i pulsanti «Salta»** (D2). 🔴 **Tre fatti verificati aprendo i file:** il wizard **non ritrova mai un paziente** (il codice proposto è sempre nuovo, quindi il confronto non colpisce) · `crea-lavoro.ts:229-230` manda `nome:''` e `cognome: alias||pz` **fissi**, quindi due caselle non arriverebbero a nulla · **nessun vincolo di unicità sul codice paziente** (`schema.sql:461`, testo nudo), mentre il numero si calcola su tutto il laboratorio e la ricerca filtra per dentista. **Mockup fatti e APPROVATI** (§0B, più varianti come da preferenza permanente): variante **A** per il passo paziente — la ricerca vive **dentro la casella «Cognome»**, perché mostra il doppione anche a chi non sospetta che esista (D9) · **avanzamento a briciole** (D10): `ProgressDots` **esce dal wizard**, niente conteggio, in testata si vede *ciò che hai già scelto*. 🔑 Il fatto che ha aperto quella strada: il numero dei passi è ignoto **solo per i primi due** — scelto il tipo, la sequenza è determinata. 🗑️ **«Dimmelo a voce» esce del tutto** (D13): censiti 4 usi + 2 test + 1 regola DS. 📐 **Stretto per le domande, largo per denti e colore** (D14). **SPEC SCRITTA, 🟡 da ratificare:** `docs/superpowers/specs/2026-07-28-wizard-ondata-b-schermate-design.md`. **Gate FASE 3 passato → percorso GRANDE** (migration + dati sanitari con deroga). ⚠️ **Mancano 3 mockup prima del React:** passo foto · passo cassetta · avviso «codice già in uso». **Prossimo:** rilettura della spec → piano. 🛑 **PUNTO DI RIPRESA: `docs/roadmap/2026-07-28-ondata-b-piano-handoff.md`** — l'handoff di stamattina è **superato** (porta un banner in testa: la premessa sul catalogo era falsificata e il panel è decaduto). **10 ritrovamenti fuori mandato** raccolti in §6 del verbale, ognuno con la sua destinazione.

**Aggiornamento precedente:** 28 luglio 2026 (26) — 🚀 **VOCE 1, ONDATA (a): IN PRODUZIONE.** Merge `a3e52379` su `main` (78 commit, fast-forward pulito), autorizzato da Francesco dopo il collaudo. **Denti e colore sono ora un dato vero in `lavori_denti`, non più testo dentro il lavoro.** 🛑 **SI PARTE CON L'ONDATA (b) — le schermate:** punto di ripresa **`docs/roadmap/2026-07-28-ondata-b-handoff.md`**. ⚠️ **La (b) NON comincia dal codice:** è tutta interfaccia, quindi mockup da approvare **prima** (§0B) e **gate estetico L2 obbligatorio** alla fine (FASE 9b). Eredita le quattro cose elencate qui sotto, più ~~la quinta che vuole il panel (il catalogo non chiuso: esito asimmetrico…)~~ — 🛑 **DECADUTA il 28/07/2026: il catalogo è CHIUSO, decisione D3 di Francesco (v. aggiornamento 27 in testa). Non riaprire.** — più **cinque cose trovate al collaudo** (due introdotte dalla correzione del 28/07 — sovrapposizione dell'avviso e contrasto 4,06/3,76 sotto il 4,5 richiesto — due preesistenti e una di lingua).

**Aggiornamento precedente:** 28 luglio 2026 (25) — ✅ **VOCE 1, ONDATA (a): COLLAUDO NEL BROWSER SUPERATO — 5 prove su 5, nell'app vera, ogni esito verificato contro la banca dati.** Referto: **`docs/roadmap/2026-07-28-collaudo-ondata-a-referto.md`**. ✅ **Il collaudo ha trovato un difetto NUOVO fuori dalle cinque prove — le tre frasi dell'ondata non arrivavano all'utente, che leggeva solo «⚠ Errore — riprova» — e su decisione di Francesco è stato CORRETTO subito.** Una riga (`saveError && !isDirty` → `saveError`): quella condizione era **irraggiungibile**, non rara, e il paragrafo d'errore era codice morto. TDD con **prova per mutazione** + ciclo riprovato nel browser. **FASE 7 dopo la correzione: `tsc` 0 · `eslint` pulito · vitest **3625** · build ok.** 🛡️ Aggiunta anche la prova che **il controllo di conflitto scatta** (409 con data vecchia, nulla scritto): la prova 5 da sola mostrava solo che non scatta a sproposito. Il colore arriva e si ritrova · `A3,5` mostra l'avviso **e il lavoro nasce lo stesso** · azzerare il colore lo toglie **da tutti i posti** e non riappare al ricaricamento · **il rifacimento eredita denti E colore** (era G1, il più grave) · due salvataggi di fila **senza conflitto**. 3 viewport × 2 temi fatti; **la frase nuova a 390px non si tronca**. **Database riportato alla baseline esatta** (294 · 0 · 916 · 48). ⚠️ **Due rilievi PREESISTENTI riferiti e non toccati** (i file non sono toccati dal ramo, verificato col diff): disallineamento di idratazione su `LinguettaCassette`/`StanzePager`; a **1280×800** due campi colore sono coperti dalla fascia in fondo **finché non si scorre** → ondata (b). **Confermati dal vivo tre difetti già censiti** del rifacimento (`incidenti_mdr` resta vuota · route e funzione non concordano sugli stati · l'originale non viene annullato). 🛑 **RESTA SOLO: merge — lo autorizza Francesco — poi deploy e BP-1 finale.**

**Aggiornamento precedente:** 28 luglio 2026 (24) — ✅ **VOCE 1, ONDATA (a): FINITA COME CODICE — 13 task su 13 + 5 code + revisione indipendente con 5 correzioni.** `ondata-a-denti-colore`, **72 commit, mai mergiata.** **RESTA SOLO il collaudo nel browser**, poi merge e deploy. **`tsc` 0 · `eslint` 0 · build ok · vitest 3622 · DB alla baseline.** Punto di ripresa: **`docs/roadmap/2026-07-28-ondata-a-chiusura-handoff.md`**. Revisione (3 revisori, mandati disgiunti): `docs/roadmap/2026-07-28-revisione-pre-merge-ondata-a.md` — **isolamento fra laboratori provato pulito**; 3 gravi chiusi (il rifacimento perdeva denti e colore · un dente storto faceva perdere **il lavoro** · un test che non poteva fallire). 🧹 **gstack rimosso** su decisione di Francesco. 🛑 **Le migration sono già applicate sul database vivo:** «niente in produzione» vale per il codice, non per lo schema.

**Aggiornamento precedente:** 28 luglio 2026 (23) — ✅ **VOCE 1, ONDATA (a): COMPLETATA SUL RAMO — 13 task su 13 + tre code.** `ondata-a-denti-colore`, **mai mergiata, niente in produzione, il merge lo autorizzi tu.** Mancano review e QA browser. **vitest 3584 · `tsc` 0 · `eslint` 0 · build ok · DB alla baseline.** **R4 provato per intero** con richieste ostili sul database vero; **R5 no** — la prova è fallita e ha scoperto **sei tabelle che rendono un laboratorio incancellabile** (pre-esistente): sezione dedicata in coda a questa roadmap, insieme ai **due progetti Playwright fantasma**. Dettaglio: `MEMORY.md` **voce 58**.

**Aggiornamento precedente:** 28 luglio 2026 (22) — 🔨 **VOCE 1, ONDATA (a): 9 task su 13 chiusi** sul ramo `ondata-a-denti-colore`, **mai mergiata, niente in produzione**. Il T9 ha chiuso la parte «database e API»: `POST /api/lavori` crea progressivo, lavoro e denti in **una transazione sola** (commit `759fc183`, 3477 test verdi). Restano i quattro task che toccano le schermate vive — **T10, T11 e T12 vanno nello stesso deploy**. Ritrovamenti e trappole: `docs/roadmap/2026-07-28-ondata-a-esecuzione-handoff.md` §5-bis.

**Aggiornamento precedente:** 27 luglio 2026 (21) — ✅ **VOCE 1 (ripensamento del wizard): SPEC RATIFICATA DA FRANCESCO, PIANO DELL'ONDATA (a) SCRITTO.** 🛑 Nessuna riga di codice, niente committato. **Piano:** `docs/superpowers/plans/2026-07-27-wizard-ondata-a-dato-e-api.md` — 13 task TDD con codice completo. **Il lavoro si esegue in TRE ONDATE** (decisione di Francesco, 27/07): **(a)** modello dati + API + creazione atomica + reindirizzamento dei due scrittori a grafica invariata — **la migration sta tutta qui, non si spezza** · **(b)** wizard adattivo + odontogramma rifatto in v3 (tabella dei 38 tipi, «Lo scrivo dopo», illustrazioni con sagome, colore per dente) — **richiede FASE 9b, gate estetico L2** · **(c)** Dichiarazione di Conformità + gancio nel precheck di consegna. Ogni ondata si prova e va in produzione da sola, e avrà il **suo** piano: quelli di (b) e (c) si scrivono quando arriva il loro turno. **Altre due decisioni:** la foto della prescrizione **resta accessoria alla creazione ma il fallimento diventa visibile** (nessun blocco al banco, W22 — il precheck di consegna è il guardiano); la spec è stata corretta in tre punti dove contraddiceva le risposte di Francesco (W21 sulla DdC, W23 sul `DROP COLUMN`, e il `DROP` mancante dall'ordine della migration). **🔴 Due buchi trovati scrivendo il piano, creati dal taglio in ondate e chiusi lì dentro:** senza il reindirizzamento degli scrittori, i sette campi ora sentinella **smetterebbero di salvare in silenzio** (il server scarta le chiavi fuori allowlist senza errore: l'utente vedrebbe «Salvato» su un dato mai salvato); e la **Dichiarazione di Conformità sarebbe rimasta senza denti** per tutti i lavori consegnati fra l'ondata (a) e la (c) — chiuso facendo mantenere alle RPC la vecchia colonna come denormalizzazione, stesso regime di `numero_cassetta`. Dettaglio: MEMORY.md **voce 53**.

**Aggiornamento precedente:** 27 luglio 2026 (20) — 📐 **VOCE 1 (ripensamento del wizard): SPEC PRONTA, si va al piano.** Brainstorming chiuso · gate FASE 3 superato · panel advisor 3× sul modello dati · mockup misurati su 3 viewport × 2 temi · **spec scritta**. 🛑 Nessuna riga di codice, niente committato. **Handoff: `docs/roadmap/2026-07-27-wizard-spec-handoff.md`** · spec: `docs/superpowers/specs/2026-07-27-wizard-nuovo-lavoro-design.md` · verbale (23 decisioni): `docs/design/decisions/2026-07-27-wizard-nuovo-lavoro-brainstorming.md`. **La voce 3 della coda è CHIUSA dentro questo lavoro** (il colore nella DdC: verificato sull'Allegato XIII — il campo esiste già ed è vuoto). **La voce 2 (avviso alla consegna) diventa il perno**: per decisione di Francesco (W22) il precheck di consegna è il guardiano di tutti gli obblighi, e questa spec ne costruisce il gancio. ⚠️ Da valutare prima del piano: tagliare in 3 ondate — (a) dato+API+creazione atomica, (b) wizard+odontogramma, (c) DdC+precheck.

**Aggiornamento precedente:** 27 luglio 2026 (19) — ✅ **IL NOME DEL PAZIENTE SI CORREGGE.** Ramo `ondata-nome-cognome-paziente` pronto al merge: tsc 0 · eslint pulito · vitest **3424/19** (base 3364) · build ok. Dettaglio: MEMORY.md **voce 51**.

🛑 **PERIMETRO RIDOTTO IN CORSA — decisione di Francesco.** Il **wizard «Nuovo lavoro» va ripensato per intero**, quindi i task che toccavano la sua schermata sono stati **fermati**: mockup del passo 3 (prodotto ma **NON approvato**), `crea-lavoro.ts`, `PassoPaziente.tsx`, stato del wizard + bozza `v:2`. ⚠️ **Conseguenza dichiarata: la targa della cassetta NON migliora con questo ramo** — il wizard scrive ancora tutto dentro il cognome. La lamentela ratificata («taglia la coda, sparisce il cognome») **resta aperta** e si chiude col nuovo wizard.

**Consegnato:** la regola unica di scrittura di `pazienti.(nome,cognome)` · le due route che la applicano con 422 fail-closed · **nome e cognome correggibili per la prima volta** dalla scheda paziente (Art. 16 GDPR + direttiva D10) · 4 falle di errore grezzo del DB chiuse · isolamento tenant provato con richieste ostili · etichetta PDF allineata a IFU/Ricevuta.

### 🔜 Le prossime voci, nell'ordine deciso da Francesco (27/07)

| # | Voce | Percorso |
|---|---|---|
| **1** | ✅ **Ripensamento del wizard «Nuovo lavoro» — SPEC RATIFICATA 27/07/2026, in TRE ONDATE.** **(a)** *🚀 **IN PRODUZIONE dal 28/07/2026** — merge `a3e52379` su `main`, **78 commit**, fast-forward pulito, autorizzato da Francesco **dopo** il collaudo. **13 task su 13 + 5 code + revisione indipendente + FASE 9 nel browser (5 prove su 5, più il controllo positivo che il conflitto scatta).** Il collaudo ha trovato e chiuso **un difetto suo**: le tre frasi dell'ondata non arrivavano all'utente, che leggeva solo «⚠ Errore — riprova» — la condizione che le mostrava era **irraggiungibile** e quel paragrafo era codice morto. Referto: `docs/roadmap/2026-07-28-collaudo-ondata-a-referto.md`. ~~Mancano ancora: review (FASE 8) e QA browser (FASE 9).~~ FASE 7 fatta con output reale: `tsc` 0 · `eslint` 0 · `next build` ok · **vitest 3584** · DB alla baseline. **R4 (isolamento fra laboratori) provato per intero sul database vero**; **R5 (cancellabilità) provato solo per `lavori_denti`** — la prova ha scoperto sei tabelle che rendono un laboratorio incancellabile, sezione dedicata in coda. Punto di ripresa e i 23 ritrovamenti: `docs/roadmap/2026-07-28-ondata-a-esecuzione-handoff.md`; evidenze: `docs/superpowers/plans/evidenze/2026-07-27-ondata-a-isolamento.md`. 🕛 **Fuori dal deploy, può seguire: T11-bis** (POST e PUT rispondono diversamente sugli stessi dati: `422` sul PUT, `500` sul POST)* — modello dati per-dente (`lavori_denti`, 52 codici FDI, `colori_dentali`), 2 RPC atomiche, `PUT /api/lavori/[id]/denti`, sentinelle sui 7 campi, i due scrittori reindirizzati **a grafica invariata**. Piano: `docs/superpowers/plans/2026-07-27-wizard-ondata-a-dato-e-api.md`, 13 task. **(b)** *da pianificare* — 📌 **eredita quattro cose dall'ondata (a), trovate eseguendo (dettaglio: `docs/roadmap/2026-07-28-ondata-a-esecuzione-handoff.md` §5-quinquies):** ① la **tendina della scheda offre 19 codici su 48** — un `2M2` che arriva dal colore di caso rende la casella **vuota a schermo** pur essendo il dato intatto, e da oggi quella tendina è anche **la penna** del colore di caso, non solo la sua vetrina; ② il **default di caso non è correggibile alla creazione**, solo dalla scheda; ③ le **tre zone del ceramista** (collo/corpo/incisale) **non hanno destinazione senza un dente selezionato** — oggi il form si ferma e lo dice, ma è un limite, non una soluzione; ④ il wizard scrive il **nome nel cognome** (metà rimasta della tappa 1) — wizard adattivo (tabella dei 38 tipi: il tipo decide **se** la domanda compare, e quando compare **si può saltare**), denti sulle illustrazioni con sagome, colore per dente, cassetta saltabile, `OdontogrammaFDI` rifatto in v3. **⚠️ FASE 9b obbligatoria.** 🔴 **L'ondata (b) chiude anche la metà rimasta della tappa 1 «nome e cognome paziente»** (il passo paziente deve scrivere le due parti separate: oggi il wizard mette tutto nel cognome, e **la targa della cassetta non è ancora migliorata**). Verificato il 27/07: la spec del wizard non lo nominava — annotato ora in spec §12. 🟡 **Aperto, da decidere sui mockup a inizio ondata (b):** cosa mostrare al posto di «passo 2 di 3», dato che i passi variano col tipo (`ProgressDots` presuppone un numero fisso). **(c)** *da pianificare* — 📌 **eredita tre pulizie dall'ondata (a)** (§5-quinquies dell'handoff): ① due `select` della fatturazione (`fatture/[id]/xml/route.ts:109-112`, `fatture/batch/route.ts:125-128`) chiedono le quattro colonne colore e **non le leggono mai** — verificato che nessun valore fermo finisce su un documento fiscale, ma vanno ripulite; ② la vista `lavori_dashboard` espone ancora `colore_dente`, colonna ferma dal T10, nessun chiamante; ③ la **sostituzione integrale** porta **tutte** le righe a `provenienza:'eseguito'` a ogni modifica clinica, anche i denti non toccati — rileva per il precheck W20/W22 — il colore nella Dichiarazione (collasso che **conosce le arcate**: «13-23» vale 6 denti, non 11), template che legge lo snapshot, gancio nel precheck. **Fuori perimetro, tracciato:** arrivo automatico da email e portali degli scanner (W1). | **GRANDE, con migration** — la migration sta **tutta nell'ondata (a)**. Spec: `docs/superpowers/specs/2026-07-27-wizard-nuovo-lavoro-design.md` · verbale (23 decisioni): `docs/design/decisions/2026-07-27-wizard-nuovo-lavoro-brainstorming.md` |
| **2** | **Avviso alla consegna su dente/colore mancanti** — avviso, mai blocco (una totale non ha elemento, un bite non ha colore). Oggi `precheck.ts` **non li nomina** e la DdC stampa il valore grezzo | Media, con panel su quali dispositivi esentare |
| **3** | ⚠️ **Verificare se il colore compaia nella DdC** — sembra assente dal modello e dallo snapshot, mentre `ANALISI/17:119` lo elenca fra gli obbligatori. **Possibile lacuna normativa** | verifica, poi voce propria |
| **4** | **Assistente vocale completo** che sostituisca «Dimmelo a voce» (oggi non funziona) — voce e interrogazioni su tutta la PWA | GRANDE, tutto da progettare |
| **5** | **Tappa 1-bis** (D9): la fotografia congelata `paziente_nome_snapshot` + correzione dalla scheda del lavoro | GRANDE, panel normativo |

⚠️ **Nota che lega la voce 1 e la 5:** correggere il paziente **non** aggiorna lo snapshot sui lavori già creati. Probabilmente giusto per i lavori consegnati, quasi certamente sbagliato per quelli aperti — da sciogliere insieme.

**Aggiornamento precedente:** 27 luglio 2026 (18) — 🔎 **RICERCA (input per una decisione, nessuna riga di codice): «Elemento e colore vanno chiesti al Passo 3 del wizard "Nuovo lavoro"?»** Documento `docs/roadmap/2026-07-27-ricerca-presa-in-carico-lavoro.md` — quattro sezioni con fonte per ogni affermazione (ricerca web IT/EN, normativa `ANALISI/17`, codice `file:riga`, pattern UX con fonti Baymard/NN·g/GOV.UK), nessuna proposta: la decisione spetta a Francesco con un panel di 2-3 advisor (Regola Advisor). **Fatti principali:** elemento/colore **non sono mai parte della creazione del lavoro nel codice attuale** — `crea-lavoro.ts` li scrive con una PATCH separata e fail-soft dopo il POST, ed esiste già una schermata più ricca per correggerli dopo (`TabClinica.tsx`, tab «Clinica» di `/lavori/[id]/modifica`, con odontogramma FDI + scala VITA — molto più espressiva delle due righe di testo libero del wizard) · la fonte più diretta trovata è **interna**: `ANALISI/05_workflow_completo.md` (flusso reale del laboratorio di Filippo) mostra che «Registra Lavoro» non include mai elemento/colore, e il colore compare solo in «Completamento/Produzione fisica», due fasi dopo · normativamente (Allegato XIII MDR, `ANALISI/17_adempimenti_lab_2026.md`) dente/arcata e colore sono obbligatori per la Dichiarazione di Conformità **alla consegna**, non obbligo di legge trovato per l'apertura del lavoro. **Voce collegata a un punto già aperto in questa roadmap** (censimento `PATCHABLE_FIELDS`/D10 «ogni campo si corregge fino alla consegna», `arcata` oggi senza writer): stessa famiglia di campi, punti distinti. Dettaglio: MEMORY.md **voce 50**.

**Aggiornamento precedente:** 27 luglio 2026 (17) — ✅ **NOME E COGNOME DEL PAZIENTE: SPEC RATIFICATA, NIENTE CODICE.** Il punto 1 della coda dell'ondata parete/home è passato per BP-2 pieno (brainstorming → gate FASE 3 → panel 3×) e si esegue in **sessione nuova**: spec `docs/superpowers/specs/2026-07-27-nome-cognome-paziente-design.md`, handoff `docs/roadmap/2026-07-27-nome-cognome-paziente-execution-handoff.md`. **Le colonne `pazienti.nome`/`cognome` esistono già** e il trigger compone `COGNOME NOME`: niente migration, percorso Media, e la tappa 1 risolve la lamentela **senza toccare `Cassetta.tsx`**. Il panel (1 conferma con riserve, **2 «da rivedere»**) ha trovato tre trappole invisibili ai 3364 test — la peggiore avrebbe reso **non consegnabile** ogni lavoro creato dal wizard senza nome. **Decisioni nuove di Francesco:** D6 due caselle confermate · D7 lavoro in due tappe · D8 il tecnico continua a vedere il nome sulla parete (⚠️ `ANALISI/17` va allineata) · D9 correzione anche dalla scheda del lavoro (⚠️ **apre una tappa 1-bis GRANDE** sulla fotografia congelata). Dettaglio: MEMORY.md **voce 49**.

**Aggiornamento precedente:** 26 luglio 2026 (16) — ✅ **«UN TEMA SOLO»: TUTTE E TRE LE TAPPE IN PRODUZIONE.** Merge `e42601b8`, CI verde, deploy Vercel verde, **verificato su `uachelab.com`**: lo script legge `ua-tema` e **cancella** `ua-theme` senza mai leggerla, la pagina offline è alla chiave nuova, i due fondi sono serviti e del rosso non c'è traccia. L'app aveva **sette** posti che decidevano se essere chiara o scura, con **quattro** regole diverse: ora ne ha **uno** — Impostazioni → Aspetto → Tema (*Automatico* · *Sempre chiaro* · *Sempre scuro*). tsc 0 · vitest **3364 verdi / 19 skip** · build ok · QA **18/18** + prova dal vivo con Francesco dentro l'app. 📌 L'approvazione di `blocked`/`billing` è **CONDIZIONATA** alla revisione in **F2** (D6-bis): se F2 cambia, decade. ➕ Nuovo difetto di leggibilità appuntato a **F1** (token secondari v2.3 in tema scuro, §difetti). Dettaglio: MEMORY.md **voce 48**.

**Aggiornamento precedente:** 26 luglio 2026 (14, **testa corretta il 26/07/2026**) — **ONDATA «REDESIGN PARETE/HOME»: MERGIATA, PUBBLICATA E VERIFICATA IN PRODUZIONE.** Tutti i task chiusi (T16 + T17), merge fatto su via libera esplicita di Francesco: merge commit **`5504a20a`**, `main` a **`8c482e90`**, CI verde, deploy Vercel verde, `uachelab.com` controllata. ⚠️ **Questa riga apriva con «RAMO PRONTO. 🛑 NON MERGIATA — il merge è a parola di Francesco» quando il merge era già avvenuto ed era già online**: è la prima riga che si legge all'avvio di ogni sessione, quindi l'errore si propagava a tutto quello che veniva dopo. Corretta qui; il resto della voce è quello originale. Il ramo si è fermato a `ca913236` ed è storia. Worktree @ `ca913236`, 175 commit, suite **3283 verdi / 19 skip · tsc 0 · build ok** (verificati dal controller, non riferiti). Fatte in questa sessione: T16 striscia (logica + UI) e T17 chiusura completa — pulizia della strumentazione diagnostica, review whole-branch in 5 aree, triage dei **69 rilievi minori → 0 bloccanti**, review delta dei commit di fix, QA browser su 3 viewport × 2 temi, **gate estetico L2 PASS con 0 ❌**. **4 difetti gravi trovati e chiusi**, nessuno dei quali la suite verde poteva vedere: linguetta visibile su desktop con URL desincronizzato · overlay lasciati dipinti sopra un'altra stanza dal tasto indietro · con «Riduci movimento» la linguetta fuori schermo per sempre (unica via alle cassette nella forma «solo pile») · la navigazione mangiata dal ritorno automatico su **10 punti** del codice. **Decisioni di Francesco del 26/07:** nomi paziente rimandati a un'ondata separata DOPO il merge · la striscia nomina il primo allarme e conta gli altri (la forma ratificata il 24/07 è superata: la sua CTA riportava alla home) · il numero del lavoro non si taglia mai · «allunga la pancia della cassetta, non toccare la finestrella» · «Salva il nome/il colore» restano · righe del muro avvicinate da tablet in su. **Backlog dell'ondata, tracciato e quindi sopravvive al merge:** `docs/roadmap/2026-07-26-backlog-ondata-parete-home.md` (30 voci rimandate + 4 raccomandazioni del gate + le riserve delle review). **Prossimo:** il merge è FATTO; Francesco ha installato la PWA e trovato quattro cose — si riparte da lì, `docs/roadmap/2026-07-26-collaudo-pwa-installata-handoff.md` → poi nome+cognome paziente nel wizard (BP-2 pieno).

**Aggiornamento precedente:** 25 luglio 2026 (13) — **ONDATA «REDESIGN PARETE/HOME»: wave H + 2 giri fix post-verifica COMPLETI, codice chiuso (worktree @ `800dd45`, suite 3071/19). NON ancora mergiata.** Chiusi con Francesco: suoni primo tocco (+ WebKit) · linguetta F2/T2 · cassetta B + fascia C · riordino aggancio-al-dito · cassette rotte iPad (width fascia Safari) · sfumatura discendenti (pre-paint). **Piede: coreografia C2 abrogata alla prova device → statico** (semplificazione, −71 test). **Prossimi passi (handoff `docs/roadmap/2026-07-25-wave-h-chiusura-handoff.md`):** verifica finale Francesco su build 800dd45 → chiudi T15 → T16 striscia (valori ratificati nel piano) → T17 chiusura (whole-branch review + MINOR-list dal ledger + rimozione overlay `?diag=suoni` + FASE 9/9b + 🛑 merge a parola). **NUOVI ITEM IN CODA (da questa wave):** (a) «parete centrata su tablet/desktop» — richiesta semplice di Francesco, NIENTE redesign del filo (mockup rete archiviati non ratificati, verbale Add.3); (b) priorità paziente/clinico nella fascia — deferita («lasciamo così per adesso»); (c) drag scattoso iPad → confluisce in «iOS fluidità» già in coda (root cause misurata: FLIP layout-recalc, preesistente alla wave). Su main non pushati: `8b8dd40`+`2b069f0` (rm-guard) — col prossimo deploy.

**Aggiornamento precedente:** 23 luglio 2026 (12) — **ONDATA «REDESIGN PARETE/HOME» PRONTA ALL'ESECUZIONE: piano ratificando eseguito in sessione NUOVA (scelta Francesco).** Piano 17 task TDD in 4 fasi (`docs/superpowers/plans/2026-07-23-redesign-parete-home.md`, commit `5560833`): Fase A fondamenta (alias paziente · scroller parametrico · suoni+🛑ascolto · filtra-e-risali · metti-un-lavoro) → Fase B 🛑 mockup (rete+gancetto+targa · striscia) → Fase C parete visuale → Fase D home (embed parete vera · linguetta C2 · scala fluida · 🛑 QA device metà ondata · striscia · chiusura). **Handoff esecuzione: `docs/roadmap/2026-07-23-redesign-parete-home-execution-handoff.md`** (worktree `redesign-parete-home`, subagent-driven, 5 fermate 🛑). Nessuna migration nell'ondata.

**Aggiornamento precedente:** 23 luglio 2026 (11) — **ONDATA «REDESIGN PARETE/HOME» APERTA: spec RATIFICATA, si scrive il piano.** Brainstorming completo con Francesco (D1-D8: rete metallica disegnata + gancetto, targa cassetta/dentista/paziente con alias>codice e numero lavoro rimosso, striscia «solo quando serve + racconti UÀ» con aggregazione allarmi, due suoni stacco/ri-aggancio [+attenuato su annullo] + haptic, **linguetta C2** ratificata su mockup animato [sostituisce peek+bounce; hit-area ≥44, spegnimento dopo 3 accessi], «La tua home» resta a 3 modi con stanza parete = **pagina cassette vera embedded**, pile centrate + scala verticale fluida della home). Panel advisor 3× CONFERMATA CON RISERVE (23 riserve integrate — le chiave: riordino parametrizzato sullo scroller annidato PRIMA dell'embed, refresh gated in-home, mount differito, alias «codice travestito» da normalizzare, colonne parete → container query, guardie `parete-fluida` da abrogare/sostituire con decision record). Spec: `docs/superpowers/specs/2026-07-23-redesign-parete-home-design.md` (rev.2, §8 verbale panel). Gate visivi residui PRIMA del codice: mockup rete+gancetto+targhe · mockup striscia · 2 coppie suoni d'ascolto. **Restano in coda:** (3) «iOS fluidità» 🛑 device · poi «Miniature 38 + legenda» → D-11 → coda lunga.

**Aggiornamento precedente:** 23 luglio 2026 (10) — **Punto 1 handoff CHIUSO: gap cassette tablet variante C «fluida» deployata** (merge `799ed75`, prod verificata; dettagli MEMORY.md voce 35). Punto 2 (flake vitest) già chiuso ieri. **In coda breve:** (3) «iOS fluidità» — 🛑 serve il device di Francesco per il profiling (dato nuovo: su iPhone 15 niente ghost ma animazioni meno fluide del Xiaomi 17) · (4) «Redesign parete/home» (percorso Grande; la shell fluida di questa ondata renderà davvero continua la formula del gap cassette).

**Aggiornamento precedente:** 22 luglio 2026 (9) — **HANDOFF POST-R3: punto 1 «gap cassette tablet» a mockup pronto (🛑 attende la scelta di Francesco tra le varianti A/B/C/D — screenshot consegnati) · punto 2 «flake vitest di classe» CHIUSO E MERGIATO** (merge `ce877f3`: `skipAnimations` suite-wide + budget catalogo 15s helper unico; A/B 12/12 lane verdi vs CTRL 3/3 rosse; dettagli MEMORY.md voce 34). **In coda breve (invariata):** scelta variante gap cassette → «iOS fluidità» (serve device) → «Redesign parete/home».

**Aggiornamento precedente:** 22 luglio 2026 (8) — **COLLAUDO R3b DEPLOYATO** (merge `69bf6cb`, prod verificata: home 744px intrinseci, no-scroll a 375×755). P9 confermato CHIUSO da Francesco su device · D-2 ok · P-STATUSBAR risolto: non era la status bar ma la **zona morta della scala device-corti** (soglia 700→780 + compatta 744px, §7.1 rev 3.2). **Input per «Redesign parete/home»:** la scala piena della home (~900px a 375w) non entra quasi su nessun device — dimensionamento verticale da ripensare fluido. **Da fare a verifica di Francesco ok:** rimuovere l'overlay diagnostico `?diag=viewport`. **In coda breve (invariata):** mini-giro «gap cassette tablet» → flake vitest → «iOS fluidità» → «Redesign parete/home».

**Aggiornamento precedente:** 22 luglio 2026 (7) — **COLLAUDO R3 MERGIATO E DEPLOYATO** (merge `4709e1f`, CI+CD verdi, prod verificata con overlay diag live). P9 chiuso con root cause provata (ghost click → `useTapScrim`) · D-2 36px · P-STATUSBAR: overlay `?diag=viewport` ORA in prod — attesi screenshot di Francesco dalla PWA installata per il fix vero (poi rimuovere l'overlay). **In coda breve (invariata):** mini-giro «gap cassette tablet» → flake vitest → «iOS fluidità» → «Redesign parete/home».

**Aggiornamento precedente:** 22 luglio 2026 (6) — **COLLAUDO R3 ESEGUITO, branch `worktree-collaudo-r3` pronta — 🛑 ATTENDE RATIFICA (nessun merge).** P9 root cause PROVATA (ghost click Chrome Android sul scrim appena montato, re-hit-test del click sintetico) → fix di classe `useTapScrim` su Sheet+DialogConferma (panel 2× unanime) · D-2 respiro ombra 22→36px misurato · P-STATUSBAR non riproducibile fuori dal device → overlay diagnostico TEMPORANEO `?diag=viewport` (Francesco: attivarlo e mandare screenshot all'avvio della PWA installata). QA touch CDP 10/10 · review Ready-Yes · suite 2777. Dettagli: MEMORY.md voce (31). **In coda breve (invariata):** mini-giro «gap cassette tablet» → flake vitest → «iOS fluidità» → «Redesign parete/home».

**Aggiornamento precedente:** 22 luglio 2026 (5) — **COLLAUDO R2 COMPLETATO** (merge `7baa121`, prod verificata): dot pager riparati (focus preventScroll) · ombra ultima pila · «busta del tap» P9-bis su pointercancel (da validare su device Francesco). **In coda breve:** mini-giro «gap cassette tablet» (varianti da far scegliere). **In «Redesign parete/home» si aggiungono:** nascondere il peek 28px + bounce laterale dopo idle come affordance dello swipe (richiesta Francesco 22/07 sera, coerente con ratifica «stanza-anteprima sparisce»).

**Aggiornamento precedente:** 22 luglio 2026 (4) — **ONDATA «COLLAUDO R1» COMPLETATA: MERGE `6f153a1`, CI+CD VERDI, PROD VERIFICATA.** Eseguita subagent-driven come da handoff (13 commit): P10a back = pagina precedente ovunque (`tornaIndietro`, + fix `BackHeaderModifica` dalla review finale) · P10b testata scheda nel viewport · P9 tap Android (ipotesi validata dal RED esatto) · P11a/b picker iOS reale + value controllato (`facciaHex`) · **P11c rivisto pre-merge su richiesta di Francesco: Variante A «nero fedele» ratificata su screenshot reali** (classe `is-nera` speculare + floor gradiente — il solo gradiente schiarito non bastava, l'anatomia scura era invisibile sul nero) · P7 «×» cross-OS su 4 campi · P3 home senza tagli. Review per task + finale (Ready to merge) · QA E2E 56/56 · L2 senza ❌ · suite 2753/0 · emendamenti a verbale (`docs/design/decisions/2026-07-22-collaudo-r1-emendamenti.md`). **Aggiunte all'ondata «Redesign parete/home» (richieste Francesco 22/07 pomeriggio):** griglia parete come **rete metallica realistica** (riferimento: foto portacassette reali, non foglio a quadretti) · **suono sposta/ri-aggancia cassetta** (sede: `v3/sound.ts`, panel + approvazione). **A carico di Francesco su device:** prova P9/P11/back/testata/«×»/home dopo il deploy. Ordine invariato: (1) flake vitest di classe → (2) «iOS fluidità» (1,2,8) → (3) «Redesign parete/home» (4,6,12+5+13 + le 2 nuove) → «Miniature 38 + legenda» → D-11.

**Aggiornamento precedente:** 22 luglio 2026 (3) — **ONDATA «COLLAUDO R1» PRONTA ALL'ESECUZIONE: spec + piano ratificati, si esegue in sessione NUOVA (scelta Francesco).** Brainstorming chiuso in sessione (domande su P10/P11/P7 risposte, screenshot device analizzati: la testata della scheda sborda simmetricamente dal viewport). Spec con root cause ACCERTATE file:riga (`docs/superpowers/specs/2026-07-22-collaudo-r1-design.md` — P9 emendata: il click naturale post-jitter Android viene ingoiato da `handleClick`) e piano 8 task TDD con codice completo (`docs/superpowers/plans/2026-07-22-collaudo-r1.md`). **Handoff esecuzione: `docs/roadmap/2026-07-22-collaudo-r1-execution-handoff.md`** (worktree `collaudo-r1`, subagent-driven, 🛑 STOP pre-merge). Punto 14 bonus CHIUSO: shortcut Android apparsa dopo reinstallazione PWA (WebAPK rigenerata); su iOS le manifest shortcuts restano non supportate da Safari (limite piattaforma). Ordine post-R1 invariato: flake vitest di classe → «iOS fluidità» → «Redesign parete/home» → «Miniature 38» → D-11.

**Aggiornamento precedente:** 22 luglio 2026 (2) — **COLLAUDO SU DEVICE FATTO (Android + iPhone): «in linea di massima funziona», 13 punti da sistemare — TRIAGE COMPLETO + 4 RATIFICHE.** Triage in `docs/roadmap/2026-07-22-collaudo-device-parete-triage.md`: **5 bug** (home pile coperta dal tasto «+» · «×» ricerca assente su iOS · tap su cassetta libera non apre lo sheet su Android [su iOS corretto: è il comportamento previsto] · scheda lavoro con nav mal formattata/scroll rotti e back hardcoded a `/dashboard` · colore custom rotto: picker iOS, primo «Imposta» che committa il valore sbagliato, nero illeggibile) · **3 problemi piattaforma iOS** (animazioni scattose, sheet senza enter-animation né swipe-down [feature nuova], drag non fluido — indagine WebKit dedicata) · **3 redesign** (striscia sotto il saluto, ricerca, layout targhe/etichette che clippano). **RATIFICHE DI FRANCESCO:** (a) punto 5: la stanza-anteprima in home SPARISCE, lo **swipe porta direttamente a `/cassette`** (ripensare preferenza «La tua home»/StanzePager nell'ondata redesign); (b) punto 6: ricerca **«filtra e risali»** — le non pertinenti spariscono, le trovate risalgono (SOSTITUISCE la decisione di spec «accende»); (c) punto 13: SÌ a **«Metti un lavoro»** nello sheet della cassetta libera; (d) **DIRETTIVA PERMANENTE back = pagina precedente ovunque** (`router.back()` + fallback; incisa in CLAUDE.md §9, censimento back hardcoded incluso). **ORDINE NUOVO (il collaudo ha la precedenza):** (1) flake vitest di classe [resta primo, test-only] → (2) ondata «**Collaudo R1 — bug fix**» (percorso Media: punti 3,7,9,10,11) → (3) ondata «**iOS fluidità**» (punti 1,2,8) → (4) ondata «**Redesign parete/home**» (design-first §0B: punti 4,6,12 + conseguenze 5 + 13). «Miniature 38 + legenda» e D-11 scalano DOPO. Coda invariata in fondo: A8 email Resend · sessione DB · perf-budget.

**Aggiornamento precedente:** 22 luglio 2026 — **PARETE DELLE CASSETTE MERGIATA E DEPLOYATA IN PRODUZIONE — ONDATA COMPLETATA.** Merge `a6d9f50` (60 commit da `4853458`), CI 6m15s + CD Vercel 2m47s **verdi**, prod verificata (shortcut PWA «Le cassette → /cassette» nel manifest live, smoke 307/200). Review finale whole-branch «Merge-ready: Sì» · QA FASE 9 PASS · Gate L2 PASS · suite 2725/19/0 · tsc 0. Worktree e branch rimossi a merge verificato. **In produzione:** `/cassette` con drag touch completo (auto-scroll ai bordi), 8+2 RPC atomiche, storico append-only con deroga di purga, 10 miniature, ricerca «che accende» (tipo+colore, conteggio visibile), preferenza «La tua home» 3 modi, deep-link `?stanza=`. **CODA POST-MERGE, in ordine:** **(1) Flake vitest — intervento di classe, PRIMO task** (commit test-only: `MotionGlobalConfig.skipAnimations` su tutta la suite, protocollo A/B in `.superpowers/sdd/diagnosi-flake-vitest.md` [copia locale conservata]; include `pill.test.tsx` timeout sotto carico). **(2) Ondata nuova «Miniature 38 + legenda in-app»** (direttiva Francesco 22/07): un simbolo per ciascuno dei 38 tipi + legenda in-app (TastoTondo «?» su `/cassette` e voce nello sheet cassetta → Sheet ds con legenda completa, cercabile, raggruppata per famiglia; dati derivati da `TIPI_LAVORO` + mappa miniature — una sola fonte di verità, mai lista duplicata a mano). Impianto proposto: **sistema visivo per FAMIGLIE** (ogni macro-famiglia tiene la silhouette base ratificata, i granulari si distinguono per dettagli codificati — 38 simboli come sistema, non 38 disegni slegati), **ratifica A BLOCCHI per famiglia**; architettura già pronta (mappa a 3 livelli GRANULARE→MACRO→generica del Task 2, zero cambi strutturali); avvertenza di design già detta a Francesco: a 22px la distinguibilità cala col numero di simboli — mitigazione = famiglie+legenda, **da validare col panel UX all'apertura** (Regola Advisor); gate §0B pieno (lavoro di design). **(3) D-11 purga per-tenant** (✓ sezione dedicata presente nel BACKLOG-TECNICO col merge; panel proprio incl. lente normativa su `fatture_sdi_eventi`). Poi coda invariata: A8 email Resend · sessione DB (A20+O4b+RPC `outbox_prepara_draft` orfana) · ricalibrazione perf-budget ~27/07. **A carico di Francesco in produzione:** prova su device del drag (auto-scroll entrambe le direzioni), PWA iOS edge-swipe, consegna→liberazione+racconto, annullo. Checklist L2 §4 corretta: **Plus Jakarta Sans** (diceva DM Sans, deprecata). Dettagli: MEMORY.md voce (26).

**Aggiornamento precedente:** 21 luglio 2026 — **PARETE DELLE CASSETTE: TASK 1→10 COMPLETI, restano 9 task (11→19).** Worktree `.claude/worktrees/parete-cassette`, branch `worktree-parete-cassette`, base `main` @ 4853458 — **34 commit, niente mergiato né pushato**. Suite **2543 passed / 19 skipped**, `tsc` 0, DS-compliance verde, `next build` OK. **Ripresa in sessione NUOVA: `docs/roadmap/2026-07-21-parete-cassette-ripresa-task11.md`.** **QUARTA MIGRATION APPLICATA (`20260721090300`):** il piano affermava il falso — il service client bypassa **RLS**, non i **GRANT di tabella**, quindi `service_role` aveva **solo SELECT** su `cassette` e il Task 4 era **non implementabile** (42501 verificato). Panel advisor 3× **unanime** + ratifica Francesco → due RPC nuove (`cassetta_crea_atomica`, `cassetta_imposta_colore_atomica`), collaudate (70+43 prove) e applicate, con verifica post-apply **anche via PostgREST**. **Ratificato da Francesco:** PATCH cassette = **un campo per chiamata** (422 altrimenti) · `admin_sistema` → **403**, non 200 silenzioso · `db push` autorizzato. **Tre difetti di classe chiusi, validi per tutto il progetto:** `postgrest-js` **non lancia** sugli errori DB (un `try/catch` da solo non intercetta nulla — il difetto era nel piano in **tre** task) · il builder è un **thenable pigro**, quindi `void svc.rpc(...)` non spedisce nulla e un retry che riusa una promise è **teatro** · un esito non gestito che cade nel successo è la **direzione di guasto peggiore**. **Regole nuove:** l'albero non può mai restare rosso su `tsc` (l'hook lo esegue sull'intero progetto a ogni commit) · collaudo migration su **`postgres:17-alpine`** (il live è PG 17.6) · le funzioni `server-only` **sono** testabili. **NON MERGIARE prima del Task 16** (`PATCH numero_cassetta` è oggi un no-op silenzioso in `ConfermaCassettaSheet`/`TabAccettazione`) e **anticipare il seed E2E** del Task 19 prima della QA. **Gate residui: 2** — mockup delle 4 miniature nuove (Task 18) e merge finale. **D-11 invariata**, da aprire dopo l'ondata.

**Aggiornamento precedente:** 21 luglio 2026 — **PARETE DELLE CASSETTE IN ESECUZIONE: migration APPLICATA al DB live (gate 1 superato), Task 1 e 2 completi, restano 17 task (3→19).** Worktree `.claude/worktrees/parete-cassette`, branch `worktree-parete-cassette`, base `main` @ 4853458 — **niente mergiato né pushato**. **Ripresa in sessione NUOVA:** `docs/roadmap/2026-07-21-parete-cassette-ripresa-task3.md` (il piano è già corretto: blocco «STATO AL 21/07» in testa + «CORREZIONI 21/07» dentro ai task 3,4,5,6,8,9,19). **Task 1 rifatto** dopo review + panel advisor 3× + 2 round di audit avversariale: l'SQL ratificato aveva 1 Critical + 7 Important (confermati 8/8, Critical riprodotto senza concorrenza) e i round successivi ne hanno chiusi altri 3. Esito: **3 migration** (`20260721090000` DDL+8 RPC · `20260721090100` patch `admin_delete_laboratorio` · `20260721090200` backfill+audit), applicate il 21/07, FASE 6b OK, **invarianti sul DB live a 0**. **Ratificato da Francesco:** `utente_set_nav_pref` a 4 argomenti con `p_lab` · chip «dal parco» ordinate per `max(cassette_lavori.assegnato_at)` (non `updated_at`) · `REVOKE ALL` + `GRANT SELECT` · D-11 ristretta ora alle sole cassette · nessun esito nuovo nelle RPC. **Criterio di arresto:** l'SQL garantisce la correttezza, la **route ritenta il 40P01**. **Scoperta post-apply:** `numero_cassetta` è NULL su tutti i 288 lavori → il backfill ha operato su un insieme vuoto. **DECADE la nota** «il Cerca globale dovrà unire due fonti per i consegnati pre-cutoff»: la fonte sarà **una sola**, lo storico. Il **seed E2E (Task 19) va anticipato** prima della QA, altrimenti la Parete è vuota ovunque. **Gate residui: 2** — mockup delle 4 miniature nuove (Task 18) e merge finale. **NUOVO DEBITO — D-11 (aprire subito dopo l'ondata, panel proprio incl. lente normativa):** `admin_delete_laboratorio` è **già rotta oggi in produzione** per `fatture_outbox`, `fatture_sdi_eventi` e `credito_clienti_movimenti` (FK NO ACTION, nessuna nella lista di cancellazione — verificato) → cancellazione tenant e art. 17 GDPR bloccate per quei lab. Serve un fix **di classe** con asserzione da `information_schema`. **Registrato in CLAUDE.md §8:** i dati in DB sono **solo di test**, si ripulisce tutto alla consegna → migrazione dati preesistenti = rischio basso, schema/RLS/RPC = rischio alto invariato. Poi Punto 3 handoff (design «Cerca» globale). Coda invariata: A8 email Resend · sessione DB (A20+O4b+outbox orfana) · ricalibrazione perf-budget ~27/07.

**Aggiornamento precedente:** 20 luglio 2026 (7) — **ONDATA A MINI-TRIAGE MERGIATA E DEPLOYATA** (main `50e6b79` ff, CI+CD verdi, prod verificata; conferma esplicita Francesco). Incluso fix TastoSecondario dark su card (ratifica light C + dark A, §5.3 emendato).** Tutti gli 8 punti del handoff: cassetta (targa card + conferma-arrivo + ricerca per-pila), A13 denti in scheda, morte «Le pile», O1i ×3 (Esci mobile, identità NavDesk, trial in striscia), «Persone» v3 (qui nasce il chrome pagina-lista), emendamenti spec in blocco. Review finale «Ready to merge: Yes» · tsc 0 · 2317 test · build OK · QA 31 screenshot (1 Major pre-esistente deferito: TastoSecondario dark su card, richiede emendamento §5.3). Dettagli: MEMORY.md voce (22).

**Aggiornamento precedente:** 20 luglio 2026 (6) — **DIRETTIVA FRANCESCO: TUTTO MIGRA A v3.** Due decisioni ratificate in coda alla sessione: (a) la pagina **/tecnici migra a v3 nell'ondata del mini-triage** (il bottone download cedolini nasce direttamente nel mockup v3 della pagina — niente UI nuova su superfici v2.3); (b) va preparato un **piano ondate v3 esplicito**: censimento di tutte le route ancora v2.3 + calendario di migrazione da ratificare (con panel advisor UX), come voce di roadmap — dettagli nel handoff `2026-07-20-handoff-mini-triage-e-seguito.md` Punto 1-bis. Regola operativa: ogni ondata che tocca una pagina v2.3 ne valuta PRIMA la migrazione completa a v3.

**Aggiornamento precedente:** 20 luglio 2026 (5) — **P2-d1 DEPLOYATO** (merge `cc29822`, CI+CD verdi, smoke OK; conferma esplicita Francesco): all'emissione di un draft di anno precedente la bozza DIVENTA la fattura dell'anno corrente (nuovo numero/serie/data, traccia ex-bozza in `fatture.note`); stesso anno congelato, INSERT intatto, TD04 conserva `collegata_*`. tsc 0 · **2286 pass** · build OK. Punto 2 del handoff 20/07 CHIUSO. **Nuovo backlog DB:** RPC `outbox_prepara_draft` orfana (outbox smontata 10/07, ricreata per errore dalla migration date-fiscali) — da rimuovere nella sessione DB A20+O4b. **PROSSIMO: mini-triage design (A13/A14/O1h/O1i + bottoni download export) → A8 email Resend → sessione DB A20+O4b+outbox-orfana.** Dettagli: MEMORY.md voce (16).

**Aggiornamento precedente:** 20 luglio 2026 (4) — **LEDGER RICONCILIATO + BUNDLE E (A16) DEPLOYATO + DECISIONE DRAFT NYE.** (a) **Ledger migration riconciliato:** 7 migration 16-17/07 registrate in `schema_migrations` (verifica read-only preliminare: tutte già applicate, incluse le policy storage `ricevute-sdi`); `migration list` pulita 76/76 → **`db push` di nuovo utilizzabile**; sbloccata la sessione DB A20+O4b. (b) **Bundle E — A16 chiuso e deployato** (merge `da8e436`, CI 5m57s verde + CD 2m22s, smoke prod OK): `GET /api/lavori/export` (CSV annuale lavori, esente N13 portabilità + blocco blacklist) e `GET /api/tecnici/cedolini-batch` (CSV compensi mensili per tecnico×voce, guarded N13, RBAC titolare/admin_rete); helper condivisi `csv.ts` (anti CSV-injection OWASP), `paginate.ts` (oltre cap PostgREST 1000, fail-closed), `mese.ts`; retrofit `fatture/export` su helper + paginazione. FASE 7: tsc 0 · vitest **2281 pass** · build OK; 6 task review + review finale «READY TO MERGE» (0 Critical/Important, 6 minor a backlog: validazione parametri unificata year/mese; decisione soft-delete + voci omonime cedolini). UI download → mini-triage design (decisione Francesco). (c) **Decisione Francesco draft dic→gen: variante (d)** — all'emissione di bozza d'anno precedente la fattura esce SEMPRE con numero/serie/data dell'anno corrente, la bozza vecchia resta come bozza con riferimento alla fattura nuova; panel advisor 3× agli atti (fiscale: riserva da documentare per commercialista); **implementazione in corso questa sessione** (percorso Grande FatturaPA). **PROSSIMO: P2 variante (d) → mini-triage design → A8 email Resend.** Dettagli: MEMORY.md voci (14)-(15).

**Aggiornamento precedente:** 20 luglio 2026 (3) — **DATE FISCALI EUROPE/ROME DEPLOYATE** (merge `3d5fd31`, percorso GRANDE FatturaPA: panel 3× confermato, review Yes, migration `20260720150000` applicata al DB live e registrata; tsc 0 · 2238 pass · build OK · CI+CD verdi). Anno/serie/data ora coerenti dal giorno civile di Roma su TUTTI i percorsi di emissione (route, batch, XML, RPC SQL outbox/nota-credito); `generaProgressivo` richiede l'anno del chiamante. Chiuso anche il **flaky test** consegna/avviso (5 run consecutive + stress 2 suite parallele verdi). **⚠ Anomalia da riconciliare in sessione dedicata: 7 migration 16-17/07 non registrate nel ledger remoto — MAI `supabase db push` fino ad allora** (dettagli MEMORY voce 13). Fuori scope tracciati: draft dic→gen congelato · serie lavoro/ordine UTC (non fiscali). **PROSSIMO: Bundle E (A16) → mini-triage design.** Dettagli: MEMORY.md voce (13).

**Aggiornamento precedente:** 20 luglio 2026 (2) — **BUNDLE T (tecnico) MERGIATO E DEPLOYATO** (merge `bacfde9` su main, 11 commit; tsc 0 · vitest **2229 pass** · build OK; review advisor «merge SÌ» dopo 2 fix Important). Chiusi: **O1b** (helper unico «oggi» Europe/Rome `src/lib/utils/data-roma.ts` con test DST; adottato in dashboard queries/home/admin-live/agenda/tutto-il-resto/pile-home + 4 date operative API ordini/lavori/richiedi/lotti — le date FISCALI fatture/XML restano UTC di proposito: dominio FatturaPA → task dedicato segnalato), **O1a** (4 rami prose pile coperti), **O4a** (ClienteComboBox → GET /api/clienti, chiusa injection filtro PostgREST), **A18** (hash SHA-256 firma DdC, **cut-off 20/07 senza backfill** — decisione Francesco: dati storici = test) + **anti-SSRF** campi URL storage (validazione valore in PATCH impostazioni + guardia hashFirmaDdc + anti-traversal), timeout 5s webpush, fix commento drift O4b, coverage AnnullaConsegnaBanner. **Decisione Francesco: A8 fallback email Resend = SÌ, pianificata post-Bundle E** (notifiche via email per utenti senza push attive). A6-bis (regex check-ds + sweep `--c-amber`) resta in backlog. Follow-up review (minor): TOCTOU hash/PDF firma, param limit su /api/clienti, residui UTC minori (striscia oggiMezzanotte, sheet client, produttività), test flaky `flusso-consegna` sotto carico. **PROSSIMO: Bundle E (A16 export CSV) → mini-triage design.**

**Aggiornamento precedente:** 20 luglio 2026 — **§A+§O: censimento completo ratificato + BUNDLE Q (quick wins) MERGIATO E DEPLOYATO** (main `04cf00b`, CI verde, prod verificato). Chiusi: A1 (push assegnazione tecnico), A3 (guard prefill passkey), A5 (theme_color), A6-res (amber-ink /qualita), A7 (link portale↔richiedi), A8-push (notifica lab su richiesta portale), A9 (copy), A12 (a11y ComboBox), A17-res (hydration banner; 4/5 superficie morta), O1c-parti, O1f (segnale tecnico senza anagrafica). Già risolti de facto: A2, A4. Ordine certificato: Bundle T (O1b→O1a, O4a, A18) → Bundle E (A16) → mini-triage design (A13/A14/O1h/O1i, mockup unico giro). Deferral con calendario: A20+O4b (sessione DB post-§A/§O), A15+O2 (sequenza 3 design), A19 (V2), O4c (sp.4). Dettagli: `docs/roadmap/2026-07-20-censimento-a-o.md` + MEMORY.md voce (11).

**Aggiornamento precedente:** 17 luglio 2026 (7) — **Chiuse anche le code di N14: collaudo login reale PASS (p75 1804ms ≤ 2000, utente E2E), `PERF_BUDGET_LOGIN` 4500→4000 (run perf verde), cap 3 proposte passkey + sezione Sicurezza «Attiva accesso rapido» in /impostazioni (deployata e verificata su prod).** Deferiti by design: restyling modal passkey → gate estetico L2 di ondata. **La sequenza (1) «resto dei problemi» ha ora SOLO §A e §O davanti**, poi si passa a (2) funzioni attive. Dettagli: MEMORY.md voce (10).

**Aggiornamento precedente:** 17 luglio 2026 (6) — **N11-bis ✅ · N14 ✅ · N13 ✅ TUTTI MERGIATI E DEPLOYATI IN PRODUZIONE, guard `lab.stato` in ENFORCE** (autorizzazione Francesco, shadow saltato: PWA senza utenti). Guard su 87 route API + portale (API e pagine) + interceptor client + ban sessioni su blacklist; doc GDPR out-of-band in `docs/security/` (prerequisito R4 soddisfatto); QA enforce verificata su prod col lab E2E (blacklist→404, sospeso→read ok, ripristinato). CI verde, CD Vercel OK (`6991c42`). Kill-switch: `UA_LAB_GUARD_MODE`. Dettagli: MEMORY.md voce (9). **Sequenza (1) «resto dei problemi»: restano §A e §O**, poi (2) funzioni attive.

**Aggiornamento precedente:** 17 luglio 2026 (5) — **Sequenza (1) «resto dei problemi»: N11-bis ✅ e N14 ✅ implementati (working tree main, commit da confermare); N13 ✅ IMPLEMENTATO su branch `worktree-n13-lab-guard` (guard `lab.stato` su 87 route API + portale API+pagine + interceptor client + ban sessioni su blacklist; default shadow, kill-switch `UA_LAB_GUARD_MODE`; tsc 0 · vitest 2139 · build OK · review 10 finding tutti fixati) — in attesa di: merge, deploy shadow 24-48h, doc GDPR out-of-band in `docs/security/` (bloccante), flip enforce.** Dettagli: MEMORY.md voce (8) e `docs/superpowers/plans/2026-07-17-n13-lab-guard.md`. Restano poi §A e §O prima del passaggio a (2) funzioni attive.

**Aggiornamento precedente:** 17 luglio 2026 (4) — **P0-PERF CHIUSO lato server: R2 mergiato e deployato (`3fbabca`→`5008f39`), migration N12 applicata, QA prod PASS, TTL token 900s. MISURA FINALE: TTFB pagine p75 198ms (budget ≤300 ✅, mediana 175ms = −78% dal baseline), API p75 152ms (≤250 ✅), ex-outlier riconciliazioni 660→191ms ✅. Login→dashboard 2.758ms vs ≤2.000: residuo CLIENT-side (nuovo N14, decisione UX di Francesco).** Prossimo nella sequenza ratificata: resto dei problemi (N13, N11-bis, N14, §A, §O) → funzioni attive → design coerente → audit multi-agente → collaudo capillare.

**Aggiornamento precedente:** 17 luglio 2026 (3) — **P0-PERF: R1 deployato (−56% TTFB) e R2 COMPLETO sul branch `worktree-p0-perf-r2` in attesa dei gate di Francesco** (apply migration N12 → merge → re-misura vs budget; dettagli e checklist in `P0-PERF-DIAGNOSI-2026-07-17.md` §7). R2 chiude anche N11 (sicurezza `deleted_at`, ~140 occorrenze) e N12 (atomicità prove + fix flusso UI TabProve). Nuovi item backlog: N13 (lab.stato nelle API), N11-bis (impersonate soft-deleted). La sequenza operativa resta quella ratificata il 17/07 (voce sotto): dopo P0-PERF si prosegue con (1) resto dei problemi → (2) funzioni attive → (3) design coerente → (4) audit multi-agente → (5) collaudo capillare.

**Aggiornamento precedente:** 17 luglio 2026 (2) — **NUOVA SEQUENZA OPERATIVA RATIFICATA DA FRANCESCO** (sostituisce l'ordine «audit multi-agente → sp.4» della voce precedente): **(1) risolvere tutti i problemi** — a partire dal nuovo item urgente **P0-PERF: la PWA in produzione è lentissima** (ogni operazione e ogni caricamento pagina troppo lenti — segnalazione diretta di Francesco 17/07; «la PWA deve essere velocissima e fluida»; diagnosi sistematica avviata in questa sessione) — poi il resto del backlog tecnico (§A, §N residui, §O); **(2) tutte le funzioni attive e funzionanti** (M21–M27, feature monche/orfane); **(3) design corretto, applicato e coerente su tutta la PWA** (sp.4 «Le sezioni» con Fatture+Scadenzario per prime, redesign admin O2, residui UI §M); **(4) audit multi-agente completo**; **(5) collaudo capillare di Francesco, pagina per pagina** (chiusura — gli errori grafici e le cose da ridisegnare viste oggi si definiscono e segnalano LÌ, a completamento avvenuto; O3 resta deferito a quel punto della sequenza).

**Aggiornamento precedente:** 17 luglio 2026 — **ONDATA DS v3 «FONDAMENTA RESIDUE + 4b CONSEGNA»: COMPLETATA, MERGIATA E PUSHATA** (`main` `872fe6c` fast-forward, 26 commit; tsc 0 · vitest 2008 pass | 19 skipped · build ok · check-ds ✅). Eseguita via subagent-driven-development (17 task TDD, review per-task + review finale whole-branch «Ready to merge: Yes» dopo 3 fix: `tab=dati` nel ponte modifica via `risolvi-tab.ts` condiviso, deep-link `?consegna=1` gated su consegnabilità, nota annullo `role=alert`). **Fase F chiusa** (sp.3 «Il cuore» completo): TastoWhatsApp §5.29, RigaBloccante §5.30, FotoStrip §5.33, MenuVoce §5.34, NavDesk in `components/ds/`, check-ds sui CSS globali, catalogo +5. **Fase C chiusa:** flusso Consegna v3 in-place su scheda/pila/split/HomeDesktop (2 tocchi ovunque), pagina `/consegna` morta → redirect, GET `precheck-consegna` read-only blindato, moduli orfani eliminati con sweep test, **suono firma «ua» + haptic success al frame (§9.1 — era già in spec, il piano l'aveva omesso)**. **FASE 9 QA browser lab E2E: tutti gli scenari PASS** (incl. annullo con DdC `annullata` a registro, D-1 verificato a DB). **FASE 9b gate L2: PASS** (22 screenshot + audit in `docs/design/screenshots/2026-07-16-consegna-v3/`). Backlog nuovo §O8 (drift `consegna_finalizza_atomica`, check-ds in CI, csrf Origin assente). **PROSSIMO: audit multi-agente → sp.4 «Le sezioni» (Fatture+Scadenzario per prime).**

**Aggiornamento precedente:** 16 luglio 2026 (2) — **ONDATA DS v3 «FONDAMENTA RESIDUE + 4b CONSEGNA»: DESIGN + SPEC + PIANO PRONTI E APPROVATI (su `main` locale, docs-only, non pushati).** Rientro nel binario DS v3 deciso da Francesco. Brainstorming 16/07 con 3 panel advisor (solution-architect + ux-designer + appsec-auditor sul design; backend-api + test-runner su errori/test — 5× CONFERMATA CON RISERVE, tutte integrate). **Decisioni ratificate D-1…D-6:** numerazione DdC resta a t=0 con annullo tracciato (nota bucket C 09/07 **SUPERATA** con parere normativo: Art. 52(8) impone la DdC prima dell'immissione, All. XIII non impone numerazione, ISO 13485 §4.2.4); il frame «Consegnato!» non elenca la fattura (fatturazione concordata — riga quieta statica senza link); ruoli consegna = status quo documentato (tutti i ruoli lab, parità GET/POST); NavDesk **variante A** (tasto locale H52 invariato, decisione visiva su mockup `2026-07-16-navdesk-tasto-varianti.html`); **approccio A in-place** (la pagina `/lavori/[id]/consegna` muore → redirect `?consegna=1`, flusso in scheda+pile+split+HomeDesktop 1280, 2 tocchi ovunque); warning materiali dentro il DialogConferma (MaterialiWarningSheet muore). **Scoperte chiave dai panel:** `runPrecheckMdr` (precheck-mdr.ts) è codice morto — la GET nuova riusa `precheckMDR` di produzione; il 422 `errore_pdf` è sovraccarico su 7 esiti → copy generica + Riprova (retry sicuro, ramo idempotente); helper `materiali-carenti.ts` estratto dal loop BOM. **Fase F:** TastoWhatsApp §5.29 + RigaBloccante §5.30 nuovi, FotoStrip §5.33 + MenuVoce §5.34 estratti, NavDesk in `components/ds/`, check-ds esteso a globals.css/ds-v3.css (allowlist font + anti-leak), catalogo +5 sezioni. **Zero migration, POST consegna intatto** (unica aggiunta server: GET precheck-consegna read-only). Spec `docs/superpowers/specs/2026-07-16-ds-v3-fondamenta-residue-4b-consegna-design.md` (`98dc114`); piano `docs/superpowers/plans/2026-07-16-ds-v3-fondamenta-residue-4b-consegna.md` (`ce16326`, **17 task TDD**, Fase F Task 1-7 → Fase C Task 8-15 → documentale Task 16 → FASE 7 Task 17). **PROSSIMO: eseguire il piano via subagent-driven-development in worktree `.claude/worktrees/fondamenta-4b-consegna`; poi audit multi-agente → sp.4 «Le sezioni» (Fatture+Scadenzario per prime).**

**Aggiornamento precedente:** 16 luglio 2026 — **RICONCILIAZIONI R1: COMPLETATA, MERGIATA E DEPLOYATA** (`main` `defa346`, 28 commit, CI+CD verdi, smoke prod OK — `/fatture/riconciliazioni` live). Chiude il percorso «parser-first»: contabilità `annullo_storno` a delta, tabella append-only `fatture_sdi_eventi`, parser+quarantena ricevute SdI (firma in fallback quarantena-all, spike XAdES documentato), RPC writer unico `applica_ricevuta_sdi`, override/sblocco claim atomici solo titolare, pagina «Da sistemare»; suite 1954 pass | 19 skipped. **Prossimo binario deciso da Francesco:** rientro nel percorso DS v3 — **fondamenta residue** (5 componenti ds mancanti: `TastoWhatsApp`, `RigaBloccante`, `FotoStrip`, `MenuVoce`, spostare `NavDesk` in `components/ds`; `check-ds` esteso a `globals.css`) **+ 4b Consegna** → **audit multi-agente** → **sotto-progetto 4 «Le sezioni»** (Fatture+Scadenzario per prime). **Backlog nuovo da R1:** motore firma reale (prerequisiti: 2-3 ricevute SdI reali + trust anchor produzione; piste xmlsec-wasm/patch filter2/microservizio EU-DSS); cap «max eventi parcheggiati aperti» **deliberatamente rinviato** al motore firma reale (in fallback bloccherebbe uso legittimo); minor triaged a backlog: dedup race 23505→duplicata, error-handling fetch evento cross-route, contrasto t2/t3 dark sistemico, CTA override su gruppo claim orfani (da valutare in mockup futuro), «Riprova lo storno» con più TD04 mostra solo l'ultimo numero, badge `/fatture` esegue aggregazione completa invece di COUNT dedicato.

**Aggiornamento precedente:** 15 luglio 2026 (4) — **RICONCILIAZIONI PENDENTI + RICEVUTE PEC (item B): DESIGN + SPEC + PIANO PRONTI E APPROVATI (su `main` locale, docs-only, non pushati).** Direzione **C «parser-first»** (panel unanime su delega di Francesco, ratificata): superficie `/fatture/riconciliazioni` con upload ricevuta XML SdI → verifica firma XAdES fail-closed → RPC writer unico `applica_ricevuta_sdi` forward-only; trigger TD04 riscritto a **contro-movimento `annullo_storno` a delta** (mai DELETE); alert saldo credito negativo; fix compliance `pec_sdi_address` (D-6) + guardia anti-regressione su `send-pec` (D-7); sblocco claim orfano solo manuale titolare con audit (D-3). IMAP = ondata futura sulla stessa pipeline (backlog). Panel validazione spec: 3× CONFERMATA CON RISERVE, tutte integrate (rev.2). Spec `docs/superpowers/specs/2026-07-15-riconciliazioni-ricevute-pec-design.md`; piano `docs/superpowers/plans/2026-07-15-riconciliazioni-ricevute-pec.md` (17 task TDD, fasi R1a contabile + R1b pipeline; 🛑 gate: 3 apply migration, 2 mockup §0B, spike XAdES, merge). **PROSSIMO: eseguire in sessione nuova via subagent-driven-development in worktree dedicato (handoff `docs/roadmap/2026-07-15-riconciliazioni-execution-handoff.md`).**
**Aggiornamento precedente:** 15 luglio 2026 (3) — **N10+N9 (INVIO PEC A SDI): MERGIATA E PUSHATA** (`main` merge --no-ff `9310d21`, gate esplicito di Francesco; CI/CD in verifica). Piano 6 task eseguito via subagent-driven-development (review spec+qualità per task, tutte Approved). Endpoint `POST /api/fatture/[id]/invia-pec` + helper claim `invio-claim.ts` + test-contratto invariante `send-pec` (passato subito) + hardening ramo `invia_pec` di `/xml` + bottone «Invia a SdI» **Variante A** (scelta di Francesco al gate §0B) con riga stato SDI granulare. Fix chiave da QA browser: `STATO_SDI_LABEL` in modulo condiviso `src/lib/fattura/stato-sdi-label.ts` (l'export da modulo client è client-reference nel Server Component). FASE 7: tsc 0 err · 1795 pass | 19 skipped (+46) · build ok. QA lab E2E PASS con cleanup a baseline verificato (PEC fittizia smtp.invalid, zero PEC reali; progressivo 2026-0006 consumato). Gate L2: screenshot+audit in `docs/design/screenshots/2026-07-15-invia-pec-sdi/`. BACKLOG: N9+N10 → ✅ risolti; follow-up nuovi: rate-limit per-lab invii PEC, cron riconciliazione ricevute PEC, connectionTimeout nodemailer. Review finale: READY TO MERGE (0 Critical/Important). CI+CD verdi, smoke prod OK. **PROSSIMO (scelta di Francesco): item B — Riconciliazioni pendenti + ricevute PEC (handoff `docs/roadmap/2026-07-15-riconciliazioni-handoff.md`).**

**Aggiornamento precedente:** 15 luglio 2026 (2) — **N10+N9 (INVIO PEC A SDI): DESIGN + PIANO PRONTI E APPROVATI (su `main` locale, docs-only, non pushati).** Endpoint dedicato `POST /api/fatture/[id]/invia-pec` (solo `generata`, ruoli `titolare`+`front_desk`, claim anti-doppio-invio su `smtp_inviata_at`, riuso puro `sendFatturaPEC`) + bottone «Invia a SdI» in `/fatture/[id]` con riga stato SDI granulare + hardening ramo `invia_pec` di `/xml` (gate ruolo, claim, no-leak). Nessuna migration. Panel 3 advisor (architect+backend+appsec): «CONFERMATA CON RISERVE» ×3, zero bloccanti, riserve recepite (spec rev.2). Spec `docs/superpowers/specs/2026-07-15-n10-n9-invio-pec-sdi-design.md`; piano `docs/superpowers/plans/2026-07-15-n10-n9-invio-pec-sdi.md` (6 task TDD, 🛑 gate mockup §0B al Task 4, 🛑 gate merge finale); handoff `docs/roadmap/2026-07-15-n10-execution-handoff.md`. **PROSSIMO: eseguire in sessione nuova via subagent-driven-development, worktree `.claude/worktrees/n10-invia-pec`.**
**Aggiornamento precedente:** 15 luglio 2026 — **NOTA DI CREDITO TD04: MERGIATA E PUSHATA** (`main` `d0d83c8` --no-ff; push `8267c23..d0d83c8`). Storno integrale di fatture inviate a SdI: 6 migration additive (RPC atomica claim-first, credito `storno` cappato, trigger annullo-effetti su TD04 rifiutata), XML TD04 con DatiFattureCollegate da snapshot congelato, audit completo delle letture contabili, write-guards, route due-fasi con resume, UI variante B (menu ⋯ + sheet 2 step) su `/fatture/[id]`. QA E2E 4 scenari PASS, gate L2 passato, review finale + delta READY TO MERGE. **N5 risolto** (la feature TD04 lo ingloba). +73 test (1749 pass). **Follow-up in BACKLOG §N:** invio TD04→SdI, riconciliazioni pendenti + audit rifiuto, asimmetria `scaduta`, bollo per-lab. La voce «Nota di credito XML (TD04)» della lista feature (riga V2.x) è **implementata**.
**Aggiornamento precedente:** 14 luglio 2026 (7) — **QUICK-WIN FISCALI N6+N7: MERGIATE E DEPLOYATE** (`main` `2dfbfd7`, --no-ff; push `7755b47..2dfbfd7`; CI verde, CD Vercel success + health ✓; smoke uachelab.com 307→/login → 200). **N7** gate `stato_sdi` (409 se non `draft`) su route XML fattura. **N6** invariante bollo fuori dal dovuto pre-fattura documentato (decisione C). Nuovo backlog **N9** (PEC-resend). **Nessun Blocker aperto** — B1–B22 + N4/N6/N7/N8 chiusi.
**Aggiornamento precedente:** 14 luglio 2026 (6) — **N4 (FONTE DI VERITÀ DEL PREZZO LAVORO): MERGIATA E DEPLOYATA** (`main` `b025d61`, --no-ff; push `2190c56..b025d61`; CI verde, CD Vercel success + health ✓, smoke uachelab.com → 307 `/login` → 200; 15 commit `9dac640..38c1a5e` + fix/docs). Eseguito il piano da 10 task via `superpowers:subagent-driven-development`. Helper unico read-time `prezzoEffettivoLavoro` (`src/lib/domain/prezzo-lavoro.ts`) + refactor di tutti i lettori (`generate-xml`, `contabilita/queries`, `contabilita/registra-pagamento`, `portale/[token]/fatturazione`, `scadenzario`, `lavori/pronti-da-fatturare`) + rimozione prefiltro `.gt('prezzo_unitario',0)` nei 3 siti che scartava lavori con prezzo solo nelle righe + guard server `PATCH /api/lavori/[id]` (422 se `prezzo_unitario` non-null con righe attive, carve-out azzeramento) + assertion Natura N4 in `generaFatturaPA` + badge divergenza read-time in `LavoriInAttesaSection` (nuovo token `--c-amber-ink`, Variante B scelta da Francesco). Nessuna migration. +61 test (**1670 pass | 4 skipped**), `tsc`/`build` puliti. Riconciliazione read-only pre-deploy: **0 lavori divergenti su 286** (rollout a freddo). Mergiata e deployata (gate esplicito di Francesco); CI+CD verdi, prod live. Follow-up tracciati (NON parte di N4, in BACKLOG-TECNICO §N): "bollo nel dovuto", gate `stato_sdi==='draft'` mancante su `api/fatture/[id]/xml`, CSS invalido su `urgencyPillBg`/`urgencyPillBorder`.

**Aggiornamento precedente:** 14 luglio 2026 (5) — **N4 (FONTE DI VERITÀ DEL PREZZO LAVORO): DESIGN + PIANO PRONTI E APPROVATI (su `main` locale, non pushati).** Scelto da Francesco dopo la 3b; task fiscale GRANDE (FatturaPA). Due fonti di prezzo mai sincronizzate (contabilità legge `prezzo_unitario`, fattura usa somma righe; sintomo 322€/112€) → helper unico read-time `prezzoEffettivoLavoro` (righe se esistono, altrimenti prezzo_unitario) + refactor di tutti i lettori (generate-xml, contabilità, portale/scadenzario/pronti-da-fatturare) + rimozione prefiltro `.gt('prezzo_unitario',0)` in 3 siti (bug di completezza) + guard server PATCH (carve-out azzeramento) + assertion Natura N4 + badge divergenza; **nessuna migration**. Fuori scope: margine/compenso KPI, editor righe (sp.3), branded Euro. Validato da 3 advisor specializzati (advisor + solution-architect + backend-api). Spec `docs/superpowers/specs/2026-07-14-n4-fonte-verita-prezzo-lavoro-design.md`; piano `docs/superpowers/plans/2026-07-14-n4-fonte-verita-prezzo-lavoro.md` (10 task TDD); handoff `docs/roadmap/2026-07-14-n4-execution-handoff.md`. **PROSSIMO: eseguire in sessione nuova via subagent-driven-development, worktree `worktree-n4-prezzo`.** Follow-up tracciato: "bollo nel dovuto".

**Aggiornamento precedente:** 14 luglio 2026 (4) — **ONDATA 3b (slice: nota dentista + reskin form ponte): COMPLETATA, MERGIATA E DEPLOYATA** (`d5ebb0c..751a544` su `main`, fast-forward 13 commit; CI verde, CD Vercel success + health check, smoke uachelab.com OK; worktree rimosso). Eseguito il handoff via subagent-driven-development (10 task TDD + review per-task + review finale whole-branch). **P1:** migration `20260714120000` applicata al DB **live** (3 colonne additive, ereditano RLS `lavori`); `POST /api/portale/richiedi` scrive `note_dentista`/`da_portale`/`paziente_codice_richiesta` puliti e lascia `note_interne` NULL; predicato puro `isNuovaRichiestaDentista` + `useRealtimeNotifiche` (fix realtime bloccante); `NotaDentista` read-only in scheda (attribuita al dentista, separata dalla nota lab — verificata a runtime); buono stampa `note_dentista`; 3 colonne fuori da `PATCHABLE_FIELDS` (test-locked). **P4:** reskin form ponte via aliasing CSS-var su scope `[data-ds="v3"].lavoro-form-v3` (compound selector, fix bug dark `--sh-b/--sh-i:none`) + sweep font DM Sans→`var(--font-v3,sans-serif)` (form/ + LavoroFormClient/TabProve/sheet, guard allargato) + tab oro→v3 (indicatore rosso) + focus rings + touch 44px. **FASE 7:** tsc 0 err · vitest verde (47 "fail" = timeout da carico, 23/23 PASS in isolamento) · build OK. **Note deploy non bloccanti:** migration senza backfill (righe portale vecchie `da_portale=false`); rate-limit senza `laboratorio_id` eq (sicuro). **Deferiti tracciati (NON in slice):** P3 rebuild nativo (O6j); **N4 fonte prezzo** (task fiscale GRANDE); chat portale WhatsApp. **PROSSIMO:** review finale whole-branch presentata → merge/push (gate Francesco) → CI/CD → smoke uachelab.com. Poi P3/N4 e prossime superfici DS v3.

**Aggiornamento precedente:** 14 luglio 2026 (3) — **ONDATA 3b (slice: nota dentista + reskin form ponte): DESIGN + PIANO PRONTI E APPROVATI (su `main` locale, non pushati).** Francesco ha scelto **B (Ondata 3b)**; in brainstorming l'ondata è stata **decomposta** in una slice a basso rischio + deferiti. **Slice = P1 (nota dentista) + P4 (reskin form ponte).** **P1:** migration additiva `lavori` (`note_dentista`/`da_portale`/`paziente_codice_richiesta`) → il write path portale scrive campi puliti (oggi stipa tutto in `note_interne` come `RICHIESTA_DENTISTA|Paz|Note`), 🔴 fix `useRealtimeNotifiche` (2° consumatore del marcatore), display `NotaDentista` nella scheda, nota sul buono di lavorazione, colonne fuori da `PATCHABLE_FIELDS`. **P4:** reskin form ponte a v3 via aliasing variabili su scope `.lavoro-form-v3` (fixa bug dark latente) + sweep font DM Sans→font-v3 + tab oro→v3 (reskin, NON rebuild). **Deferiti tracciati:** P3 rebuild nativo flussi ⋯ (YAGNI, O6j); **N4 fonte di verità del prezzo** (bug fiscale reale, task GRANDE dedicato, §N4); chat portale WhatsApp (feature futura su `messaggi`). Spec `docs/superpowers/specs/2026-07-14-ondata-3b-nota-dentista-reskin-ponte-design.md` (2 review specializzate integrate); piano `docs/superpowers/plans/2026-07-14-ondata-3b-nota-dentista-reskin-ponte.md` (10 task TDD); handoff `docs/roadmap/2026-07-14-ondata-3b-execution-handoff.md`. **PROSSIMO: eseguire il piano in sessione nuova (contesto pulito) via subagent-driven-development, worktree `worktree-ondata-3b-nota-reskin`; Task 1 al gate apply migration; gate L2 dark×3viewport.** Poi restano P3/N4 e le prossime superfici DS v3.

**Aggiornamento precedente:** 14 luglio 2026 (housekeeping) — **POLISH LIVELLO 1 SCHEDA v3 + HOUSEKEEPING COMPLETATI.** **Polish L1** (primo 🟢 L1 della strategia audit UI/UX): scheda v3 rifinita (route `/lavori/[id]` + `/modifica` registrate in `SCHEDA_V3_RE` → avatar+BottomNavPill legacy si ritirano; a11y focus-ring blu, aria «Modifica consegna», predicato consegna state-aware; layout desktop V3 «Bilanciata» rail+stage 60/40; RifacimentoButton→token v3) — **MERGIATA + DEPLOYATA** (`c420609..d2696ec` su `main`, CI verde, CD Vercel success, smoke uachelab.com OK; suite **1596 pass | 4 skipped**). **Gate estetico 🟡 L2 integrato nel workflow** (`ua-app/CLAUDE.md` BP-2 FASE 9b + REGOLA ZERO «mai mergere UI senza gate L2» + §0B «più varianti in anteprima»). **Housekeeping** (`docs/roadmap/2026-07-14-housekeeping-handoff.md`): pushati i 2 commit docs pendenti (`cdc38c6`+`18a57b9`, CI verde); rimossi i 2 worktree già mergiati (`dashboard-v2-rewrite`, `worktree-fix-ua-list-grid-reduced-motion`); **`plan-c-dashboard-rbac`** (7 commit non mergiati, 920 dietro — dashboard-work morto/sostituito dalle pile, admin-preview+impersonation già in main, uniche parti uniche import DentalMaster + e2e Piano E ma stale) → **archiviato** in tag `archive/plan-c-dashboard-rbac` (pushato) + rimosso, per **decisione esplicita di Francesco**. Stato repo: un solo worktree, albero pulito, `main` sync. **PROSSIMO (scelta di Francesco, menu handoff §3):** **A.** Consegna→portale dentista (deciso 10/07, dominio critico FatturaPA/N4 → percorso GRANDE) · **B.** Ondata 3b (deferiti scheda: form ponte v3, `note_dentista`+portale, N4 prezzo, flussi ⋯ nativi) · **C.** prossima superficie DS v3 (impostazioni/magazzino/qualità/fatture/portale/admin, ognuna un'ondata col gate L2).

**Aggiornamento precedente:** 13 luglio 2026 (3) — **DS v3 «Il cuore» — ONDATA 3a (SCHEDA LAVORO v3): COMPLETATA, MERGIATA E DEPLOYATA** (`8e55ebe..1369630` su `main`, 15 commit; CI verde, CD Vercel success; smoke prod `/lavori/[id]/modifica`→307). **Follow-up: strategia AUDIT UI/UX a 3 livelli impostata in `docs/design/audit-ui-ux/` (L1 micro-pass per-superficie SUBITO → L2 gate di fine ondata → L3 audit capillare totale a v3 completo, prima dell'audit generale). PROSSIMA SESSIONE: eseguire `docs/design/audit-ui-ux/LIVELLO-1-scheda-v3.md`.** Worktree `ondata-3a-scheda` (branch `worktree-ondata-3a-scheda`, 14 commit `eec71f7..5b1d748` da `8e55ebe`), eseguita via `subagent-driven-development` (11 task TDD + review per-task + review finale whole-branch Opus «Ready to merge: With fixes»). `/lavori/[id]` è ora **scheda-vista v3** (`SchedaLavoroV3`): header ‹·n.+pill·⋯; CardInfo con modifica per-riga via Sheet (dentista/tecnico/consegna/note, PATCH ottimistico + rollback); strip foto read-only; **CardFasiV3** gesto FATTA; **CONSEGNA** gated `derivaUrgenza().consegnabile`→push `/consegna`, disabled+callout altrimenti, mai nascosto; Rifacimento/Segnala contestuali; banner annullo; **AvvisoTracciabilita** MDR. Menu ⋯ 6 voci (4 **ponte** `/lavori/[id]/modifica?tab=…` → `LavoroFormClient bridged` [sopprime SOLO CONSEGNA, tiene Salva] + Documenti hub + Annulla **disabled**). Desktop card centrata (≥768, max-width 640). **Zero API nuove, zero migration.** **DECISIONE Francesco:** niente campo nota-dentista nello schema → `note_interne` mostrata come **nota onesta del laboratorio** (no attribuzione dentista, tappabile); vera nota del dentista (colonna `note_dentista` + portale + display) = **3b**. **Bug FK-refresh trovato dalla review finale e fixato** (`5b1d748`: edit tecnico/dentista non si aggiornavano in-sessione — sync prop→state dopo `router.refresh()`). Verifiche: tsc pulito · vitest **1590 pass | 4 skipped** · build OK · DS-compliance OK. **PROSSIMO (gate Francesco):** QA browser lab E2E `00000000-…-0001` (MAI lab Filippo, 3 viewport × 2 temi, edit persistono, cleanup a baseline) → merge fast-forward su `main` + push → CI/CD Vercel → smoke uachelab.com. Poi **3b** (flussi ⋯ pesanti nativi + N4 prezzo + nota dentista) e 4b (UI Consegna).

**Aggiornamento precedente:** 13 luglio 2026 (2) — **DS v3 «Il cuore» — ONDATA 3a (SCHEDA LAVORO v3): DESIGN + PIANO PRONTI E APPROVATI (su `main` locale, non pushati).** Ondata 3 (Scheda lavoro) si **decompone in 3a + 3b**. **3a** = `/lavori/[id]` da form multi-tab v2.3 a **scheda-vista v3** (header ‹·n.+pill·⋯; CardInfo con **modifica per-riga via Sheet**; NotaDentista; strip foto read-only; **CardFasiV3** con gesto FATTA; **CONSEGNA** che naviga a `/consegna` v2.3 quando consegnabile [come il `TastoConsegnaInline` della pila], disabled+callout altrimenti; Rifacimento/Segnala contestuali; banner annullo). Menu ⋯ = 6 voci del mockup: Prezzi&lavorazioni/Dati clinici/Prove/Foto **a ponte** (route `/lavori/[id]/modifica` → `LavoroFormClient bridged` che sopprime solo CONSEGNA e tiene Salva); **Documenti** hub nativo; **Annulla lavoro** disabilitata (no backend → stage dedicato). Desktop = card centrata; upgrade pannello pile = follow-up. **Zero API nuove, zero migration, zero dominio fiscale** (N4 → 3b). Spec `docs/superpowers/specs/2026-07-13-ds-v3-il-cuore-ondata-3a-scheda-design.md`; piano `docs/superpowers/plans/2026-07-13-ds-v3-il-cuore-ondata-3a-scheda.md` (11 task TDD). 3 review advisor integrate. **PROSSIMO: eseguire il piano in sessione nuova (contesto pulito) via subagent-driven-development, worktree `ondata-3a-scheda`.** Poi 3b (flussi pesanti + N4) e 4b (UI Consegna).

**Aggiornamento precedente:** 13 luglio 2026 — **DS v3 «Il cuore» — ONDATA 2 (WIZARD NUOVO LAVORO) COMPLETATA, MERGIATA E DEPLOYATA** (`432e5f8..d5ffd3c` su `main`, 25 commit; CI+CD verdi; smoke prod `/lavori/nuovo`→307). `/lavori/nuovo` è ora il **wizard v3 a 3 passi** (dentista → tipo → paziente → Fatto); il form multi-tab v2.3 è morto (resta per `/lavori/[id]`). Contenuto: **tassonomia granulare 38 tipi in 10 famiglie** (`tipi-lavoro.ts`; macro in `tipo_dispositivo`, label granulare in `descrizione`, classe di rischio default) · **macro nuovo `bite_splint`** (migration applicata in prod, CHECK a 10 valori) · validazione enum 422 (B2) + label consolidate (B4) · componenti ds **ChipScelta §5.31 + ProgressDots §5.32**, **TileScelta a due righe** (`nomeRiga2`, ratifica Francesco) · tempi medi cascata (`GIORNI_FALLBACK_LIBERO=7`, domenica→lunedì) · sheet «Nuovo dentista» 4 campi (A7) · catalogo tipi + «Descrivilo» · Passo 3 paziente alias opt-in GDPR + foto · creazione **fail-soft** + frame «Fatto!» (consegna suggerita + Cambia data PATCH + CTA foto) · persistenza abbandono 24h. **QA browser funzionale** su lab E2E (invarianti DB verificati: tipo_dispositivo=MACRO, label granulare, classe corretta, data, enum 422, bite_splint 201) + **1 bug critico server/client scoperto in QA e fixato** (`inizioGiorno` chiamato dal server → modulo neutro `src/lib/date/giorni.ts`). Suite 1561 pass | 4 skipped. Review finale whole-branch «YES». **Backlog nuovi (BACKLOG-TECNICO §O5):** ESLint guard import client-da-server; `chiudiRipresa` pronto=false; ratifica visiva animazioni; deviazione stimaGiorni fallback. **PROSSIMO: Ondata 3 (Scheda lavoro v3)**, poi 4b UI Consegna, salvo ripriorizzazione di Francesco.

**Aggiornamento precedente:** 12 luglio 2026 (sera) — **DS v3 «Il cuore» — ONDATA 1 (HOME + PILE) COMPLETATA E DEPLOYATA** (`bb0c084..bee02fa` su `main`, 24 commit; suite 1373 pass | 4 skipped; zero migration). Home v3 a 4 pile unica per tutti i ruoli (le 4 dashboard per ruolo sono MORTE), `/lavori?pila=` + «Le pile», StrisciaStato gerarchica, «Tutto il resto», desktop 1280 a 3 pannelli + split 768 (ADR B6: searchParams server-driven, ratificato), adapter `derivaUrgenza` (E4), legge madre v3.1, BottomNavPill+avatar ritirati dalle route migrate, anteprima admin a Home v3 read-only. Decisioni al gate: perimetro tecnico fail-closed · avatar → destinazione finale a roadmap («Esci» §7.16, riga-identità NavDesk) · emoji Vuoti ratificate. Residui in BACKLOG-TECNICO §O1. **PROSSIMO: Ondata 2 — Wizard nuovo lavoro** (mockup `wizard.html` approvato, 3 tocchi), poi Ondata 3 Scheda e 4b UI Consegna.

**Aggiornamento precedente:** 12 luglio 2026 — **DS v3 «Il cuore» — ONDATA 0 MOCKUP COMPLETATA, APPROVATA DA FRANCESCO E MERGIATA** (`3d883af..cdc3d2a` su `main`, 22 commit, docs-only + script screenshot; suite 1297 pass | 4 skipped, tsc pulito). 7 mockup in `docs/design/mockups/2026-07-09-il-cuore/` (home a **4 pile**, pila-aperta, wizard, scheda, consegna, tutto-il-resto, stati-vuoti-errori) + kit `_base.css` + 50 screenshot (3-4 viewport × 2 temi, assert anti-scroll home). Processo: 7 task SDD review-approvati → review finale whole-branch (1C+4I fixati) → **6 advisor professionali** (visual/UX/odontotecnico/normativo/a11y/DS-eng) → bucket A (10 fix: fasi zirconia, bloccante prescrizione MDR, copy normativo, wizard una-decisione…) → bucket B (7 decisioni Francesco: faint AA, verdi scuriti, **4ª pila viola «DA RIFARE / IN PROVA»**, spike architettura in Ondata 1…). Decision doc: `docs/design/decisions/2026-07-09-il-cuore-mockups.md` (deviazioni ratificate + **revisioni di legge da emendare**: §3 faint+viola, §3.3 regola 2, §3.3.4, §4.2, §5.4, §5.7/§7.1 quattro pile, §5.9 IN PROVA, §5.14, §5.17). **PROSSIMO: piano Ondata 1 (Home+pile)** via `superpowers:writing-plans`, che parte con lo **spike architettura desktop route↔pannelli** (decisione B6) e l'**emendamento della legge madre + token** (`ds-v3.css`/`tokens.ts`: viola, faint, verdi WhatsApp/PillFase) prima dei componenti.

**Aggiornamento precedente:** 11 luglio 2026 (pomeriggio, 2) — **Follow-up review Ondata 3 CHIUSO** (worktree `fix-followup-ondata-3`, 2 commit): **fixati** `getContabilitaCliente`/`fetchMovimentiCreditoValidi` fail-closed su errori di lettura (mai più saldo parziale silenzioso; try/catch coerente nelle route scadenzario cliente + credito applica/rimborsa — la route portale già catturava) e a11y `aria-controls`/`id` sui collassabili della Situazione economica. Suite 1297 pass | 4 skipped, review indipendente «Ready to merge: Yes». **Tracciati come task dedicati nel BACKLOG-TECNICO §N:** **N4 — fonte di verità del prezzo lavoro** (`prezzo_unitario` vs righe `lavori_lavorazioni` divergono, la fattura usa le righe e la contabilità il campo, route PUT lavorazioni orfana; decisione di design da prendere nel redesign scheda lavoro DS v3 sp.3, PRIMA di fatturare lavori con righe importate) e **N5 — TD01 hardcoded in `generaFatturaPA`** (prerequisito bloccante della feature note di credito TD04). **PROSSIMO: sequenza DS v3 «Il cuore»** (mockup Ondata 0 → Home+pile → Wizard → Scheda → 4b UI Consegna), salvo ripriorizzazione di Francesco.

**Aggiornamento precedente:** 11 luglio 2026 (pomeriggio) — **Ondata 3 «situazione economica» (Portale Dentista v2) COMPLETATA E DEPLOYATA → PORTALE DENTISTA v2 COMPLETO (ondate 0-3 tutte chiuse)** (`d7d4e1a..8629d9a` su `main`, CI+CD verdi, smoke prod ok, ZERO migration). Contenuto: sezione «Situazione economica» nel portale dietro PIN — card saldo a 4 numeri (stessa funzione `getContabilitaCliente` dello scadenzario lab: coerenza per costruzione), dettaglio dovuti (estratto conto completo, D-O3-1) e pagamenti registrati (`getPagamentiCliente` nuova, mai `metodo_nota`), audit `view_situazione` fail-loud; mockup approvato (gate 0B). Suite 1293 pass | 4 skipped; review finale Opus «Ready to merge YES» 0 Critical/Important. Backlog dal branch (non bloccanti): `.error → throw` in `getContabilitaCliente`; a11y `aria-controls` sui collassabili; indagine `prezzo_unitario` vs righe listino nel batch. **PROSSIMO: sequenza DS v3 «Il cuore»** (mockup Ondata 0 → Home+pile → Wizard → Scheda → 4b UI Consegna), salvo ripriorizzazione di Francesco. Backlog caldo invariato: TD01 hardcoded in `generaFatturaPA` da fixare prima di emettere note di credito TD04.

**Aggiornamento precedente:** 11 luglio 2026 — **Ondata 2 «storico fatture» (Portale Dentista v2) COMPLETATA E DEPLOYATA** (`febafbe..18c7c76` su `main`, CI+CD verdi, smoke prod ok). Pre-check I-6 VERDE (bucket `fatture-pdf` privato; `xml_url` deprecata, zero reader/writer). Contenuto: copia di cortesia PDF generata con l'XML (`FatturaCortesiaTemplate` + `fatture.pdf_storage_path`, migration applicata con gate Francesco); sezione «Fatture» nel portale dietro PIN (lista per anno + download signed URL 300s pattern B5, audit `view_fatture`/`download_fattura` fail-loud); mockup approvato (gate 0B). 3 bug pre-esistenti fixati: `<Data>` XML ≠ data fattura; pagina `/fatture/[id]` mai apribile (colonne inesistenti nella select + embed). Suite 1274 pass | 4 skipped; review finale Opus «Ready to merge YES» 0 Critical/Important. Backlog nuovo IMPORTANT: TD01 hardcoded in `generaFatturaPA` — da fixare prima di emettere note di credito TD04. **PROSSIMO: Ondata 3 — situazione economica nel portale** (saldo, fatture da pagare/pagate, pagamenti — query `src/lib/contabilita/` lato dentista, dietro PIN), ultima ondata del Portale Dentista v2. Poi la sequenza DS v3 (mockup «Il cuore» → Home+pile → Wizard → Scheda → 4b UI Consegna), salvo ripriorizzazione di Francesco.

**Aggiornamento precedente:** 10 luglio 2026 (sera) — **Ondata 0 «pulizia» (Portale Dentista v2 — fatturazione concordata) COMPLETATA E DEPLOYATA** (`6628ace..c8cac17` su `main`, CI+CD verdi). La 4a-server era stata INTERROTTA al Task 8 da Francesco (il modello «emetti salvo rifiuto» contraddice il principio della fatturazione concordata col clinico) → nuova spec `docs/superpowers/specs/2026-07-10-portale-dentista-v2-fatturazione-concordata-design.md` (rev.2 post-audit 3 advisor) con 4 ondate. **Ondata 0 chiusa:** emissione fiscale inline rimossa (B-1), batch unico writer di `fatture.lavoro_id` (B-2), migration di pulizia outbox applicata al DB live (gate Francesco, pg_net rimossa), RPC annullo col doppio gate fiscale + P2-1 chiuso (DdC → `annullata` funziona davvero), gate B1 server-side 422, 11 lettori DdC filtrano le annullate; +1 bugfix pre-esistente scoperto in QA (il batch fatture non poteva MAI generare una fattura — claim/load in contraddizione). Review finale Opus «Ready to merge» 0 Critical/Important; QA lab E2E completa. **Ondata 1 COMPLETATA (11/07, worktree ondata-1-portale, review finale Ready-to-merge — in attesa env Vercel + merge): lista «Da fatturare» dietro PIN con esclusione a doppia sorgente, proposta del dentista, conferma nello scadenzario, I-2 allowlist, D7 blindato. PROSSIMO dopo merge: Ondata 2 (storico fatture, pre-check I-6).** Poi Ondata 2 (storico fatture, pre-check I-6 bucket `fatture-pdf`) e Ondata 3 (situazione economica). La sequenza DS v3 (mockup «Il cuore» → Home+pile → Wizard → Scheda → 4b UI Consegna) resta in coda dopo il portale, salvo ripriorizzazione di Francesco.

**Aggiornamento precedente:** 10 luglio 2026 (notte) — **Ondata 4a-server: design approvato + piano pronto** *(POI INTERROTTA AL TASK 8 — vedi aggiornamento sopra)* — spec `docs/superpowers/specs/2026-07-09-ondata-4a-server-consegna-fiscale-design.md` (validata da 3 advisor: architect/appsec/sre) e piano 18 task `docs/superpowers/plans/2026-07-09-ondata-4a-server-consegna-fiscale.md`, entrambi su `main`. Decisioni: pg_cron+pg_net · DdC annulla+rigenera · policy «emetti salvo rifiuto» con claim `incluso_in_fattura` (chiude anche la doppia fatturazione auto+batch preesistente) · osservabilità completa con sezione `/admin/coda-emissione`. **PROSSIMA SESSIONE (contesto pulito): esecuzione del piano in worktree via subagent-driven-development; il Task 8 (apply migration + Vault + env) è gated su conferma esplicita di Francesco.** Dopo la 4a: Ondata 0 mockup → 1 → 2 → 3 → 4b (sequenza sp.3 §12 invariata).

**Aggiornamento precedente:** 09 luglio 2026 (notte) — **P2 (pre-check chirurgico consegna/annullo/SDI + data layer) COMPLETATO** — report: `docs/roadmap/P2-PRECHECK-CONSEGNA-SDI-2026-07-09.md` (sola ispezione, zero modifiche a codice/schema, verifiche su DB live). B1/B2/B3+C4 confermati con dettagli chirurgici (B1 più ampio: perfino un lavoro `annullato` è consegnabile via API — gate solo client-side); 10 nuovi item P2-1…P2-10 con triage: **nessun S1**, tutti S2 (dentro il perimetro Ondata 4a: annullo-DdC no-op da sempre, doppia fattura su annullo+riconsegna, UNIQUE DdC pieno vs annulla-e-rigenera, fire-and-forget inaffidabile + draft orfano, progressivi da consumare solo all'emissione, gate annullo su fattura inviata) o S3 (dead code, residui minori, nota `in_ritardo` lazy che conferma `derivaUrgenza`/E4). pg_cron già attivo → candidato naturale per l'outbox E3. **La sequenza del binario unico è CONFERMATA. Prossimo step: Ondata 4a-server** (brainstorm+piano dedicati, worktree, TDD puro zero UI, review rafforzata fiscale, FASE 6b).

**Aggiornamento precedente:** 09 luglio 2026 (notte, dopo binario unico) — **P1 (B22 migration repair) RISOLTO** (commit `ee52f09` su `main`): 26 migration fondative pre-02/07 registrate in history via `migration repair --status applied` una alla volta con verifica contenuti contro DB live; 13 file rinominati a versioni univoche (collisioni di versione 20260517/18/20/21); scoperto e sanato 1 file mai applicato davvero (`realtime_replica_identity` — `REPLICA IDENTITY FULL` applicato ora a `lavori`/`fatture` sul DB live, gap reale per `useRealtimeNotifiche`). Done verificato: `migration list` 50/50 pulito, `db push --dry-run` "up to date". Dettaglio: `BACKLOG-TECNICO-2026-07-02.md` §B22. **Blocker aperti residui: NESSUNO** (restano solo i §N etichettati). **Prossimo step della sequenza: P2 — pre-check chirurgico consegna/annullo/SDI + data layer**, poi Ondata 4a-server.

**Aggiornamento precedente:** 09 luglio 2026 (notte) — **BINARIO UNICO ADOTTATO: backlog tecnico e redesign DS v3 unificati in una sola sequenza (decisione Francesco, validata da 2 advisor: engineering manager + solution architect).** Emendamenti E1-E7 ratificati e incisi nella spec sp.3 (`docs/superpowers/specs/2026-07-09-ds-v3-il-cuore-design.md` §2.1): **sequenza operativa = B22 (migration repair, PRIMO) → pre-check chirurgico consegna/data-layer → Ondata 4a-server (fix fiscali B1/B2/B3+C4, outbox+cron, `STATI_CONSEGNABILI`, worktree dedicato) → Ondata 0 mockup → 1 Home+pile (adapter `derivaUrgenza`) → 2 Wizard → 3 Scheda → 4b UI Consegna → collaudo Francesco → residui → AUDIT MULTI-AGENTE COMPLETO → sotto-progetto 4 (coi finding come priorità)**. **Decisione (09/07 notte): tutti i collaudi interni li esegue Francesco — UÀ è un prodotto per molti laboratori, non l'app di uno; i test sul campo con laboratori pilota saranno pianificati da Francesco a PWA stabile.** Operating model (E6): un solo writer di codice per repo; ROADMAP/MEMORY scrivibili solo dalla sessione primaria; reconcile-before-write (stati verificati contro `git log`); interruzioni S1/S2/S3; WIP limit 1 ondata + 1 interstiziale; DoD di ondata include la riconciliazione documentale; registro ADR-lite (decisione UX ↔ item tecnico ↔ ondata). Triage nuovi item: tocca una superficie in migrazione? → nell'ondata pertinente; altrimenti → BACKLOG-TECNICO etichettato. Race inviti → gate pre-distribuzione utenti reali. **Correzione stato (verificata via `git merge-base --is-ancestor`): B6 e B16 erano GIÀ risolti e deployati l'08/07** (`335ef88`, `30bbeef` entrambi antenati di `main`) — la voce B14 qui sotto li elencava come aperti per drift documentale da sessioni concorrenti (lo stesso fenomeno che l'operating model E6 elimina). **Blocker aperti residui REALI: solo B22** (non bloccante) + nuovi item etichettati in BACKLOG-TECNICO §N.

**Aggiornamento precedente:** 09 luglio 2026 — **B14 (`tecnici.compenso_base` ambiguo) RISOLTO, MERGIATO E DEPLOYATO IN PRODUZIONE** (worktree `b14-tipo-compenso-tecnico`, fast-forward su `main`, pushato, CI verde, deploy Vercel confermato, `uachelab.com` 200 — worktree e branch rimossi). `tecnici.tipo_compenso` (già in schema/DB live, mai collegato a nulla) ora esposto nel form tecnico e usato per disambiguare la card Compenso in `ProduttivitaTecnico.tsx`: fisso → stipendio statico; percentuale/per_lavorazione → barra target invariata; non classificato → nudge esplicita "Tipo compenso non specificato". Nessuna migration necessaria. Dettaglio completo: `memory/MEMORY.md` §0 e `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` §B14. ~~**Blocker aperti residui:** B6 (Service Worker offline), B16 (query `/ordini` non supportata), B22~~ *(correzione 09/07 notte: B6 e B16 erano già risolti e deployati l'08/07 — stato stale da sessione concorrente, verificato via git; l'unico residuo reale è B22, non bloccante).*

**Aggiornamento precedente:** 09 luglio 2026 (sera) — **DS v3 Sotto-progetto 3 «Il cuore» — DESIGN COMPLETATO E APPROVATO (spec figlia + piano Ondata 0 su `main`); prossima sessione: esecuzione mockup.** Brainstorming multi-advisor (UX dominio + ricerca web + fattibilità dati: zero API nuove). Decisioni chiave ratificate da Francesco: home UNICA per tutti i ruoli; mappatura stati→pile con vocabolario esteso (`FERMO`, `DA IERI`, `−N GIORNI`); wizard 4 tocchi senza modifiche API; **il form multi-tab muore** (modifica a sheet per-RigaDato + flussi ⋯); muore la pagina `/lavori/[id]/consegna` (DialogConferma + sheet bloccanti); annullo consegna 5→10 min + **emissione fiscale differita a fine finestra**; fix server autorizzati: gate stato consegna in `orchestraConsegna()` (oggi solo UI!), verifica CHECK `dichiarazioni_conformita.stato` su DB reale prima di toccare l'annullo. **TRACCIATI per Sotto-progetto 4 (Le sezioni):** «I conti» (KPI titolare — margine, pagamenti scaduti, da fatturare, su cache esistente) e «Il mio compenso» (tecnico) — decisione esplicita: nessuna casa-ponte per i KPI tra sp.3 e sp.4. **Backlog nuovi:** workflow firma DdC (`firmata_at` mai scritto); deprecazione `in_ritardo` come stato. Spec: `docs/superpowers/specs/2026-07-09-ds-v3-il-cuore-design.md`. Piano Ondata 0 (mockup, gate approvazione Francesco schermata per schermata): `docs/superpowers/plans/2026-07-09-ds-v3-il-cuore-ondata-0-mockup.md`. Piani Ondate 1-4 (Home+pile / Wizard / Scheda / Consegna) da scrivere SOLO dopo i mockup approvati. Dettaglio: `memory/MEMORY.md` §0.

**Aggiornamento precedente:** 09 luglio 2026 — **REDESIGN DS v3 «Una cosa alla volta» — Sotto-progetto 2 (componenti core) COMPLETATO E APPROVATO DA FRANCESCO (gate §14.2 chiuso 09/07/2026), IN MERGE su `main`** (pivot deciso da Francesco, B20-successivi in pausa). **Sotto-progetto 2** su worktree `.claude/worktrees/ds-v3-componenti`, branch `worktree-ds-v3-componenti` — 16 task base via `superpowers:subagent-driven-development` + refinement rounds dopo 2 round di feedback live di Francesco sul catalogo: tutti e 27 i componenti §5 completi in `src/components/ds/` (TastoPrimario/Secondario/Tondo, LinkQuieto, PillTempo/PillStato/PillFase, TastoPiu, Avatar, TileScelta/Nuovo, RigaCerca, Pila, StrisciaStato, CardLavoro, CardInfo/RigaDato, RigaFase/CheckTondo, Sheet, DialogConferma, Avviso, Skeleton, Vuoto, Campi testo/numero/data, BarraMateriale, EroeTuttoAPosto, CardUAHaFatto, NotaDentista, GiornoAgenda/RigaAgenda, PillVoce). **2 redesign nati dal feedback live e ora legge:** TastoPiu «il punto rosso» (spec §5.2 rev 2) e PillVoce «la pill di carta» (spec §5.15 rev 2); **suoni reali** scelti da Francesco (tap/fatta/arrivo/errore, sostituiscono la sintesi provvisoria — `ua` resta sintetico); Sheet con swipe-to-dismiss (§5.16, gap chiuso) e bug scroll-lock Critical risolto. Catalogo pubblico `/ds-v3-catalogo` ora **14 sezioni** (+Suoni, §9.1), toggle tema, in `PUBLIC_ROUTES`, con 6 screenshot QA rigenerati post-approvazione in `docs/design/catalogo-v3/` (390/768/1280 × light/dark). Suite **1106 passed | 4 skipped** (era 707 alla baseline `main`, +399), `tsc` pulito, zero regressioni. Spec (legge di riferimento): `docs/superpowers/specs/2026-07-07-design-system-v3-una-cosa-alla-volta.md` (evoluta on-branch: §5.2 rev 2, §5.15 rev 2, §9.1). Dettaglio completo: `memory/MEMORY.md` §0. **Prossimo:** merge su `main` (non fast-forward, main avanzato a `3de835d`) → push → deploy Vercel → verifica prod → **sotto-progetto 3 «Il cuore»** (Home, pile, wizard, scheda lavoro, flusso Consegna — spec §14.3).

**Aggiornamento precedente:** 08 luglio 2026 — **REDESIGN DS v3 «Una cosa alla volta» IN CORSO** (pivot deciso da Francesco, B20-successivi in pausa). **Sotto-progetto 1 (fondamenta in codice) COMPLETATO** su worktree `.claude/worktrees/ds-v3-fondamenta`, branch `worktree-ds-v3-fondamenta` — **MERGIATO E DEPLOYATO** (fast-forward `12246d0..9fde21d` su `main`, worktree e branch rimossi; nota di correzione: questa voce diceva erroneamente "non ancora mergiato" quando fu scritta). 10 task via `superpowers:subagent-driven-development`, tutti review-approvati: font Plus Jakarta Sans self-hosted, dizionario parole vietate + linter runtime, token v3 (con correzione WCAG ambra light → `#9A5C00`), CSS scoped `[data-ds="v3"]` (mai `:root`, coesistenza totale con v2.3), motion v3 (5 molle Apple + 8 coreografie), 5 suoni WAV provvisori + player Web Audio, haptic Android-only, estensione DS-compliance guard. Suite 707 passed | 4 skipped (era 670), `tsc`/`next build` puliti. Spec (legge di riferimento): `docs/superpowers/specs/2026-07-07-design-system-v3-una-cosa-alla-volta.md`. Dettaglio completo: `memory/MEMORY.md` §0.

**Aggiornamento precedente:** 07 luglio 2026 — **B20 (PSUR/PMS Report differenziato per classe di rischio) RISOLTO, MERGIATO E DEPLOYATO IN PRODUZIONE** (fast-forward `58ac033..3f106ea` su `main`, pushato, CI verde, deploy Vercel confermato, `uachelab.com` risponde 200 — worktree e branch rimossi, 10 task via `superpowers:subagent-driven-development`). Sostituisce l'attuale "PSUR" unico indifferenziato con: Classe I → PMS Report (Art. 85, nessuna cadenza fissa), Classe IIa → PSUR biennale (Art. 86), Classe IIb/III → PSUR annuale (Art. 86). Migration DB live applicata (`psur.gruppo_classe` + vincolo UNIQUE esteso, conferma esplicita di Francesco). 2 finding Important corretti in review (fail-closed su chiavi ereditate da `Object.prototype` in `rilevaGruppi`; colore testo `--gold` non WCAG-conforme nel badge "Bozza", CLAUDE.md violato dal codice del piano stesso — confermato da Francesco prima della correzione). **QA browser manuale eseguita (lab E2E, mai lab Filippo) ha scoperto un bug bloccante reale**: il bottone "Genera" falliva sempre con 400 (form HTML nativo invia `application/x-www-form-urlencoded`, la route leggeva solo JSON — bug preesistente a B20, reso bloccante dal nuovo campo obbligatorio `gruppo_classe`) — corretto e riverificato dal vivo (PMS Report e PSUR generati con successo, coesistenza confermata, 390/768/1280px light/dark OK). `tsc`/`vitest` (663/4 skipped, era 645)/`next build`/DS-compliance tutti puliti. Dettaglio completo: `memory/MEMORY.md` §0. Spec: `docs/superpowers/specs/2026-07-07-b20-psur-pms-classe-rischio-design.md`. Piano: `docs/superpowers/plans/2026-07-07-b20-psur-pms-classe-rischio.md`. **Prossimi Blocker aperti dopo B20:** B6 (Service Worker offline, fix rapido), B14 (`tecnici.compenso_base` ambiguo), B16 (query `/ordini` non supportata).

**Aggiornamento precedente:** 06 luglio 2026 — **CRUD `cicli_produzione` COMPLETATO, MERGIATO E DEPLOYATO** (merge fast-forward `65777ac` su `main`, pushato su `origin/main`, worktree e branch feature rimossi) — chiude il follow-up scoperto durante la QA di B5 ("nessun modo di creare un ciclo via UI/API"). Migration live (indice UNIQUE parziale `(laboratorio_id, codice) WHERE deleted_at IS NULL`, pattern B18), nuove route `POST`/`PATCH`/`DELETE /api/cicli[/id]`, sheet create/edit e bottone elimina in UI. Review finale (Opus) "Ready to merge: Yes" dopo 2 fix Important (validazione PATCH allineata a POST, payload PATCH costruito come delta per non rompere modifica su valori storici fuori lista canonica). 626/626 test, tsc/build puliti. QA browser end-to-end su creazione/modifica/eliminazione (mai lab Filippo); non verificato manualmente il blocco 409 su DELETE referenziato né il viewport tablet. Dettaglio completo: `memory/MEMORY.md` §0. Spec: `docs/superpowers/specs/2026-07-06-cicli-produzione-crud-design.md`. Piano: `docs/superpowers/plans/2026-07-06-cicli-produzione-crud.md`.

**Correzione doc (06/07/2026):** verificato via `git log`/`git branch --contains` che B4 (as any PDF), le norme armonizzate MDR §7, e B13 (1/2 + 2/2, entrambe le parti) sono **già mergiati su `main`** da sessioni precedenti — i relativi paragrafi più sotto e in `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` dicevano ancora "non ancora mergiato", testo non aggiornato dopo il merge effettivo. Nessun'azione di merge necessaria per queste voci, restano solo come riferimento storico.

**Prossima priorità (07/07/2026): B20** (PSUR/PMS Report non differenziato per classe di rischio, `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` sezione B20) — unico Blocker aperto senza piano dettagliato, richiede prima una sessione di brainstorming/design. Altri Blocker aperti dopo B20: B6 (Service Worker offline, fix rapido), B14 (`tecnici.compenso_base` ambiguo), B16 (query `/ordini` non supportata). Handoff completo: `memory/SESSION_ACTIVE.md`.

**Aggiornamento precedente:** 05 luglio 2026 — **Norme armonizzate applicate (MDR §7) RISOLTO E COMMITTATO** (worktree `worktree-norme-armonizzate-ddc`, 5 commit, non ancora mergiato su `main`) — chiude il follow-up aperto da B4. `rischi_tipo_dispositivo` estesa con `norme_json JSONB` (migration applicata al DB live con conferma esplicita di Francesco), editor/API/`generateDdC()`/`DdcTemplate.tsx` tutti aggiornati per validare, persistere e stampare l'elenco delle normative armonizzate (`{codice, titolo, anno?}`) in una nuova sezione condizionale §6-bis della DdC. 6 task via `superpowers:subagent-driven-development`, tutti approvati senza Critical/Important. `tsc`/`vitest` (478/4 skipped, era 466)/`eslint`/`next build` puliti. Dettaglio completo: `memory/MEMORY.md` §0. Spec: `docs/superpowers/specs/2026-07-05-norme-armonizzate-ddc-design.md`. Piano: `docs/superpowers/plans/2026-07-05-norme-armonizzate-ddc.md`. **Ancora da fare:** merge su `main`, push, deploy, QA browser.

**Aggiornamento precedente:** 05 luglio 2026 — **B4 RISOLTO** (`as any` nei generatori PDF MDR, 11/11 eliminati, branch `worktree-b4-pdf-generators-type-safety`, non ancora mergiato su `main`). Helper `renderPdfDocument()` isola il cast boundary renderer (9 occorrenze → 1); `getTypedServiceClient()` (nuovo, dopo un pivot tecnico scoperto durante l'implementazione — la tecnica di design originaria non compilava su un client Supabase non tipizzato) sostituisce i cast di accesso dati senza toccare il client condiviso `getServiceClient()`. Aggiunta `validateDpaData()` (unico gap reale di validazione trovato). Test coverage costruita da zero per gli 8 generatori (prima solo `DdcTemplate` era testato): 466/4 skipped (era 450). **Scoperto e corretto come effetto collaterale (Task 12): bug di produzione reale** — `generateDdC()` falliva su ogni chiamata reale su `main` (colonna `testo_conformita` NOT NULL mai valorizzata + colonna fantasma `norma_riferimento` inviata all'INSERT), mascherato dal client non tipizzato. `tsc`/`vitest`/`eslint`/`next build` tutti puliti. Dettaglio completo: `memory/MEMORY.md` §0. Spec: `docs/superpowers/specs/2026-07-05-b4-pdf-generators-type-safety-design.md`. Piano: `docs/superpowers/plans/2026-07-05-b4-pdf-generators-type-safety.md`. **Backlog aperto:** tipizzare `getServiceClient()` con `<Database>` (147 file, root cause reale); `precheck-mdr.ts` non verifica P.IVA del laboratorio; `norme_json` (MDR §7) mai popolato da `generateDdC()` — task separato in corso.

**Aggiornamento precedente:** 05 luglio 2026 — **2 fix minori segnalati da Francesco (QA sessioni precedenti) RISOLTI E DEPLOYATI** (merge fast-forward `85faad1` su `main`, pushato, CI verde, deploy Vercel confermato). (1) `PazientiSearchList.tsx` migrato al layout responsive `ua-list-grid` (era `flex-column` inline) — chiude il follow-up aperto da B9 (`task_8422a838`); scoperto in verifica visiva live che anche `ClientiSearchList.tsx` (il riferimento) aveva lo stesso bug (card edge-to-edge, 0px padding orizzontale) — corretto in entrambi con conferma di Francesco, non solo replicato. (2) `useReducedMotion()` in `motion.ts` aveva lo stesso hydration mismatch già risolto in B18 per `useTheme.ts` — risolto con lo stesso pattern "mounted guard", trade-off (breve flash animazione) approvato da Francesco. TDD completo, 450/450 test, tsc/build/lint puliti, review "Ready to merge: Yes". **Verificato in questa sessione: il bug z-index bottom-sheet vs `BottomNavPill` (segnalato da Francesco) è GIÀ RISOLTO** nel codice attuale (era stato corretto durante B8 4/5 e con l'hotfix `fix-listino-zindex` del 03/07) — nessuna azione necessaria. **Corretto: l'infrastruttura di test locale per le RPC NON è mancante** — era già stata costruita nella sessione precedente (`worktree-rpc-integration-tests`, merge `ab2e02c`); resta aperta solo l'estensione ad altre RPC `SECURITY DEFINER`. Dettaglio: `memory/MEMORY.md` §0. **Prossima priorità: B4** (`as any` nei generatori PDF MDR).

**Aggiornamento precedente:** 04 luglio 2026 — **B19 (Supabase Security Advisor hardening) RISOLTO per i 10 ERROR critici, committato ma non ancora mergiato su `main`** (branch `worktree-security-advisor-hardening`, 5 commit tecnici): non da audit tecnico precedente — segnalato da Francesco direttamente dalla dashboard Supabase Security Advisor. Chiusi tutti e 10 gli ERROR (RLS mancante su 3 tabelle, 7 Security Definer View senza `security_invoker`) più i WARN di sicurezza selezionati (8 funzioni `SECURITY DEFINER` con `REVOKE` da `anon`/`authenticated`, 33 funzioni con `search_path` fissato, 3 funzioni dead code eliminate). 5 funzioni helper RLS lasciate intenzionalmente intatte (WARN accettato, romperebbero tutte le policy). `get_advisors` ri-eseguito conferma **0 ERROR residui**. Leaked password protection resta ⛔ **non risolto** (bloccato: richiede piano Supabase Pro, progetto è su FREE). 445/445 test, tsc/build puliti. Dettaglio: `memory/MEMORY.md` §0, `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` (sezione B19). **Prossima priorità invariata: B4** (`as any` nei generatori PDF MDR) — questo era un fuori-programma di sicurezza, non sposta le priorità pianificate.

**Aggiornamento precedente:** 04 luglio 2026 — **B18 RISOLTO E DEPLOYATO** (branch `worktree-b18-hardening`, 5 commit, merge fast-forward `06a497d` su `main`, pushato su `origin/main`): chiusi tutti e 8 i finding di hardening trasversale emersi durante B3 (escape PostgREST, indice UNIQUE parziale, dedup React, CSS fallback, mock test, race condition, 2 hydration mismatch). Durante il lavoro scoperto e risolto separatamente (hotfix `23e0d15`, prima di B18) un bug critico nella RPC `salva_fasi_ciclo_atomico()` che soft-deletava ogni fase appena inserita — nessun dato reale perso. 445/445 test, tsc/build puliti, review finale "Ready to merge: Yes". Dettaglio: `memory/MEMORY.md` §0. **Prossima priorità: B4** (`as any` nei generatori PDF MDR).

**Aggiornamento precedente:** 04 luglio 2026 — **B3 RISOLTO E DEPLOYATO** (branch `worktree-b3-cicli-produzione`, 18 commit, merge fast-forward `c609a93` su `main`, pushato su `origin/main`, autorizzato esplicitamente da Francesco): cicli di produzione ora generano automaticamente le fasi alla creazione lavoro, `TabProduzione` persiste realmente esito/non conformità (2 bug di regressione corretti). Review finale whole-branch "Ready to merge: Yes" dopo un fix critico (RPC atomica per il salvataggio batch fasi). Dettaglio: `memory/MEMORY.md` §0.
**Fonte di verità:** questo file + MEMORY.md + `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` + `docs/roadmap/FEATURES-E-FLUSSI-2026-07-02.md`

> ⚠️ Questo documento è la **fonte di verità unica** per le decisioni di roadmap.
> Aggiornarlo ad ogni sessione di lavoro.

---

## VERSIONE CORRENTE: V1.9.3 (in produzione) — RE-AUDIT 02/07/2026 COMPLETATO

Il re-audit dell'11 agenti (02/07/2026) ha verificato con codice + test live che **il claim "DS v2.3 compliant al 100%" era falso** (login WCAG-fail, violazioni residue in `qualita/page.tsx`, migrazione palette parziale). Ha inoltre trovato **2 blocker critici nuovi** non coperti dalla roadmap precedente: tracciabilità MDR materiali/lotti strutturalmente rotta, e dati contrastanti tra Dashboard e Scadenzario sui crediti clienti.

**Prima di procedere con qualunque item sotto, leggere `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md`** — sostituisce come priorità operativa la sessione S4 originariamente pianificata qui sotto.

---

## 🎨 SESSIONE DESIGN — CHIUSA (28/05/2026)

### ✅ Design System v2.3 — Completato

**Cosa è stato fatto:**
- Spec completa `docs/superpowers/specs/2026-05-27-design-system-v2-3.md`
- Token CSS: t2/t3 WCAG-compliant, alias `--sfc`, rainbow vars `--c-*`
- Token TS: `src/design-system/tokens.ts` — importa da qui, mai inline
- Motion: `src/design-system/motion.ts` v2.1 — 4 categorie, frequency gate
- Enforcement: `scripts/check-ds-compliance.sh` + `.husky/pre-commit`

**Applicato su tutta la PWA:**
- ~70 file: fallback t2/t3 aggiornati
- ~33 file: gold-as-text → `var(--c-amber)`
- ~47 file: shadow inline → `var(--sh-b/c/i/red)` (dark mode ora funziona)
- 14 file: CSS transition timing → `var(--tr)`
- KpiCard, StatoBadge: rainbow semantic colors
- BottomNavPill: CTA → `.ua-tasto-plus` fisico con `::before` corona

**Escluse** (da non toccare): `src/app/(auth)/` · `src/app/admin/`

**Regola DS — OBBLIGATORIA per ogni nuova sessione:**
> Prima di scrivere qualsiasi UI → rispetta `docs/superpowers/specs/2026-05-27-design-system-v2-3.md`.
> Nessun colore/shadow/font/animazione inline. Tutto da token.

---

## ✅ B18 RISOLTO (04/07/2026) — PROSSIMA SESSIONE → B4

**B3 è RISOLTO E DEPLOYATO (04/07/2026, branch `worktree-b3-cicli-produzione`, merge fast-forward `c609a93` su `main`, pushato su `origin/main`, autorizzato esplicitamente da Francesco).** Cicli di produzione non generavano mai fasi per i lavori nuovi: `TabProduzione.tsx` prometteva un selettore ciclo mai esistito in `TabDati.tsx`, nessun trigger materializzava `lavori_fasi` da `fasi_produzione`. Fix: piano di 13 task con TDD + subagent-driven-development, 2 migration live (audit `updated_by`/trigger + RPC atomica `salva_fasi_ciclo_atomico` per salvataggio batch transazionale), 3 nuove route API, 2 componenti (`CicloComboBox`/`CicloFasiEditor`), 2 pagine nuove `/cicli-produzione`. Corretti anche 2 bug di regressione trovati in audit: persistenza esito fase mai avvenuta, `non_conforme` mai sincronizzato. Review finale whole-branch (Opus): "Ready to merge: With fixes" → 1 Important bloccante (scrittura silenziosa senza controllo errori in `PATCH /api/cicli/[id]/fasi`, risolto con RPC atomica) + fix "Invalid Date" pre-esistente in `/qualita` → ri-review "Ready to merge: Yes". 421/421 test, tsc/build puliti. QA E2E completa su lab isolato (mai il lab Filippo), dati rimossi, baseline verificata. Dettaglio completo: `memory/MEMORY.md` §0, `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` (sezione B3). **Su richiesta esplicita di Francesco, aggiunta voce B18** (hardening trasversale — 8 finding non bloccanti emersi durante B3), **risolta lo stesso giorno** (branch `worktree-b18-hardening`, merge `06a497d`) — vedi `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` (sezione B18) per il dettaglio completo, incluso il bug critico scoperto e risolto come hotfix separato durante il lavoro (`salva_fasi_ciclo_atomico()`). **Prossima priorità: B4** (`as any` nei generatori PDF MDR).

**B10 è RISOLTO (chiuso 04/07/2026, merge fast-forward `fab5437`).** `/api/fornitori` mancante bloccava la creazione ordini: `NuovoOrdineSheet.tsx` chiamava una route inesistente (errore ingoiato), select "Fornitore" sempre vuoto, bottoni invio ordine WhatsApp/Email sempre disabilitati. Fix: nuova `GET /api/fornitori`, stesso pattern già in produzione di `GET /api/listino` (scoping lab, `attivo=true`, mapping `ragione_sociale`→`nome`), nessuna migration necessaria. TDD, 376/376 test, tsc/build puliti, review "Ready to merge: Yes" (zero Critical/Important). QA manuale in browser reale confermata: select popolato, bottoni WhatsApp/Email abilitati dopo selezione fornitore. Dettaglio completo: `memory/MEMORY.md` §0, `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` (sezione B10).

**S4 (Email template branding) è COMPLETO (04/07/2026, commit `01e915c`).** I 3 template Supabase Auth (Confirm Signup, Reset Password, Invite User) sono stati ridisegnati con palette Warm Panna, logo reale (`ua-logo-email.png`), link di fallback cliccabile/copiabile nascosto sotto spoiler `<details>` — approvati da Francesco in 2 round di feedback su mockup, poi **applicati direttamente su Supabase Dashboard da Claude via `claude-in-chrome`** (Francesco ha installato l'estensione e connesso la sessione), verificati byte-per-byte e con un invio reale (reset password su `francesco.formicola@live.it`, confermato `mail.send` nei log Auth senza errori). HTML finale in `docs/email-templates-supabase.md`, decisione in `docs/design/decisions/2026-07-04-s4-email-templates-branding.md`. Dettaglio completo: `memory/MEMORY.md` §0.

**B9 è RISOLTO (chiuso 04/07/2026, merge `ea2a3a9`).** Lista pazienti (BUG #13, noto da settimane) ora navigabile: `PazientiSearchList.tsx` usava righe `<li><div>` statiche senza `Link`; la pagina di dettaglio `/pazienti/[id]` esisteva già e funzionava ma era irraggiungibile. Fix minimo (`<Link href>` + pattern flex/chevron gemello di `ClientiSearchList.tsx`), TDD, review "Ready to merge: Yes". Dettaglio completo: `memory/MEMORY.md` §0. ~~Follow-up non bloccante aperto separatamente (`spawn_task task_8422a838`): migrare `PazientiSearchList` al layout `ua-list-grid` per coerenza responsive con `ClientiSearchList`.~~ ✅ risolto 05/07/2026 (merge `85faad1`), vedi aggiornamento in testa al file.

**B8 è COMPLETO (5/5, chiuso 04/07/2026).** Tutte e 5 le route del backlog sono risolte e in produzione: magazzino/nuovo (1/5), listino/nuovo (2/5), qualita/rischi/[id] (3/5), rete/nuova (4/5), rete/[id] (5/5, merge `0c3e040`). Il link "Gestisci rete →" ora apre un dettaglio rete funzionante con rinomina, inviti lab-a-rete, rimozione membri e sezione admin. Dettaglio completo B8 (5/5): `memory/MEMORY.md` §0. **Prossima priorità: S4** (Email template branding, bozza HTML già pronta in `docs/email-templates-supabase.md`).

**A4 (Alto, non blocker) è chiuso definitivamente (03/07/2026, merge `4a36f89`).** Cache versioning automatico nel Service Worker: `public/sw.js` è diventato un file generato (gitignored) da `scripts/generate-sw.mjs`, con `CACHE_NAME` legato al build-id (git sha in produzione, `ua-dev` in sviluppo) invece del bump manuale `ua-v1→ua-v2`. Nessun TTL/pulizia cache aggiunto (decisione esplicita in brainstorming — il fix RSC di B2 aveva già eliminato la crescita illimitata). Spec: `docs/superpowers/specs/2026-07-03-a4-cache-versioning-design.md`. Piano: `docs/superpowers/plans/2026-07-03-a4-cache-versioning.md`. Dettaglio completo: `memory/MEMORY.md` §0.

**B7 (blocker critico) è risolto, mergiato su `main` (`fe81be6`) e deployato (03/07/2026).** Il titolare ora può invitare tecnico/front_desk/co-titolare direttamente dall'UI in `/tecnici`; fix incluso della RPC `accept_invite_atomic` che non creava la riga `tecnici` mancante, più un secondo fix di idempotenza (bug di duplicazione righe trovato dalla review finale verificando direttamente su Supabase live). Dettaglio completo: `memory/MEMORY.md` §0. Handoff strutturato per la prossima sessione: `memory/SESSION_ACTIVE.md`.

**B1 e B2 (i 2 blocker critici del re-audit 02/07) sono risolti, mergiati su `main` e deployati.** Da eseguire subito all'avvio della prossima sessione, PRIMA di S4 o di qualunque nuova feature:

```
Leggi docs/roadmap/BACKLOG-TECNICO-2026-07-02.md sezione BLOCKER.
Priorità: procedere con S4 (Email template branding, bozza già pronta in docs/email-templates-supabase.md).

~~B8 — 5 route CRUD che portavano a 404~~ — ✅ COMPLETO 04/07/2026 (magazzino/nuovo, listino/nuovo, qualita/rischi/[id], rete/nuova, rete/[id] tutte risolte e in produzione).
~~B9 — Lista pazienti non navigabile (BUG #13)~~ — ✅ RISOLTO 04/07/2026 (merge `ea2a3a9`).
```

**Storico B1/B2/B7 (per contesto, non richiede più azione):**
- B1 — Tracciabilità MDR materiali/lotti — ✅ RISOLTO 02/07 (commit `31cc47c`)
- B2 — Dashboard/Scadenzario dati contrastanti sui crediti clienti — ✅ RISOLTO e MERGIATO 03/07 (piano 16 task su worktree `b2-contabilita-clienti`, merge `05612ec`). Due round di bug trovati SOLO in verifica finale/review whole-branch (mai in review di singolo task) e corretti prima della chiusura: (1) Task 16 → `scadenzario/route.ts` non nettava `importo_pagato` sulle fatture con pagamento parziale (fix `cbc034b`); (2) review finale sull'intero branch → `scadenzario/route.ts` escludeva i lavori `fatturare`-non-inclusi che le altre superfici contano, e `getContabilitaCliente` non escludeva le fatture bozza (fix `ac48530`). Dettaglio completo in MEMORY.md §0.
- B7 — "Invita tecnico" irraggiungibile da UI — ✅ RISOLTO e MERGIATO 03/07 (piano 12 task su worktree `worktree-b7-invito-collaboratori`, merge `fe81be6`). Bug reale trovato SOLO dalla review finale whole-branch verificando direttamente su Supabase live (stesso pattern di B2: mai nelle review di singolo task): `accept_invite_atomic` duplicava la riga `tecnici` su re-invito+re-accettazione, corretto con una seconda migration (`WHERE NOT EXISTS`) prima del merge. QA e2e reale in browser eseguita post-merge. Dettaglio completo in MEMORY.md §0.
- **Follow-up collegati, risolti in sessione separata (commit `7fc181b`):** bug Service Worker PWA (cache RSC di `router.refresh()`, causava UI stale su tutta l'app dopo mutazioni — scoperto durante B2, backlog A4) + allineamento `BACKLOG-TECNICO-2026-07-02.md` (B2 ✅, A4 🔄 parziale, B7 ✅).

~~**Nota:** S4 Email template branding resta valida come task (bozza HTML già pronta, manca solo applicazione manuale su Supabase dashboard, 3h) ma non è la priorità — B8/B9 vengono prima.~~ ⚠️ **SUPERATA:** era vera quando è stata scritta; **S4 è stata chiusa il 04/07/2026** (commit `01e915c`), template applicati su Supabase e verificati con un invio reale — v. il paragrafo S4 qui sotto.

---

### 📌 CODA DELL'ONDATA «REDESIGN PARETE/HOME» (aggiornata 26/07/2026)

Handoff corrente: **`docs/roadmap/2026-07-27-nome-cognome-paziente-execution-handoff.md`** (27/07) —
è il più recente ed è quello da cui si riparte. Sostituisce
`2026-07-26-un-tema-solo-chiuso-handoff.md`, che resta valido come storia della giornata del tema e
per i punti 2→6 della sua §3 (linguetta · ondata B · voci di backlog del tema · trappola
edge-to-edge · centro notifiche).

⚠️ **Questa riga è già stata trovata stantia due volte** (puntava a `2026-07-26-wave-h-handoff.md`,
poi al collaudo della PWA installata, mentre nel frattempo ne erano arrivati altri). **È lo stesso
guasto che ha lasciato T16 e T17 scritti come «da fare» dopo la loro pubblicazione**, qui sotto:
chi legge un puntatore vecchio rimette in coda lavoro già fatto. **Chi scrive un handoff nuovo
aggiorna QUESTA riga nello stesso commit.**

Nell'ordine:

1. **Nome e cognome del paziente nel wizard** — ✅ **SPEC RATIFICATA IL 27/07, si esegue in sessione
   nuova.** Spec: `docs/superpowers/specs/2026-07-27-nome-cognome-paziente-design.md` · handoff di
   esecuzione: `docs/roadmap/2026-07-27-nome-cognome-paziente-execution-handoff.md`. **Nessuna
   riga di codice scritta**, per scelta: il panel 3× ha trovato tre trappole che nessun test poteva
   vedere.
   ⚠️ **Questa riga diceva «wizard e form paziente compongono `nome_cognome` in ordine opposto»:
   era SBAGLIATO.** Il form paziente manda `nome`+`cognome` separati e il trigger DB compone
   `COGNOME NOME`; il wizard invece infila **tutta la stringa dentro `cognome`** e lascia `nome`
   vuoto. Non è un ordine opposto: è che uno dei due non distingue affatto le due parti. La
   correzione cambia la conclusione — **le colonne esistono già, non serve nessuna migration.**
   **Si spezza in tre (D7 + D9):** **tappa 1** il dato (wizard + tre trappole + rettifica) ·
   **tappa 1-bis** la fotografia congelata `paziente_nome_snapshot` + correzione dalla scheda del
   lavoro — **percorso GRANDE con panel normativo**, apre la tensione fra rettifica (Art. 16 GDPR) e
   immutabilità della documentazione (MDR Art. 10(8)) · **tappa 2** la scala con l'iniziale
   puntata, **solo se dopo la tappa 1 il difetto è ancora visibile sul device**, e da ritarare (i
   corpi del mockup erano quelli del clinico, non del paziente).
2. ~~**T16 — la striscia in home**~~ **✅ FATTO e in produzione** (26/07/2026). Logica e forma
   nuove, valori ratificati; la forma del 24/07 è stata superata dalla decisione del 26/07
   («nomina il primo allarme, conta gli altri»).
3. ~~**T17 — chiusura ondata**~~ **✅ FATTO e in produzione** (26/07/2026): strumentazione
   diagnostica dei suoni rimossa · `scripts/tmp/` pulito e gitignorato · FASE 7 · review
   whole-branch in 5 aree con il triage dei 69 rilievi → 0 bloccanti · allineamento spec DS v3 ·
   FASE 9 Playwright su 3 viewport × 2 temi · **FASE 9b gate estetico L2 PASS, 0 ❌** · merge
   `5504a20a` autorizzato da Francesco · BP-1.

⚠️ **T16 e T17 restavano scritti qui come lavoro da fare** anche dopo essere stati chiusi,
mergiati e pubblicati: chi leggeva questa lista li rimetteva in coda. L'unico punto ancora aperto
di questa coda è il **numero 1** (nome e cognome del paziente nel wizard), più i quattro punti del
collaudo della PWA installata.

### 🔑 NUOVA DIRETTIVA DI PRODOTTO (Francesco, 27/07/2026) — «ogni campo del lavoro si corregge, fino alla consegna»

> «una volta creato, io devo avere la possibilità di poter modificare sempre ogni campo del lavoro
> […] se l'addetta al front desk fa un errore di digitazione o altro, bisogna sempre poter
> intervenire, fino a poi la consegna con l'eventuale fatturazione.»

**Incisa in `ua-app/CLAUDE.md` §9** come direttiva permanente: vale per ogni superficie futura, non
solo per il nome del paziente. Il testo lì spiega anche **come si applica quando si progetta** e
quali sono le ragioni valide per tenere un campo fuori dalla modifica.

**Stato misurato il 27/07:** rispettata **a metà**. La finestra «modificabile finché non è
fatturato» esiste già, ma **solo per i campi del prezzo** (`LOCKED_PRICE_FIELDS`). L'allowlist
`PATCHABLE_FIELDS` (`src/app/api/lavori/[id]/route.ts`) esclude **16 campi** con la motivazione
«nessun writer nel form React attuale» — cioè non per una ragione, ma perché il modulo non li
mostra: `arcata`, `colorazione_esterna`, `impronta_digitale`, `numero_prescrizione`,
`norma_riferimento`, `richiedente_email`, `stato_fisico`, `tipo_arco`, `codice_interno`,
`anamnesi_note`, `classe_rischio`, `paziente_nascita_snapshot`, `paziente_nome_snapshot`,
`prescrizione_digitale_id`, `spedizione_note`, `spedizione_stato`.

🛑 **Da NON confondere con le esclusioni legittime**, che restano tali e non si toccano:
`numero_cassetta` (RPC atomiche, o si desincronizza da `cassette_lavori`) · `proposta_dentista`
(sentinella D7, altra autorità) · i campi calcolati server-side · quelli congelati dopo l'emissione
di un documento.

**Voce di lavoro che ne discende — «Il lavoro si corregge»:** censimento dei 16 campi (chi li
scrive oggi, chi dovrebbe poterli correggere, quale finestra), generalizzazione del modello
`LOCKED_PRICE_FIELDS` a tutto il lavoro, e le schermate da cui correggere. ⚠️ **Interseca la tappa
1-bis** del nome paziente (fotografia congelata): stessa materia, da progettare insieme e **mai due
volte**. Collocazione da ratificare — la sua superficie naturale è `/lavori/[id]/modifica`, che ha
già una voce «smontaggio bridge» nella riga **Trasversale** del calendario ondate.

**Da studiare insieme, richiesta esplicita di Francesco (27/07):** «dovremmo studiare bene come
abbiamo sviluppato la creazione di un nuovo lavoro» — revisione del percorso di ingresso del lavoro
in laboratorio, dalla creazione alla consegna. Non è questa ondata: è il contesto in cui questa
direttiva vive.

---

**Deferito alle ondate proprietarie** (ratifica 26/07): bottone oro e i difetti di leggibilità
già appuntati sotto — «può essere che scomparirà del tutto quando revisioneremo le pagine».
**Non si fa** (ratifica 26/07): campo «nome breve» in anagrafica cliente.

---

## 🗺 CALENDARIO ONDATE MIGRAZIONE v3 — RATIFICATO DA FRANCESCO (20/07/2026)

Direttiva «tutto deve migrare a v3». Censimento: **37 route ancora v2.3** (fonte `src/lib/nav/route-migrate-v3.ts`; griglia completa nel report censimento della sessione 20/07 sera — MEMORY (17)). ⚠️ Qui c'era scritto **30**, ed era sbagliato: la verifica di copertura del 26/07 più in basso in questa stessa sezione lo aveva già corretto a **37**, ma l'apertura non era stata allineata e il file si contraddiceva a poche righe di distanza. Le ondate B→G ne elencano 37: la copertura era già giusta, era sbagliato solo il numero in testa. Validato da panel advisor (solution-architect) con 4 correzioni integrate. Ordine per priorità d'uso + affinità di dominio, **max ~4 route per ondata, mai auth mischiata ad altro**:

| Ondata | Route | Note |
|---|---|---|
| **A** (✅ COMPLETATA — mergiata e deployata 20/07) | `/tecnici` → «Persone» | Qui NASCE il chrome v3 di pagina-lista (titolo + back + slot azioni): nominato in A, promosso a `components/ds` in B |
| **B** «giro clienti» | `/agenda` + `/clienti` + `/clienti/[id]` | Generalizza il chrome v3 (sostituto AppHeader/PageWrapper). Se `/clienti/[id]` sfora → B1/B2 senza rimescolare |
| **C** «fiscale» | `/fatture` + `/fatture/[id]` + `/fatture/riconciliazioni` | **Percorso Grande** (dominio critico FatturaPA) |
| **D** «pazienti e crediti» | `/pazienti` ×2 + `/scadenzario` ×2 | |
| **E** «banco e magazzino» | `/magazzino` ×2 + `/ordini` + `/listino` | Cicli spostati in G (dominio produzione, advisor) |
| **F1** «casa» | `/impostazioni` ×4 | Qui nasce la riga «I tuoi dati» (export CSV lavori, §7.16) |
| **F2** «accessi» | auth (login/forgot/reset/invite) + `/onboarding` + billing/blocked | **Percorso Grande** (OVERRIDE auth) + QA dedicato redirect loop |
| **F3** «rete» | `/rete` ×3 | |
| **G** «qualità e produzione» | `/qualita` ×5 + `/cicli-produzione` ×2 + `/tecnici/[id]/produttivita` | + analytics quando arriva la sessione A15 |
| **Trasversale** | smontaggio bridge `/lavori/[id]/modifica` | Un tab per ondata affine (es. TabClinica con l'ondata odontogramma) |
| **Milestone finale** | ritiro chrome legacy globale | `BottomNavPill` + `UserProfileSheet` dal layout `(app)` — possibile solo a ultima route migrata; prerequisito O1i (fatto nel mini-triage) |
| **DOPO la milestone finale** | **Centro notifiche + sistema di notifiche vero della PWA** | Decisione di Francesco del 26/07/2026 — v. sottosezione qui sotto. **È l'ultimo lavoro della lista.** |

Fuori scope ondate: admin (DS neomorphic separato, sessione O2 dedicata) · portale dentista (standalone). La navbar a scomparsa (BottomNavPill) muore per sottrazione: già `null` su ogni route v3, sparisce del tutto alla milestone finale.

### 🔔 CENTRO NOTIFICHE — collocato in fondo, per decisione di Francesco (26/07/2026)

> «per il centro notifiche, ok, ma **implementiamolo alla fine della roadmap che abbiamo**».

**Dove sta:** dopo la «Milestone finale — ritiro chrome legacy globale» del calendario qui sopra.
Cioè **ultimo**, come dicono le sue parole. Non si comincia adesso e non si infila davanti a niente.
Quando arriverà il suo turno serve il percorso **GRANDE** (`ua-app/CLAUDE.md` §0C) con panel di
advisor prima di ratificare l'architettura: è un dominio nuovo (tabella, RLS con
`public.current_lab_id()`, stato letto/non letto, consegna, ruoli, GDPR), non uno spostamento di UI.
Il materiale per il brainstorming è già raccolto in
`docs/roadmap/2026-07-26-collaudo-pwa-installata-handoff.md` §3.

⚠️ **La tensione fra i due ordinamenti, scritta qui perché nessuno la risolva di nascosto.** Questa
roadmap contiene **due ordini che non sono mai stati riconciliati**: il **calendario di migrazione**
qui sopra (che finisce con la milestone finale) e la **sequenza operativa ratificata il 17/07**
(risolvere i problemi → funzioni attive → design coerente → audit multi-agente → collaudo di
Francesco), che sta in testa a questo file. Secondo il calendario il centro notifiche va **ultimo**;
secondo la sequenza del 17/07 una **funzione nuova** cadrebbe al **passo 2**, cioè prima di tutto il
lavoro di design — l'opposto. **Vale la decisione di Francesco: ultimo.** Se un domani i due ordini
vanno davvero riconciliati, **è una decisione sua, non di una sessione**: nessuno lo rimetta al
passo 2 leggendo la sequenza del 17/07 e ragionando da solo.

⚠️ **Non duplicare: due voci esistenti parlano già in parte di questa roba.** Vanno agganciate qui,
non riscritte da capo — altrimenti la roadmap porta la stessa cosa due volte.
- **V2.0 n.1 «Portale dentista V2 — comunicazione bidirezionale»**: è il pezzo **lab↔clinico** della
  messaggistica. Stessa materia, superficie diversa (il portale ha un'altra autenticazione).
- **V2.0 n.8 «Log WhatsApp (`agenda_messaggi_clienti`)»**: è il registro dei messaggi mandati, cioè
  metà dello schema che servirebbe comunque a un centro notifiche.

Il pezzo davvero **nuovo** e senza casa è la messaggistica **fra gli operatori dello stesso
laboratorio**: quella non esiste da nessuna parte, né qui né in V2.0.

### ✅ VERIFICA DI COPERTURA (26/07/2026, su richiesta di Francesco)

Incrociato l'elenco REALE delle route (`src/app/**/page.tsx`, 55 pagine) col calendario qui
sopra. **Nessuna pagina resta scoperta: le ondate B→G coprono tutto.** I conteggi per famiglia
del calendario tornano uno a uno con il codice (`/qualita` ×5, `/rete` ×3, `/pazienti` ×2,
`/scadenzario` ×2, `/magazzino` ×2, `/impostazioni` ×4, `/cicli-produzione` ×2, `/fatture` ×3).

Due correzioni al testo sopra:

1. **Il censimento «30 route ancora v2.3» è SOTTOSTIMATO: sono 37.** Il conteggio corretto:
   55 pagine totali − 9 già v3 (`/dashboard`, `/tutto-il-resto`, `/lavori`, `/lavori/nuovo`,
   `/lavori/[id]`(+`/modifica`), `/tecnici`, `/cassette`, `/ds-v3-catalogo`) − 2 redirect non
   migrabili (`/` e `/lavori/[id]/consegna`, quest'ultimo già dichiarato fuori dal predicato) −
   7 fuori scope (admin ×5, portale, richiedi) = **37**. Le ondate ne elencano 37: la copertura
   era già giusta, era sbagliato solo il numero in testa.
2. **`/analytics` è l'unica route con collocazione CONDIZIONALE** («+ analytics quando arriva la
   sessione A15»). È anche la pagina con più difetti di leggibilità aperti (v. sotto): se A15
   slitta, `/analytics` slitta con lei. Da sciogliere.

### 📌 DIFETTI DI LEGGIBILITÀ APPUNTATI ALLE ONDATE (26/07/2026 — ratifica Francesco: NON si interviene ora)

Misurati il 26/07 durante la verifica del fondo unico (contrasti campionati dai pixel resi).
**Decisione di Francesco:** quelle pagine devono comunque essere migrate, riviste e rifatte —
correggerle adesso in v2.3 sarebbe lavoro buttato. Si appuntano qui e si risolvono **dentro
l'ondata che possiede la pagina**, con gli inchiostri già progettati.

Materiale pronto da riusare: mockup `docs/design/mockups/2026-07-26-leggibilita-colori-v23.html`
(prima/dopo misurati, chiaro e scuro) · token proposti e tarati: `--gold-ink`, `--c-blue-ink`,
`--c-green-ink`, `--gold-fg`.

| Ondata | Pagina | Difetto misurato | Soglia |
|---|---|---|---|
| **G** (cond. A15) | `/analytics` | numeroni KPI: oro **1,67:1** ×2 (esadecimale scritto a mano, non il token), verde `--success` **2,49:1**, arancio `--urgente` **2,12:1** | 3:1 (testo grande) |
| **G** | `/qualita/psur` | alert blu **2,4:1** e arancio **2,1:1** su tinta chiara semi-trasparente | 4,5:1 |
| **F1** | `/impostazioni` | badge ambra `--c-amber` come testo a 12px, **1,62:1** — c'è già `--c-amber-ink` per questo | 4,5:1 |
| **B** | `/clienti` | stesso uso di `--c-amber` come testo (`ClientiSearchList.tsx:248`) | 4,5:1 |
| **G** | `/tecnici/[id]/produttivita` | stesso uso di `--c-amber` come testo | 4,5:1 |
| **F1 + F2 + B + trasversale** | bottone oro, **13 punti** | testo bianco su oro **2,21:1**; e 10 di quei punti usano `var(--t1)` sopra l'oro → 7,90:1 in chiaro ma **1,90:1 in SCURO** (il token si schiarisce col tema, l'oro no) | 4,5:1 |

⚠️ **Il bottone oro è trasversale a più ondate** e porta una scelta di gusto ancora aperta
(inchiostro scuro sull'oro, che tiene l'oro di sempre a 7,90:1 · oppure bianco su oro più scuro
`#8A6716` a 5,21:1). Chi arriva per primo su una di quelle route deve chiedere a Francesco e poi
applicare la STESSA scelta ovunque, in una volta: `.ua-btn-gold` in `globals.css` più le copie
inline. Il caso «etichetta *consigliato* a 9px su scheda al 55% di opacità» resta sotto soglia
con entrambe le vie — è un problema di opacità, non di colore, e va risolto lì.

Nota trasversale: `/analytics` passa il colore come **esadecimale scritto a mano** invece del
token. Anche dopo la correzione, un valore scritto a mano scavalca il sistema e tornerà a
divergere — nella sua ondata va ricondotto ai token.

#### ➕ Aggiunto il 26/07/2026 (notte), misurato durante il QA della tappa 3 del tema

| Ondata | Pagina | Difetto misurato | Soglia |
|---|---|---|---|
| **F1** (e ogni pagina v2.3) | `/impostazioni`, **tema scuro** | i **token secondari** sono sotto soglia sulla scheda (`#232018`): `--t3` `#5A5652` → **2,24:1**, `--t2` `#8A8580` → **4,45:1**. Non è un uso sbagliato di un token: sono **i token stessi** | 4,5:1 |

🛑 **Non è un difetto della riga «Tema» aggiunta quella notte: è di sistema.** La nota preesistente
«Le pile restano raggiungibili da ☰ → I lavori» ha **esattamente lo stesso 2,24:1**, e così ogni
testo secondario di ogni pagina ancora v2.3 quando l'utente sceglie il tema scuro. Prima della
tappa 3 si vedeva di rado — il tema scuro era difficile da tenere; ora che si blocca da
Impostazioni con un tap, si vedrà molto di più.

**Tampone già applicato, solo lì:** la frase di stato della riga «Tema» usa `--t2` invece di `--t3`
(da 2,24 a 4,45), perché è l'unica cosa che dice all'utente che cosa sta seguendo — illeggibile
proprio nel tema in cui serve non era un dettaglio estetico. **4,45 resta sotto soglia:** è il meglio
che la tavolozza v2.3 offra senza gridare quanto un'etichetta. La correzione vera è nei token, e
arriva con l'ondata che possiede la pagina.

---

## V1.9 — Completamento Pre-Collaudo
**Priorità:** Massima — da fare PRIMA che Filippo usi l'app seriamente.

| # | Feature | Priorità | Stima | Stato |
|---|---------|----------|-------|-------|
| DS | **Design System v2.3** | P0 | — | ⚠️ **Parziale, non 100%** (re-audit 02/07: login WCAG-fail, 2 violazioni residue in qualita/page.tsx, migrazione palette parziale — vedi backlog B12, A6, M6) |
| 1 | **Dettatura vocale** (Web Speech API) | P0 | 4h | ✅ **FATTA** — `src/components/ds/PillVoce.tsx` implementa la Web Speech API (`window.SpeechRecognition` / `webkitSpeechRecognition`) ed è montata nei **tre passi del wizard** (`PassoTipo`, `PassoDentista`, `PassoPaziente`) **e nel catalogo** `/ds-v3-catalogo`. ⚠️ Qui c'era scritto «⏳ Confermato non iniziato (grep SpeechRecognition → 0 risultati)»: quel grep era **falso** al momento della lettura — il file esiste e la stringa c'è. Il grep, evidentemente, era stato lanciato prima che il componente nascesse (o fuori da `src/`) e la riga non è più stata guardata. Resta a backlog la variante avanzata, che è un'altra cosa: **V2.0 n.4 «Dettatura vocale avanzata (Whisper)»** |
| 2 | **Email template branding** (Supabase) | P0 | 3h | ✅ **FATTO** il 04/07/2026 (commit `01e915c`) — i 3 template Auth ridisegnati, **applicati sul Dashboard Supabase e verificati con un invio reale** (reset password su `francesco.formicola@live.it`, `mail.send` confermato nei log Auth). HTML in `docs/email-templates-supabase.md`, decisione in `docs/design/decisions/2026-07-04-s4-email-templates-branding.md`. ⚠️ Questa riga diceva «manca solo applicazione manuale» mentre **la stessa roadmap**, nella sezione S4 più in alto, dava il lavoro per completato e applicato: contraddizione interna, sciolta a favore di quello che mostrano il commit e il verbale. Stessa correzione alla «Nota» di §S4 più su |
| 3 | **Rifacimenti UI** | P0 | 6h | ✅ Completato S2 (26/05/2026) |
| 4 | **Logo + firma DdC** | P0 | 4h | ✅ Completato — rendering in `DdcTemplate.tsx` + hash SHA-256 integrità firma (A18, Bundle T 20/07, cut-off senza backfill) |
| NEW | **2 Blocker critici da re-audit 02/07** (B1 materiali/lotti MDR, B2 dashboard/scadenzario) | 🔴 P0 | non stimato | ✅ Entrambi risolti — B1 (31cc47c, 02/07) · B2 (cbc034b, 03/07, dopo fix del bug residuo trovato in verifica finale), vedi sopra |
| A8 | **Fallback email notifiche (Resend)** — se l'utente non ha push attive, la notifica arriva via email | P1 | 4-6h | ⏳ **Ratificata da Francesco 20/07** («Sì, pianificala») — da eseguire post-Bundle E; provider Resend |
~~5 Magazzino visivo → spostato in V2.0~~ |

**Vedi `docs/roadmap/BACKLOG-TECNICO-2026-07-02.md` per altri 14 item Blocker, 18 Alto e 30 Medio non elencati qui per brevità.**

---

## V2.0 — Post-Collaudo sul campo
**Priorità:** Alta — sviluppare dopo le prime 2-3 settimane di uso reale
**Trigger:** i primi laboratori pilota usano l'app quotidianamente e danno feedback reali *(rev. 09/07: era "Collaudo Filippo" — i collaudi interni sono di Francesco; i laboratori pilota, di cui Filippo potrà essere uno dei tanti, arrivano a PWA stabile)*

| # | Feature | Priorità | Note |
|---|---------|----------|------|
| 1 | **Portale dentista V2** | P0 | Comunicazione bidirezionale, ordine lavoro dal portale, download DdC. 🔗 **La «comunicazione bidirezionale» è lo stesso soggetto** del centro notifiche collocato in fondo al calendario ondate v3 (§ «Centro notifiche», sopra): è il suo pezzo lab↔clinico. Da progettare insieme, mai due volte |
| 2 | **Analytics avanzate** | P0 | Top clienti, margine per tipo dispositivo, lead time, tasso rifacimento |
| 3 | **Multi-tier pricing avanzato** | P1 | listino_prezzi_tier con assegnazione per cliente. Ora 4 tier, serve 6+ |
| 4 | **Dettatura vocale avanzata (Whisper)** | P1 | Whisper API OpenAI per accuracy su termini tecnici italiani (~€0.006/min) |
| 5 | **Sezione Rete funzionale** | P1 | Dashboard multi-lab per admin_rete. Architettura: reti + reti_members già in DB |
| 6 | **WhatsApp Cloud API ufficiale** | P2 | Notifiche automatiche (Meta Business + 360dialog/Twilio). Ora solo deep links |
| 7 | **Allegati clinici avanzati** | P2 | Note vocali (audio), file STL scansioni, PDF prescrizione digitale |
| 8 | **Log WhatsApp (agenda_messaggi_clienti)** | P2 | Tracciamento ogni messaggio mandato: data, testo, esito. 🔗 **Metà dello schema che servirebbe comunque** al centro notifiche collocato in fondo al calendario ondate v3 (§ «Centro notifiche», sopra): stesso registro di messaggi. Da progettare insieme, mai due volte |
| 9 | **Magazzino visivo** (Mixel-inspired) | P2 | Concept C: tile 3 col, 12 glifo SVG, fill-bar semaforo. Toggle lista↔vetrina. Vedi `MAGAZZINO-VISIVO-BRAINSTORM.md` |

---

## V2.1 — AI Assistant
**Priorità:** Alta business value — pricing già pianificato
**Modello:** Stripe metered billing €24,90/mese (1.000 msg) + scaglioni

| # | Feature | Note |
|---|---------|------|
| 1 | **Voce continua** (stile ChatGPT) | Web Speech API V1 → Whisper V2 |
| 2 | **Chat AI** per inserimento dati | "Crea lavoro per Dr. Rossi, corona ceramica urgente" |
| 3 | **TTS risposta** | Web Speech Synthesis gratuita |
| 4 | **Smart routing modelli** | 40% Haiku 4.5 + 60% Sonnet = blended ~€0.008/msg |
| 5 | **Billing metered Stripe** | €24,90/mese base + €11 ogni 1.000 msg extra |
| 6 | **Caching prompt** | 8.000 token system cached → riduce costo input a $0.0024/msg |

---

## V2.2 — Compliance Avanzata MDR
**Trigger:** Se Filippo cerca certificazione ISO 13485 o ha clienti PA

| # | Feature | Note |
|---|---------|------|
| 1 | **CAPA** (Corrective and Preventive Action) | Link a incidenti/non conformità. ISO 13485 |
| 2 | **Fascicolo Tecnico MDR** | 6 tab: specifiche, FMEA, valutazione clinica, test, DdC, PSUR |
| 3 | **PMCF follow-up automatico** | Reminder 6/12 mesi post-consegna. MDR Art. 83 obbligatorio |
| 4 | **Firma Digitale P7M** | Per FatturaPA alla PA (Regioni, ASL). Richiede AgID |
| 5 | **Nota di credito XML (TD04)** ✅ implementata 15/07/2026 (`d0d83c8`) | Per correggere fatture già emesse — storno integrale; manca solo l'invio a SdI (BACKLOG §N) |

---

## V3.0 — Platform Scale
**Trigger:** Secondo cliente + espansione

| # | Feature | Note |
|---|---------|------|
| 1 | **Migrazione DentalMaster completa** | 30 anni storico. FileMaker fp7 → UÀ. Import batch |
| 2 | **Prescrizione digitale dentista** | Form digitale dal portale con firma digitale |
| 3 | **SDI diretto** | Senza PEC. Richiede accordi HUB SDI accreditato |
| 4 | **Terzismo inter-lab** | Lavori in subappalto tra laboratori della rete |
| 5 | **Onboarding self-service** | Nuovo lab si iscrive, paga Stripe, riceve seed automatico |
| 6 | **API pubblica** | Per integratori CAD/CAM, software clinici |
| 7 | **CAD/CAM integration** | Import STL nativi, link scansione digitale in Tab Clinica |
| 8 | **White label** | Lab con marchio proprio |

---

## 🔐 IN CODA A TUTTO — Ondata «accesso con passkey»: i 5 difetti rimasti

**Collocazione decisa da Francesco il 28/07/2026:** *«segniamoli alla fine di tutta la nostra roadmap,
tanto non andiamo in produzione finché non lo dico io»*. Nessuno di questi sta facendo danno oggi; il
sesto della lista (email confrontata con le maiuscole) **era** attivo ed è già stato corretto e
deployato (`24474b5c`).

**Origine:** censimento del 28/07/2026 con panel 2× (sicurezza applicativa + compatibilità), nato
dalla decisione CSRF sulle due route di accesso WebAuthn. Verbale nel commit `5773c6b6` e in
`memory/MEMORY.md` voce 56. 🛑 **La questione CSRF è CHIUSA e non si riapre**: esclusione legittima con
la ragione scritta in `scripts/check-csrf.sh` (quelle route non leggono né scrivono cookie, e l'origine
è già legata dentro la firma dell'autenticatore). L'esclusione decade solo se una di esse inizia a
leggere una sessione o a fare `Set-Cookie`.

| # | Difetto | Quando morde | Nota |
|---|---------|--------------|------|
| ② | **Consumo della challenge non atomico** — `challenge.ts:18-32` legge `used_at` e poi lo scrive in una seconda istruzione, con **l'errore della UPDATE non controllato**: due richieste simultanee passano entrambe → replay | **Ora**, ma serve concorrenza mirata | 🔴 Il più grave: il contatore anti-replay è **inerte** (misurato: 4 credenziali su 4 con `counter = 0`, passkey sincronizzate), quindi l'uso unico della challenge è **l'unica** difesa. Fix indicato: `UPDATE … WHERE id=$1 AND used_at IS NULL AND expires_at > now() RETURNING challenge` |
| ③ | **`listUsers()` senza paginazione** — `login/options:19`, `login/verify:20`: l'API admin è paginata a **50 per pagina** | **Al 51° utente** | Ordinamento `created_at DESC` → cadono fuori **i più vecchi, cioè i titolari**. Sintomo: 404 «Nessuna credenziale registrata» indistinguibile da «non hai passkey». Fix consigliato: RPC `SECURITY DEFINER` su `auth.users` con `lower(email)`, blindata col pattern §9. ⚠️ **Scartato** leggere `public.utenti`: nessun trigger allinea `utenti.email` a un cambio email lato auth |
| ④ | **Nessun filtro `deleted_at`/`attivo` all'accesso** — la registrazione lo fa (`register/options:13-15`), l'accesso no | Quando si revoca un utente che ha la passkey | Ottiene comunque un gettone di sessione. Contenimento parziale via RLS, ma la definizione di `current_lab_id()` nel repo è **commentata**: la garanzia vive solo nel database vivo |
| ⑤ | **Nessun rate limiting** su due endpoint non autenticati, uno dei quali chiama `generateLink` | Sotto abuso | Unico precedente in casa: `portale/richiedi/route.ts:94-111` |
| ⑥ | **Enumerazione degli account** — 200 con `allowCredentials` se l'email ha una passkey, 404 altrimenti | Sempre, dall'esterno | W3C WebAuthn §14.6.2. ⚠️ `isSameOrigin` **non** la ferma (con `Origin` assente ritorna `true`, e un attaccante non usa un browser). Strada giusta: login **senza username** — le credenziali sono già `residentKey: 'required'` |

⚠️ **Da mettere in conto prima di iniziare:** `RP_ID = 'uachelab.com'` è fisso (`webauthn/config.ts:3`),
quindi la cerimonia **non è eseguibile in locale né su anteprima**. Come si verificano le correzioni va
deciso **prima** di scriverle, non dopo.

**Percorso:** dominio `auth` → **GRANDE** automatico (`ua-app/CLAUDE.md` §0C, gate FASE 3).

---

## 🔴 TROVATO PROVANDO L'ONDATA (a) — un laboratorio può diventare INCANCELLABILE (28/07/2026)

Non è un difetto dell'ondata (a): è **pre-esistente**, ed è emerso perché il Task 13 provava la
cancellabilità sul database vero. **La prova prevista dal piano è FALLITA** — 11 righe invece di zero.

**Sei tabelle hanno una chiave esterna verso `laboratori` senza `ON DELETE`, e `admin_delete_laboratorio`
non le cancella** (verificato: `confdeltype='a'` su tutte e sei, **zero menzioni** nel corpo della
funzione): `credito_clienti_movimenti` · `fatture_sdi_eventi` · `listino_materiali_auto` ·
`ordini_fornitori` · `pagamenti` · `scarichi_magazzino`.
**Latenti solo perché a zero righe.** Repro eseguito: stesso laboratorio, cancellazione → `true`;
si inserisce **una** riga in `credito_clienti_movimenti` → `SQLSTATE 23503 … violates foreign key
constraint`. 🛑 **Alla prima fattura pagata o al primo scarico di magazzino, quel laboratorio smette
di essere cancellabile** — e la cancellabilità del laboratorio è un obbligo GDPR, non una comodità.
⚠️ In più `progressivi_anno` **lascia orfani** (nessuna FK, quindi non blocca, ma le righe restano).

🔑 **E lo strumento che avrebbe dovuto scoprirlo mente nella direzione rassicurante:** l'asserzione
«strutturale» usa `regexp_matches(prosrc, 'DELETE FROM (\w+)')` su un corpo di funzione — non legge
`DELETE FROM public.x`, conta anche i **commenti**, e sulla funzione accanto **ha estratto una tabella
chiamata `public`**. Chi ripara questo difetto **non riusi quel controllo**: si interroghi
`pg_constraint`, che è il catalogo vero.

**Percorso:** schema + RLS → **GRANDE** automatico. Evidenze: `docs/superpowers/plans/evidenze/2026-07-27-ondata-a-isolamento.md`.

---

## 🔴 UN CANCELLO AUTOMATICO CHE NON ESISTE — due progetti Playwright fantasma (28/07/2026)

Stessa classe delle quattro guardie non agganciate a nulla (voce 55): **una protezione dichiarata che
non gira mai**, e che per questo dà una falsa sicurezza.

`playwright.config.ts` dichiara due progetti — `cross-tenant` → `rls-cross-tenant.spec.ts` (righe 32)
e `api-coverage` → `api-coverage.spec.ts` (righe 35-36) — e **nessuno dei due file esiste in
`tests/e2e/`** (verificato). Il progetto crede di avere un cancello automatico **proprio sul rischio
di isolamento fra laboratori**, quello che il Task 13 ha dovuto provare **a mano**.
⚠️ E l'accesso E2E **non ha mai avuto una sessione**: `tests/e2e/auth.setup.ts` legge
`TEST_USER_EMAIL`/`TEST_USER_PASSWORD`, mentre `.env.local` definisce `TEST_EMAIL`/`TEST_PASSWORD` —
l'helper prende il ramo «avviso e salvo auth vuota», quindi il progetto `authenticated` gira **senza
sessione**. Due difetti che si nascondono a vicenda: il cancello non c'è, e anche se ci fosse non
avrebbe di che accedere.

**Da decidere quando si affronta:** scrivere davvero le due prove, oppure **togliere le due
dichiarazioni** — ma non lasciarle lì. Una riga che promette un controllo inesistente è peggio di
nessuna riga.

🔴 **E c'è un terzo caso, trovato lo stesso giorno provando a usarlo (28/07/2026): la skill di
revisione del progetto non si raggiunge.** ⚠️ **La prima diagnosi diceva «gstack non esiste, è un
guscio»: era SBAGLIATA**, e resta qui scritta perché è istruttiva — una diagnosi affrettata avrebbe
fatto **cancellare un'installazione completa e funzionante**.
**Fatto verificato:** **gstack c'è per intero**, in `ua-app/.agents/skills/gstack/` (checklist,
`greptile-triage.md`, `design-checklist.md`, `specialists/`, `bin/`), e il symlink tracciato
`.claude/skills/gstack` lo raggiunge. **Il difetto è di percorsi:** il `SKILL.md` della review cerca
la checklist in `.claude/skills/review/checklist.md` (lì c'è **solo** `SKILL.md`) e richiama **64
volte** `~/.claude/skills/gstack/…`, cioè la **home**, dove gstack **non** è installato. Le due copie
del `SKILL.md` sono **identiche** e sbagliano allo stesso modo: **installato in modalità progetto,
scritto per la modalità home**.
🔑 **Tre volte in un giorno lo stesso SINTOMO, ma non la stessa causa:** le quattro guardie non
agganciate (voce 55) e i due progetti Playwright fantasma erano **capacità che non esistevano**;
questa **esiste per intero e non si raggiunge**. Il tratto davvero comune è l'altro: **nessuna delle
tre falliva rumorosamente.** Un controllo che non gira non protesta — e da fuori è identico a uno che
gira e passa.
✅ **DECISO E FATTO il 28/07/2026 — Francesco: «non lo uso più, togli tutto».** Rimosso:
il corpo `ua-app/.agents/` (**1,1 GB**, conteneva solo gstack) · le **53** cartelle-scorciatoia sotto
`.claude/skills/` (censite una per una: **53 su 53** puntavano dentro gstack, **zero eccezioni**) ·
il symlink **tracciato** `.claude/skills/gstack` (rimosso da git) · le **53 voci** gstack in
`.claude/settings.local.json` · la voce in `skills-lock.json` · le due righe in `.gitignore` · la
cartella di scarto `.gstack/`. **Tutto nel cestino, non cancellato: è ripristinabile.**
🛑 **NON toccate le 11 skill di design di Francesco** (`animejs`, `brandkit`, `ckm-design-system`,
`css-animations`, `design-taste-frontend`, `gsap`, `high-end-visual-design`, `lottie`, `tailwind`,
`ui-ux-pro-max`, `waapi`): verificate una per una **dopo** la rimozione, tutte e 11 ancora vive.
**Istruzioni corrette perché non ingannino:** `CLAUDE.md` FASE 9 (diceva `/gstack qa`) ·
`PROSSIMA-SESSIONE.md` · e un **avviso in testa a `WORKFLOW-STANDARD.md`**, che NON è stato riscritto:
oltre quaranta `/gstack:*` lo attraversano, e sostituirli è **ridefinire il processo** (gstack
occupava il livello «decisione» e i due gate) — decisione di Francesco con panel, non una pulizia.
⚠️ **Restano i riferimenti nei documenti STORICI** (`MEMORY.md`, handoff, piani vecchi): sono cronaca
di ciò che è stato fatto allora, e non si riscrivono.

---

## 🕛 IN CODA — due residui dell'ondata (a) che NON appartengono a nessun task (28/07/2026)

Stanno qui perché **non hanno una casa dentro l'ondata (a)**, e un difetto senza proprietario è un
difetto che nessuno raccoglie. Origine: la coda del Task 9 (commit `63361649`), che ha corretto lo
stesso errore nella route del POST. Dettaglio in `docs/roadmap/2026-07-28-ondata-a-esecuzione-handoff.md` §5-bis.

| # | Cosa | Quando morde | Perché è qui e non in un task |
|---|------|--------------|-------------------------------|
| ⑧ | **`genera_numero_lavoro()` ricava l'anno da `EXTRACT(YEAR FROM now())`** (`supabase/schema.sql:1996-2000`), cioè l'anno della sessione Postgres — **UTC su Supabase**, non il giorno civile di Roma | Fra le 00:00 e le 01:00 di Roma del **1° gennaio**: numero pescato dalla serie dell'anno vecchio | **Latente: zero chiamanti in `src/`** (la referenziano solo la propria definizione, la migration di hardening `search_path` e i tipi generati). Chiuderla vuole **una migration**, e l'ondata (a) non ne apre altre. Se un domani qualcuno la chiama, il difetto si sveglia |
| ⑨ | **La suite di test non fissa il fuso orario da nessuna parte** (verificato in `vitest.config.ts`, `tests/setup.ts`, `package.json`, `.github/workflows/`) | Sempre, in silenzio: su una macchina italiana un test di confine-data **non può fallire** | Non è un difetto del prodotto, è una **rete che sembra una rete e non lo è**: `tests/unit/fatture-data-roma.test.ts:42-48` discrimina solo in CI. Fissare `TZ:'UTC'` in `vitest.config.ts` cambia la semantica di **tutta** la suite → **decisione con panel** (Regola Advisor), non un residuo da sbrigare in un task |

---

## DECISIONI ARCHITETTURALI PERMANENTI

| Decisione | Rationale |
|-----------|-----------|
| WhatsApp: solo `wa.me` deep links | ToS Meta-compliant. Cloud API solo se serve V2 |
| Tecnici: disattiva (non cancella) | Storico lavori collegato. `lab_memberships.attivo=false` |
| Fatture: generate durante `orchestraConsegna` | `incluso_in_fattura` = discriminatore. No fatturazione separata |
| Pazienti: pseudonimizzazione GDPR | Solo `codice_paziente`, mai nome in WhatsApp/portale |
| Push notifications: lazy-init VAPID | No crash build CI se chiavi mancanti |
| Stack voice V1: Web Speech API nativa | Zero costi. Whisper solo per V2 se serve accuracy |

---

## VANTAGGI COMPETITIVI UNICI (non toccare)

| Vantaggio | vs Competitor |
|-----------|--------------|
| 1-tap CONSEGNA (DdC+Fattura+Stock) | DentalMaster = 6+ click |
| Mobile-first PWA | OrisLab, OdontoSoft, ODIX = solo desktop |
| WhatsApp nativo | Tutti = messaggistica interna |
| WebAuthn passkey (Touch ID) | Nessun competitor |
| SaaS multi-tenant zero IT | Nessun competitor |
| GDPR pseudonimizzazione nativa | DentalMaster = zero |
| Portale dentista senza login | Nessun competitor |
| Numero progressivo DdC nativo | DentalMaster non ce l'ha |

---

## MAGAZZINO VISIVO — Feature Brainstorm (V1.9)
*Da espandere dopo ricerca Mixel — vedi docs/roadmap/MAGAZZINO-VISIVO-BRAINSTORM.md*

---

## STACK DI ORCHESTRAZIONE

Il progetto usa 3 orchestratori in layers:

| Orchestratore | Layer | Quando | Installato |
|--------------|-------|--------|-----------|
| **Superpowers** | Esecuzione TDD + subagenti | Sempre | ✅ (marketplace Anthropic) |
| **gstack** (Garry Tan, YC) | Decisioni strategiche (23 specialisti simulati) | Feature media/grande | ✅ `~/.agents/skills/gstack` |
| **GSD** (Lex Christopherson) | Stabilità spec multi-sessione | Feature grande (10+ file) | ⚠️ Procedurale (no SKILL.md) |

Procedura completa: `docs/processes/WORKFLOW-STANDARD.md`

---

## CHANGELOG ROADMAP

| Data | Modifica | Chi |
|------|----------|-----|
| 22/05/2026 | Documento creato da riconciliazione 6 file HTML + sessioni di sviluppo | Francesco + Claude |
| 22/05/2026 | V1.9 aggiunta (dettatura vocale, email template, rifacimenti UI, logo DdC, magazzino visivo) | Francesco + Claude |
| 22/05/2026 | Magazzino visivo: Concept C selezionato (Mixel-inspired, tile+glifo+fill-bar) | Francesco + Claude |
| 22/05/2026 | Stack orchestratori documentato: Superpowers + gstack + GSD | Francesco + Claude |
| 25/05/2026 | V1.9.1: S1 fix residui completati — badge LIVE rimosso, preferenza_dashboard toggle, Da fatturare lista inline | Francesco + Claude |
| 26/05/2026 | S2 completato: RifacimentoButton bottom sheet 7 motivi, motion policy, mockup approvato | Francesco + Claude |
| 28/05/2026 | DS v2.3 brainstorming + approvazione: spec completa, tokens.ts, motion.ts v2.1, 4-cat taxonomy | Francesco + Claude |
| 28/05/2026 | DS v2.3 implementazione completa: compliance 100% su tutta la PWA, pre-commit guard attivo — V1.9.3 | Francesco + Claude |
| 05/06/2026 | Sessione design chiusa. Prossimo: S4 email template branding (Supabase) | Francesco + Claude |
| 02/07/2026 | Re-audit completo (11 agenti persona) dopo quasi un mese di inattività (zero commit dal 05/06). Score medio 7.29/10 (era 7.1). Claim "DS v2.3 100%" smentito. 2 blocker critici nuovi trovati (materiali/lotti MDR, dashboard/scadenzario disallineati). Prodotti `BACKLOG-TECNICO-2026-07-02.md` e `FEATURES-E-FLUSSI-2026-07-02.md` come nuova fonte di verità tecnica. | Francesco + Claude |
| 03/07/2026 | B2 risolto: Task 16 (verifica finale) aveva trovato un bug residuo in `src/app/api/scadenzario/route.ts` (non nettava `importo_pagato` sulle fatture con pagamento parziale/credito, disaccordo Scadenzario vs Dashboard/Contabilità cliente) e correttamente riportato BLOCKED — fix applicato (netta il residuo come già fanno le altre superfici), tsc/vitest/build verdi, ri-verificato con dati reali (commit `cbc034b`). Entrambi i 2 blocker critici del re-audit 02/07 sono ora risolti. | Francesco + Claude |
| 03/07/2026 | Review finale sull'intero branch B2 (26 commit) trova un secondo disaccordo tra superfici, invisibile a qualunque review di singolo task: Scadenzario escludeva i lavori "fatturare non ancora inclusi" che Dashboard/Contabilità cliente contano già, e `getContabilitaCliente` non escludeva le fatture in bozza. Chiesto a Francesco quale comportamento adottare (nessuna risposta nel timeout, applicata l'opzione raccomandata coerente con lo spec) — fix `ac48530`, re-review "Ready to merge: Yes". Branch mergiato fast-forward su `main` (`05612ec`) e pushato — deploy Vercel. **B2 chiuso.** | Francesco + Claude |
| 03/07/2026 | 2 follow-up B2 risolti in sessione separata (commit `7fc181b`): bug Service Worker PWA scoperto durante B2 (cachava le fetch RSC di `router.refresh()` nonostante `Cache-Control: no-cache` di Next, causando UI stale su tutta l'app dopo ogni mutazione — verificato dal vivo con Playwright, non solo lettura codice) + allineamento `BACKLOG-TECNICO-2026-07-02.md` (B2 ✅, A4 🔄 parziale). | Francesco + Claude |
| 04/07/2026 | B19 aggiunto e risolto per i 10 ERROR critici: hardening Supabase Security Advisor segnalato da Francesco fuori dal backlog tecnico esistente (RLS mancante su 3 tabelle, 7 Security Definer View, 8 funzioni `SECURITY DEFINER` con `REVOKE`, 33 `search_path` fissati, 3 funzioni dead code eliminate, 5 helper RLS WARN accettato). `get_advisors` conferma 0 ERROR residui. Leaked password protection resta bloccato (piano Free). Branch `worktree-security-advisor-hardening` committato, non ancora mergiato su `main`. Priorità B4 invariata. | Francesco + Claude |
| 05/07/2026 | 2 fix minori risolti e deployati: `PazientiSearchList` migrato a `ua-list-grid` (chiude follow-up B9), stesso bug scoperto e corretto anche in `ClientiSearchList` (card edge-to-edge in produzione); `useReducedMotion()` hydration mismatch risolto con pattern "mounted guard" (stesso di B18/`useTheme.ts`), trade-off approvato da Francesco. Verificato che il bug z-index bottom-sheet segnalato era già risolto (B8 4/5 + hotfix listino) e che l'infrastruttura test RPC locale era già stata costruita la sessione precedente (non mancante, solo da estendere). Merge `85faad1`, CI verde, deploy confermato. Priorità B4 invariata. | Francesco + Claude |
| 05/07/2026 | B4 risolto e mergiato (`as any` nei generatori PDF MDR, 11/11 cast eliminati) — scoperto e corretto come effetto collaterale un bug di produzione reale in `generateDdC()`. A seguire: norme armonizzate applicate (MDR §7, `dichiarazioni_conformita.norme_json`) implementate in worktree dedicato — migration `rischi_tipo_dispositivo.norme_json` applicata al DB live, editor/API/generatore/template DdC aggiornati, 6 task via `superpowers:subagent-driven-development`, 478/4 test verdi. Chiude il follow-up aperto da B4. Non ancora mergiato su `main`. | Francesco + Claude |
| 17/07/2026 | Nuova sequenza operativa ratificata da Francesco: (1) tutti i problemi (nuovo P0-PERF: lentezza sistemica in produzione) → (2) tutte le funzioni attive → (3) design coerente ovunque (sp.4 + admin) → (4) audit multi-agente → (5) collaudo capillare Francesco pagina per pagina. Gli errori grafici visti oggi si raccolgono al collaudo finale. | Francesco + Claude |
| 20/07/2026 | **Ondata A mini-triage MERGIATA E DEPLOYATA** (main `50e6b79`, CI+CD verdi, prod ok; 14 task SDD, review finale «Yes», 2319 test). Il Major QA (TastoSecondario dark su card) RISOLTO pre-merge su ratifica Francesco: light variante C, dark variante A, §5.3 emendato. | Francesco + Claude |
| 20/07/2026 | **Design mini-triage CHIUSO (4 giri) + Parete specificata.** Ratifiche finali: miniature per tipo ✓, home a DUE STANZE Pile↔Parete con personalizzazione per utente (solo pile/sola parete/entrambe) e accesso globale a /cassette (shortcut PWA + Tutto il resto + NavDesk), estetica di riferimento = parete-cassette-v2. Decisioni consolidate in `docs/design/decisions/2026-07-20-mini-triage-e-parete.md`; implementazione ondata A autorizzata — handoff `docs/roadmap/2026-07-21-handoff-implementazione-mini-triage.md` (poi: spec Parete percorso Grande, sessione «Cerca», A8, sessione DB). | Francesco + Claude |
| 20/07/2026 | **Secondo giro mini-triage:** A13 confermato (ponte, redesign odontogramma resta in ondata bridge) · A14 variante A «co-identità» targa cassetta + assegnazione cassetta alla conferma-arrivo IN ondata · «Le pile» eliminata (redirect → /dashboard, O1h chiuso) · ricerca per cassetta: per-pila in ondata, globale in sessione design dedicata (casa: /lavori liberato) · export CSV → «I tuoi dati» in «Il mio laboratorio» (ondata F1). **NUOVA FEATURE IN DESIGN: «La Parete delle Cassette»** (visione Francesco: griglia adattiva drag&drop delle cassette fisiche colorate, ricerca per cassetta/lavoro) — concept 2 direzioni in `docs/design/mockups/2026-07-20-parete-cassette-concept.html`, intervista in corso, collocazione roadmap da ratificare a spec pronta (sostituisce l'idea «parco cassette gestito V2»). | Francesco + Claude |
| 20/07/2026 | **Calendario ondate v3 ratificato** (sezione dedicata sopra) + decisioni mini-triage primo giro: O1i 1A+2A+3A (Esci LinkQuieto, identità+Esci NavDesk, trial ambra in striscia) · export cedolini 2A (card in «Persone» v3) · deferral A10/A11 confermati. A13/A14/O1h/export-lavori in secondo giro (flusso odontogramma, cassetta ripensata cassetta-first, proposta eliminazione «Le pile»). | Francesco + Claude |
| 06/07/2026 | CRUD completo `cicli_produzione` implementato (worktree `worktree-cicli-produzione-crud`) — chiude il follow-up scoperto in QA B5 ("nessun modo di creare un ciclo via UI/API"). Migration live (indice UNIQUE parziale su `codice`, pattern B18), nuove route `POST`/`PATCH`/`DELETE /api/cicli[/id]`, sheet create/edit e bottone elimina. Review finale (Opus) "Ready to merge: Yes" dopo 2 fix Important (validazione PATCH allineata a POST, payload PATCH costruito come delta). 626/626 test, tsc/build puliti. QA browser end-to-end (mai lab Filippo); non verificato manualmente il blocco 409 su DELETE referenziato né il tablet. Non ancora mergiato su `main`. | Francesco + Claude |
| 28/07/2026 | **Regole di piano ed esecuzione ratificate** (`ua-app/CLAUDE.md` §0C): R-P1 un blocco senza marchio è NON provato · R-P2 l'elenco dei file da aprire non lo decide l'autore · R-P6 censimento su ogni identificatore · R-P4 abbozzo inerte + conteggio · R-E1 un compito un esecutore fresco · R-E2 si riferisce, non si patcha. Due scartate con motivo scritto. Panel 3× con una lente **avversariale**, che ha prodotto il ritrovamento decisivo (R-P2 nella forma proposta non avrebbe intercettato il difetto peggiore) e la regola mancante R-P6. Verbale: `docs/processes/2026-07-27-lezioni-piano-ondata-a.md` §7. | Francesco + Claude |
| 28/07/2026 | **Ondata (a): T9 chiuso + la sua coda** (`759fc183`, `63361649`). `POST /api/lavori` crea progressivo, lavoro e denti in **una transazione sola**; 19 test nuovi, **18 su 18 cadevano contro un abbozzo sempre-201** (forza misurata, non dedotta); 3478 verdi, `tsc`/`eslint`/`next build` puliti, DB alla baseline. Due difetti chiusi che il piano non prevedeva: `denti` non-array diventava **lista vuota** (201, zero denti, nessun errore) → ora **422**; e l'anno del numero di lavoro seguiva il fuso del processo invece di Roma, su una catena che alimenta DdC e fatturazione. 🔑 Il nono difetto stava **nella diagnosi, non nel codice**: la suite non fissa il fuso, e su questo Mac il test prescritto sarebbe passato **verde col difetto in casa**. Due residui senza casa → sezione «in coda» sopra. Nono task su nove con un difetto trovato. | Francesco + Claude |
| 28/07/2026 | **Guardie agganciate + accesso con passkey riparato — IN PRODUZIONE** (main `24474b5c`, CI verde, deploy Vercel ok, `/login` 200). I 4 script di verifica del repo non erano eseguiti da nulla e **due non potevano nemmeno fallire**; ora `check-csrf.sh` (riscritta, 0,33 s) e `guardia-reduced-motion.mjs` (4,6 s) girano a ogni commit, le altre due restano manuali con il motivo scritto. Rimosse 104 righe su 149 di `.claude/settings.json` che dichiaravano scansioni di sicurezza inesistenti. Corretta la riga di `CLAUDE.md` §9 che dava una direttiva per protetta da un controllo mai eseguito. Chiusa la questione CSRF sulle route WebAuthn di accesso (esclusione con ragione) e **corretto il difetto attivo** che impediva l'accesso con l'impronta a chi digitava una maiuscola nell'email. I 5 difetti rimasti sono in coda a questa roadmap. | Francesco + Claude |
