# Referto — T11 e T11-bis · ondata (b) · il caricamento delle foto torna vivo

**Data:** 01/08/2026 · **Ramo:** `ondata-b-schermate` · **Commit:** `5507ea18` → `782fe812` → `d57ed0f3`
**Piano:** `docs/superpowers/plans/2026-07-30-album-foto-scheda-lavoro.md` → **Task 11** (+ T11-bis, nato in revisione)
**Da dove vengo:** `docs/roadmap/2026-08-01-t10-referto.md` · **Dove va:** T12 (l'eliminazione dal visore).

**Esito:** ✅ fatto e **approvato in revisione** (conformità ✅ · qualità **approvato**, zero rilievi Importanti).

---

## 1. Che cosa era rotto, e ora non lo è più

**Sul ramo, caricare una foto non funzionava affatto.** Il server era già stato riparato in T3 — pretende
il campo `categoria`, lo valida e **rifiuta** chi non lo manda — ma il client continuava a spedire il campo
vecchio (`descrizione`). Ogni caricamento riceveva un errore.

🔴 **E i chiamanti rotti erano TRE, non uno.** T11 ne ha riparato uno (la scheda del lavoro). **La revisione
ne ha trovati altri due, vivi**, con un `grep` su tutto il repo che nessuno aveva fatto:
`src/lib/wizard/crea-lavoro.ts:393` e `src/components/features/wizard/FrameFatto.tsx:170` — cioè **il
caricamento dal wizard «Nuovo lavoro»**. Da qui **T11-bis**, che li ha chiusi.

🛑 **Nel secondo c'era una trappola:** mandava `'prescrizione'`, che **non è una delle sei categorie**.
Rinominare il campo non sarebbe bastato — il server avrebbe rifiutato lo stesso. L'elenco è **chiuso**
(D72), quindi il valore è `'altro'`, il ripiego che l'elenco stesso prevede.
⚠️ **Conseguenza da sapere:** quella foto ora arriva con la vecchia `descrizione` vuota, quindi **il
dettaglio «era la prescrizione» non è più registrato da nessuna parte.** Non è un errore del task — è un
dato che prima esisteva e ora no. **Se serve, va deciso dove tenerlo.**

---

## 2. Che altro cambia in T11

L'elenco locale delle categorie **sparisce** (si importa quello unico: una copia non la vedrebbe né la spia
della migration né il compilatore) · la riga che **indovinava** la categoria dalla sorgente del file
**sparisce**, e al suo posto il foglio `FoglioCategoria` (§5.41) **la chiede**, una volta per gruppo · i
**due gestori quasi identici** diventano **uno solo** · la foto locale **sparisce quando arriva quella
vera** (prima restava: doppia a schermo e contata due volte) · un **PDF** si rende come **tessera
documento**, mai come immagine rotta.

🔑 **`FoglioCategoria` ha il suo primo chiamante vero, e la trappola dichiarata è stata chiusa qui:** la
prop `ancoraFocus` riceve un **ref stabile**, non un letterale inline — che, ricreato a ogni render,
**strapperebbe il focus all'utente**.

---

## 3. FASE 7 e i conti

```
npx tsc --noEmit → 0 errori
npx vitest run   → Test Files  368 passed | 3 skipped (371)
                   Tests  4207 passed | 19 skipped (4226)
npx next build   → ok
```
Riferimento a T10 chiuso: 367 | 3 file e 4191 | 19 prove. **+1 file, +16 prove**, tutte del file nuovo.
T11-bis non muove i totali: ha rinforzato prove esistenti.

**R-P4:** abbozzo inerte **15 su 16** — l'unica verde è una prova di **assenza**. Numero **riprodotto dal
revisore**, non creduto.

**Le mutazioni:** l'esecutore ne dichiarava una; **il revisore ne ha fatte otto**, e tre sue hanno trovato
comportamenti **non sorvegliati** (v. §5). T11-bis ne porta altre quattro, ognuna con **1 prova accesa**.

---

## 4. 🔴 Un difetto che l'esecutore ha trovato NEL PROPRIO LAVORO

Durante la sua auto-revisione ha scoperto che **la prova sull'àncora del focus passava per la ragione
sbagliata**: sparava un evento di ridimensionamento **senza cambiare davvero la larghezza della finestra**,
quindi React non ri-renderizzava e la prova non poteva accorgersi di niente. Corretta nel secondo commit.

✅ **Il revisore l'ha verificato nel modo giusto:** ha preso il test **della prima versione** e gli ha messo
sotto il difetto (il letterale inline) → **16 prove su 16 verdi**. La prova era davvero cieca, e la
correzione la chiude davvero.

---

## 5. I Minori, per la revisione finale di ramo

1. **Tre comportamenti che la suite non sorveglia** (mutati dal revisore, tutti verdi): il foglio che non si
   chiude più · i PDF che rientrano fra le anteprime · il rilascio dell'indirizzo temporaneo dell'anteprima.
   Il codice è **giusto** in tutti e tre: manca la rete.
2. **In vista elenco nessuna foto ha più il selettore di categoria** (prima la carta locale ne aveva uno).
   Il buco lato banca dati è preesistente e la via di correzione resta (basta tornare alla griglia), quindi
   la direttiva «ogni campo si corregge» **non è violata** — ma è un delta di comportamento che il rapporto
   descriveva come pura rimozione di codice morto.
3. **Lo stub di prova ricopia i sei valori a mano** invece di mappare l'elenco vero: è la stessa copia
   locale contro cui quel modulo mette in guardia, in un file nato oggi.
4. **Una larghezza di finestra forzata non viene mai ripristinata** e resta per le prove successive: stessa
   classe di fragilità su cui l'esecutore era già inciampato una volta.
5. **La spia del «punto unico di scrittura» è testuale** e limitata a un file: non vedrebbe un secondo
   punto scritto altrove.

---

## 6. 🔴 Riferito, non corretto

**① Difetti del piano confermati dalla revisione.** La tabella del Task 11 si presenta come censimento
completo («tutto già censito, R-P6») ma **non nomina il montaggio di `FoglioCategoria`** come voce, **non
nomina mai `ancoraFocus`** — cioè proprio la trappola — e **non porta il requisito del PDF**, che pure sta
nel testo del task. Un censimento che si dichiara completo e non lo è è il modo classico per dimenticare un
caso.

**② La riga «il contatore conta doppio» è fuorviante** (confermato): quel contatore **non ha nessuna resa
visibile** — il testo a schermo usa un altro valore. Il sintomo era reale, ma il suo unico lettore è la
soglia che decide quando la lista cambia forma, ed è lì che la prova è stata ancorata.

**③ L'animazione d'uscita di `FoglioCategoria` è morta** (difetto di T9, reso **osservabile** da T11 che è
il suo primo chiamante): dichiara una variante d'uscita ma restituisce `null` alla chiusura e non ha
`AnimatePresence`, né dentro di sé né nel chiamante. Il modello di casa fa l'opposto. **Vale per tutti e
quattro gli strati nuovi: si decide insieme al gate estetico L2 (T13).**

---

## 7. Che cosa resta da provare a mano — e non è simulabile qui

**Caricare una foto vera e vederla salire.** Le due metà sono coperte da prove separate (il client manda la
categoria; il server rifiuta senza e accetta con), ma la catena intera vuole l'app accesa, le credenziali
del banco e un lavoro vero. **Resta a T13**, e con essa la resa della tessera documento a 390/768/1280 in
chiaro e scuro.

**Nessuna decisione nuova di Francesco:** il verbale resta a **novanta**, la prossima è **D91**.
