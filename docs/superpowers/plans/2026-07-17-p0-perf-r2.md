# P0-PERF R2 — Riduzione round-trip per request — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chiudere il gap residuo post-R1 fino ai budget (TTFB p75 ≤300ms, API GET p75 ≤250ms, login→dashboard ≤2s) riducendo i round-trip interni per request, includendo il fix di sicurezza N11 e l'atomicità N12.

**Architecture:** Due helper di contesto (`getLabContext` claims-based+`React.cache()`, `getFreshLabContext` getUser-based) sostituiscono il boilerplate auth triplicato; `getClaims()` nel middleware (verifica ES256 locale); `Promise.all` sui loader indipendenti; consolidamenti embed PostgREST su riconciliazioni; 2 RPC transazionali per N12. Osservabilità (Server-Timing, log strutturato, Speed Insights, cron budget) installata contestualmente.

**Tech Stack:** Next.js 16.2.6 (App Router) · @supabase/supabase-js 2.105.4 (`getClaims`) · @supabase/ssr 0.5.2 · vitest · Playwright (perf-audit) · PL/pgSQL.

**Spec di riferimento (LEGGE):** `docs/superpowers/specs/2026-07-17-p0-perf-r2-design.md` (rev.2, validata dal panel — leggerla per ogni task).

## Global Constraints

- **Due merge unit:** Unit 1 = Task 1-13 (solo codice, mergiabile senza migration). Unit 2 = Task 14-15 (N12: 🛑 apply migration = gate Francesco PRIMA del merge del codice route).
- `"@supabase/supabase-js": "^2.105.0"` pinned in package.json (Task 1).
- **Mapping status FISSO:** contesto `null` → 401 · contesto con `laboratorioId === null` su route che richiede lab → 403. Deviazione dichiarata: soft-deleted 403→401.
- **MAI `!inner`** sugli embed di contesto (`utenti→laboratori`) e sugli embed riconciliazioni (gruppo 3 e 5).
- **`getUser()` RESTA** su mutazioni (POST/PATCH/PUT/DELETE), fiscale (fatture, pagamenti, PSUR, PEC, stripe), admin, borderline (scadenzario ×2, cedolino, pronti-da-fatturare, nomina-prrc) → tutti via `getFreshLabContext()`.
- Nel layout: check `admin_sistema` PRIMA del check `lab === null`.
- Il LabContext NON si allarga con campi extra per-pagina (select locali restano).
- Suite verde tra le ondate (`npx vitest run` dopo ogni task); commit frequenti formato `perf(scope): …` / `fix(scope): …` / `feat(scope): …`.
- Nessun placeholder, nessun TODO. Nessuna modifica UI/DS.
- Comportamenti odierni con labId vuoto (magazzino/analytics pagina vuota, qualita null) NON si normalizzano.

## Ondate (ordine di esecuzione e commit)

- **A (Task 1-4):** fondamenta — pin, helper, guard test, middleware
- **B (Task 5-8):** layout + dashboard/striscia + pagine (app) + admin
- **C (Task 9-11):** API GET categoria A + sweep B/C + R2c pagine residue
- **D (Task 12):** riconciliazioni embed
- **E (Task 13):** osservabilità (Speed Insights, perf-audit v2, cron)
- **F (Task 14-15):** N12 (merge unit 2)
- **G (Task 16):** verifica finale FASE 7 + review whole-branch

---

### Task 1: Pin supabase-js + verifica FK embed

**Files:**
- Modify: `package.json` (riga `"@supabase/supabase-js": "^2"`)

**Interfaces:**
- Produces: prerequisito codificato nel manifest; conferma FK `utenti.laboratorio_id → laboratori.id` per l'embed PostgREST del Task 2.

- [ ] **Step 1:** In `package.json` sostituire `"@supabase/supabase-js": "^2"` con `"@supabase/supabase-js": "^2.105.0"`. Eseguire `npm install` (aggiorna il lockfile senza cambiare la versione installata 2.105.4).
- [ ] **Step 2:** Verificare la FK dichiarata (pre-condizione embed, riserva SA):
  Run: `grep -rn "laboratorio_id" supabase/schema.sql | grep -i "REFERENCES laboratori" | head -5` e `grep -rn "utenti" supabase/schema.sql | grep -iE "FOREIGN KEY|REFERENCES" | head -5`
  Expected: una constraint FK da `utenti.laboratorio_id` a `laboratori(id)`. Se assente nello schema.sql (stantio), verificare in `supabase/migrations/` (`grep -rn "laboratorio_id" supabase/migrations/*.sql | grep -i references | grep utenti`). Se NON esiste da nessuna parte → STOP, riportare BLOCKED (l'embed non funzionerebbe).
- [ ] **Step 3:** Run: `npx tsc --noEmit` → 0 errori. `npx vitest run --silent` → suite verde (baseline).
- [ ] **Step 4:** Commit: `git add package.json package-lock.json && git commit -m "chore(deps): pin @supabase/supabase-js ^2.105.0 (prerequisito getClaims R2)"`

---

### Task 2: Helper di contesto `lab-context.ts` (TDD)

**Files:**
- Create: `src/lib/supabase/lab-context.ts`
- Test: `src/lib/supabase/__tests__/lab-context.test.ts` (seguire il pattern mock dei test esistenti, es. `src/lib/invito/__tests__/` se presente, altrimenti mock con `vi.mock`)

**Interfaces:**
- Consumes: `getServerUserClient()` da `./server-user`, `getServiceClient()` da `./server-service`
- Produces (usati da TUTTI i task successivi):
  - `type LabContext = { userId: string; email: string | null; ruolo: string; laboratorioId: string | null; nome: string | null; cognome: string | null; lab: { stato: string; trial_ends_at: string | null; nome: string } | null }`
  - `type ContextTimings = { authMs: number; dbMs: number }`
  - `getLabContext(): Promise<LabContext | null>` — cached, claims-based
  - `getLabContextWithTimings(): Promise<{ context: LabContext | null; timings: ContextTimings }>` — stessa cache entry
  - `getFreshLabContext(timings?: Partial<ContextTimings>): Promise<LabContext | null>` — getUser-based, non cached

- [ ] **Step 1: Scrivere i test che falliscono** — casi obbligatori (spec §6):
  1. claims validi + riga utenti con lab → LabContext completo
  2. claims assenti (`data.claims` undefined) → `null`
  3. utente soft-deleted (query `.single()` ritorna null perché il mock verifica che la chain contenga `.is('deleted_at', null)`) → `null`
  4. admin_sistema con `laboratorio_id: null` e `laboratori: null` → context con `laboratorioId: null`, `lab: null` (embed LEFT — il mock DEVE asserire che la select NON contenga `!inner`)
  5. `getFreshLabContext`: user da `getUser()` presente → context; user assente → null; sink timings valorizzato (`authMs`/`dbMs` numerici)
  6. `getLabContext` chiamato 2× nello stesso test → la query `utenti` eseguita 1 sola volta (spy sul mock service client; nota: `React.cache()` in ambiente test vitest non memoizza tra render pass diversi — testare la memoizzazione tramite il wrapper: se `cache` non è attivo nel test env, verificare almeno che le 2 chiamate ritornino lo stesso shape e documentare che la dedup è garantita da React in RSC)

```ts
// src/lib/supabase/__tests__/lab-context.test.ts — struttura
import { describe, it, expect, vi, beforeEach } from 'vitest'

const mockGetClaims = vi.fn()
const mockGetUser = vi.fn()
const mockSingle = vi.fn()
const chain = {
  select: vi.fn().mockReturnThis(), eq: vi.fn().mockReturnThis(),
  is: vi.fn().mockReturnThis(), single: mockSingle,
}
vi.mock('@/lib/supabase/server-user', () => ({
  getServerUserClient: vi.fn(async () => ({ auth: { getClaims: mockGetClaims, getUser: mockGetUser } })),
}))
vi.mock('@/lib/supabase/server-service', () => ({
  getServiceClient: vi.fn(() => ({ from: vi.fn(() => chain) })),
}))

import { getLabContext, getFreshLabContext, getLabContextWithTimings } from '@/lib/supabase/lab-context'

const RIGA = { ruolo: 'titolare', laboratorio_id: 'lab-1', nome: 'Anna', cognome: 'Bianchi',
  laboratori: { stato: 'attivo', trial_ends_at: null, nome: 'Lab Uno' } }

beforeEach(() => { vi.clearAllMocks() })

describe('getLabContext', () => {
  it('claims validi → LabContext completo', async () => {
    mockGetClaims.mockResolvedValue({ data: { claims: { sub: 'u-1', email: 'a@b.it' } }, error: null })
    mockSingle.mockResolvedValue({ data: RIGA, error: null })
    const ctx = await getLabContext()
    expect(ctx).toEqual({ userId: 'u-1', email: 'a@b.it', ruolo: 'titolare', laboratorioId: 'lab-1',
      nome: 'Anna', cognome: 'Bianchi', lab: { stato: 'attivo', trial_ends_at: null, nome: 'Lab Uno' } })
    expect(chain.is).toHaveBeenCalledWith('deleted_at', null)
    const selectArg = chain.select.mock.calls[0][0] as string
    expect(selectArg).toContain('laboratori(')
    expect(selectArg).not.toContain('!inner')
  })
  it('claims assenti → null', async () => {
    mockGetClaims.mockResolvedValue({ data: null, error: null })
    expect(await getLabContext()).toBeNull()
  })
  it('soft-deleted (riga assente) → null', async () => {
    mockGetClaims.mockResolvedValue({ data: { claims: { sub: 'u-del' } }, error: null })
    mockSingle.mockResolvedValue({ data: null, error: { code: 'PGRST116' } })
    expect(await getLabContext()).toBeNull()
  })
  it('admin_sistema senza lab → laboratorioId null, lab null', async () => {
    mockGetClaims.mockResolvedValue({ data: { claims: { sub: 'u-adm' } }, error: null })
    mockSingle.mockResolvedValue({ data: { ...RIGA, ruolo: 'admin_sistema', laboratorio_id: null, laboratori: null }, error: null })
    const ctx = await getLabContext()
    expect(ctx?.ruolo).toBe('admin_sistema')
    expect(ctx?.laboratorioId).toBeNull()
    expect(ctx?.lab).toBeNull()
  })
})

describe('getFreshLabContext', () => {
  it('user presente → context via getUser (NON getClaims), timings valorizzati', async () => {
    mockGetUser.mockResolvedValue({ data: { user: { id: 'u-1', email: 'a@b.it' } } })
    mockSingle.mockResolvedValue({ data: RIGA, error: null })
    const t: { authMs?: number; dbMs?: number } = {}
    const ctx = await getFreshLabContext(t)
    expect(ctx?.laboratorioId).toBe('lab-1')
    expect(mockGetUser).toHaveBeenCalled()
    expect(mockGetClaims).not.toHaveBeenCalled()
    expect(typeof t.authMs).toBe('number')
    expect(typeof t.dbMs).toBe('number')
  })
  it('user assente → null', async () => {
    mockGetUser.mockResolvedValue({ data: { user: null } })
    expect(await getFreshLabContext()).toBeNull()
  })
})

describe('getLabContextWithTimings', () => {
  it('ritorna context + timings numerici', async () => {
    mockGetClaims.mockResolvedValue({ data: { claims: { sub: 'u-1' } }, error: null })
    mockSingle.mockResolvedValue({ data: RIGA, error: null })
    const { context, timings } = await getLabContextWithTimings()
    expect(context?.userId).toBe('u-1')
    expect(typeof timings.authMs).toBe('number')
    expect(typeof timings.dbMs).toBe('number')
  })
})
```

- [ ] **Step 2:** Run: `npx vitest run src/lib/supabase/__tests__/lab-context.test.ts` → FAIL (modulo inesistente).
- [ ] **Step 3: Implementazione**

```ts
// src/lib/supabase/lab-context.ts
import 'server-only'
import { cache } from 'react'
import { getServerUserClient } from './server-user'
import { getServiceClient } from './server-service'

// Contesto utente corrente — UNICA via per il lookup `utenti` self (N11: filtro
// deleted_at SEMPRE, allineato a public.current_lab_id()/has_role()).
// Due vie d'identità (spec R2 §D-2):
// - getLabContext: getClaims() locale (zero rete) — SOLO Server Components e GET
//   categoria A (allowlist in lab-context-allowlist.ts, test di guardia).
// - getFreshLabContext: getUser() di rete — mutazioni, fiscale, admin, borderline.
export type LabContext = {
  userId: string
  email: string | null
  ruolo: string
  laboratorioId: string | null // null legale SOLO per admin_sistema
  nome: string | null
  cognome: string | null
  lab: { stato: string; trial_ends_at: string | null; nome: string } | null
}
export type ContextTimings = { authMs: number; dbMs: number }

// Embed LEFT (MAI !inner): admin_sistema ha laboratorio_id NULL by design.
const SELECT_CONTEXT = 'ruolo, laboratorio_id, nome, cognome, laboratori(stato, trial_ends_at, nome)'

type UtenteRow = {
  ruolo: string
  laboratorio_id: string | null
  nome: string | null
  cognome: string | null
  laboratori: { stato: string; trial_ends_at: string | null; nome: string } | null
}

async function fetchUtenteRow(userId: string): Promise<UtenteRow | null> {
  const svc = getServiceClient()
  const { data } = await svc
    .from('utenti')
    .select(SELECT_CONTEXT)
    .eq('id', userId)
    .is('deleted_at', null)
    .single()
  return (data as UtenteRow | null) ?? null
}

function toContext(userId: string, email: string | null, row: UtenteRow): LabContext {
  return {
    userId,
    email,
    ruolo: row.ruolo,
    laboratorioId: row.laboratorio_id,
    nome: row.nome,
    cognome: row.cognome,
    lab: row.laboratori,
  }
}

const getLabContextCached = cache(
  async (): Promise<{ context: LabContext | null; timings: ContextTimings }> => {
    const t0 = performance.now()
    const supabase = await getServerUserClient()
    const { data } = await supabase.auth.getClaims()
    const authMs = performance.now() - t0
    const sub = data?.claims?.sub as string | undefined
    const email = (data?.claims?.email as string | undefined) ?? null
    if (!sub) return { context: null, timings: { authMs, dbMs: 0 } }
    const t1 = performance.now()
    const row = await fetchUtenteRow(sub)
    const dbMs = performance.now() - t1
    if (!row) return { context: null, timings: { authMs, dbMs } }
    return { context: toContext(sub, email, row), timings: { authMs, dbMs } }
  }
)

export async function getLabContext(): Promise<LabContext | null> {
  return (await getLabContextCached()).context
}

export async function getLabContextWithTimings(): Promise<{
  context: LabContext | null
  timings: ContextTimings
}> {
  return getLabContextCached()
}

export async function getFreshLabContext(
  timings?: Partial<ContextTimings>
): Promise<LabContext | null> {
  const t0 = performance.now()
  const supabase = await getServerUserClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (timings) timings.authMs = performance.now() - t0
  if (!user) return null
  const t1 = performance.now()
  const row = await fetchUtenteRow(user.id)
  if (timings) timings.dbMs = performance.now() - t1
  if (!row) return null
  return toContext(user.id, user.email ?? null, row)
}
```

- [ ] **Step 4:** Run: `npx vitest run src/lib/supabase/__tests__/lab-context.test.ts` → PASS. `npx tsc --noEmit` → 0 errori.
- [ ] **Step 5:** Commit: `git add src/lib/supabase/lab-context.ts src/lib/supabase/__tests__/lab-context.test.ts && git commit -m "feat(auth): helper di contesto getLabContext/getFreshLabContext con filtro deleted_at (N11)"`

---

### Task 3: Allowlist + test di guardia anti-misuse

**Files:**
- Create: `src/lib/supabase/lab-context-allowlist.ts`
- Test: `src/lib/supabase/__tests__/lab-context-guard.test.ts`

**Interfaces:**
- Consumes: filesystem (`node:fs`, `node:path`), `LAB_CONTEXT_ROUTE_ALLOWLIST` (array di path relativi a `src/`)
- Produces: `LAB_CONTEXT_ROUTE_ALLOWLIST: readonly string[]` — i 28 file route categoria A che POSSONO importare `getLabContext`.

- [ ] **Step 1: Allowlist** (i 28 file dal censimento spec §3 — path relativi a `src/`):

```ts
// src/lib/supabase/lab-context-allowlist.ts
// GET categoria A (spec R2 §3, censimento 17/07/2026): SOLO questi route file
// possono usare getLabContext (claims-based). Ogni altro route.ts usa
// getFreshLabContext. Il test di guardia lab-context-guard.test.ts fa enforcement.
export const LAB_CONTEXT_ROUTE_ALLOWLIST = [
  'app/api/cicli/route.ts',
  'app/api/clienti/route.ts',
  'app/api/clienti/[id]/route.ts',
  'app/api/clienti/[id]/dpa/route.ts',
  'app/api/clienti/[id]/studio-members/route.ts',
  'app/api/dashboard/kpi/route.ts',
  'app/api/fasi-produzione/ricerca/route.ts',
  'app/api/fornitori/route.ts',
  'app/api/impostazioni/route.ts',
  'app/api/lavori/route.ts',
  'app/api/lavori/[id]/route.ts',
  'app/api/lavori/[id]/etichetta/route.ts',
  'app/api/lavori/[id]/ifu/route.ts',
  'app/api/lavori/[id]/precheck-consegna/route.ts',
  'app/api/lavori/[id]/prove/route.ts',
  'app/api/lavori/[id]/ricevuta-consegna/route.ts',
  'app/api/lavori/[id]/scheda-fabbricazione/route.ts',
  'app/api/listino/route.ts',
  'app/api/listino/[id]/route.ts',
  'app/api/magazzino/route.ts',
  'app/api/magazzino/[id]/lotti/route.ts',
  'app/api/ordini/route.ts',
  'app/api/pazienti/route.ts',
  'app/api/qualita/incidenti/route.ts',
  'app/api/qualita/rischi/route.ts',
  'app/api/rete/route.ts',
  'app/api/tecnici/route.ts',
  'app/api/tecnici/[id]/produttivita/route.ts',
] as const
```

- [ ] **Step 2: Test di guardia** (3 regole; scansione ricorsiva di `src/app`):

```ts
// src/lib/supabase/__tests__/lab-context-guard.test.ts
import { describe, it, expect } from 'vitest'
import { readFileSync, readdirSync, statSync } from 'node:fs'
import { join, relative } from 'node:path'
import { LAB_CONTEXT_ROUTE_ALLOWLIST } from '@/lib/supabase/lab-context-allowlist'

const SRC = join(__dirname, '..', '..', '..') // → src/
function walk(dir: string, out: string[] = []): string[] {
  for (const e of readdirSync(dir)) {
    const p = join(dir, e)
    if (statSync(p).isDirectory()) walk(p, out)
    else if (e.endsWith('.ts') || e.endsWith('.tsx')) out.push(p)
  }
  return out
}
const files = walk(join(SRC, 'app'))
const rel = (p: string) => relative(SRC, p).replaceAll('\\', '/')

// Estrae il body di ogni handler mutante (fino al prossimo `export` o EOF)
function mutationBodies(src: string): string[] {
  const out: string[] = []
  const re = /export\s+async\s+function\s+(POST|PATCH|PUT|DELETE)\b/g
  let m: RegExpExecArray | null
  while ((m = re.exec(src))) {
    const rest = src.slice(m.index + m[0].length)
    const next = rest.search(/\nexport\s/)
    out.push(next === -1 ? rest : rest.slice(0, next))
  }
  return out
}

describe('guardia lab-context (spec R2 §D-2)', () => {
  it('getLabContext usato SOLO nei route.ts in allowlist', () => {
    const violazioni = files
      .filter((f) => rel(f).endsWith('route.ts'))
      .filter((f) => readFileSync(f, 'utf8').includes('getLabContext('))
      .map(rel)
      .filter((r) => !LAB_CONTEXT_ROUTE_ALLOWLIST.includes(r as never))
    expect(violazioni).toEqual([])
  })
  it('nessun handler mutante usa getLabContext (nemmeno in file allowlist)', () => {
    const violazioni = files
      .filter((f) => rel(f).endsWith('route.ts'))
      .filter((f) => mutationBodies(readFileSync(f, 'utf8')).some((b) => b.includes('getLabContext(')))
      .map(rel)
    expect(violazioni).toEqual([])
  })
  it('nessun lookup utenti self diretto fuori da lab-context.ts (anti-bypass N11)', () => {
    const violazioni = files
      .filter((f) => {
        const s = readFileSync(f, 'utf8')
        return s.includes("from('utenti')") && /\.eq\(\s*'id'\s*,\s*(user!?\.id|userId|user\.id)/.test(s)
      })
      .map(rel)
    expect(violazioni).toEqual([])
  })
})
```

- [ ] **Step 3:** Run: `npx vitest run src/lib/supabase/__tests__/lab-context-guard.test.ts` → i primi 2 test PASS (nessuno usa ancora getLabContext), il 3° **FAIL con la lista completa delle ~100+ violazioni** = è la todo-list dei Task 5-10. Marcare temporaneamente il 3° test `it.todo`/`it.skip` con commento `// si attiva al Task 10 (fine sweep)` — i primi 2 restano attivi da subito.
- [ ] **Step 4:** Commit: `git add src/lib/supabase/lab-context-allowlist.ts src/lib/supabase/__tests__/lab-context-guard.test.ts && git commit -m "feat(auth): allowlist categoria A + test di guardia anti-misuse helper contesto"`

---

### Task 4: Middleware `getClaims()` + x-pathname + Server-Timing (TDD)

**Files:**
- Modify: `src/middleware.ts`
- Test: `src/__tests__/middleware.test.ts` (nuovo; mock di `@/lib/supabase/middleware-client`)

**Interfaces:**
- Consumes: `createMiddlewareClient(request, response)`
- Produces: request header `x-pathname` (letto dal layout, Task 5); header `Server-Timing: auth;dur=N` su OGNI response (anche redirect).

- [ ] **Step 1: Test che falliscono** — casi: (1) route pubblica senza sessione → next, no redirect; (2) route protetta senza claims → redirect `/login?next=...`; (3) route protetta con claims → next + `x-pathname` sui request headers + `Server-Timing` sulla response; (4) `/auth/callback` → passthrough; (5) autenticato su `/login` → redirect `/dashboard` con `Server-Timing`; (6) **refresh 3-assert (spec §D-1)**: mock `getClaims` che simula token scaduto → il client chiama `setAll` (cookie riscritti sulla response: assert `Set-Cookie` presente); refresh fallito (`getClaims` → `{data:null}`) → redirect `/login` senza throw; claims validi → nessun secondo giro.

```ts
// src/__tests__/middleware.test.ts — struttura mock
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { NextRequest } from 'next/server'

const mockGetClaims = vi.fn()
let capturedResponse: { cookies: { set: (n: string, v: string, o?: unknown) => void } } | null = null
vi.mock('@/lib/supabase/middleware-client', () => ({
  createMiddlewareClient: vi.fn((_req: unknown, res: never) => {
    capturedResponse = res
    return { auth: { getClaims: mockGetClaims } }
  }),
}))
import { middleware } from '@/middleware'

const req = (path: string) => new NextRequest(new URL(`https://uachelab.com${path}`))
const CLAIMS = { data: { claims: { sub: 'u-1' } }, error: null }
const NO_CLAIMS = { data: null, error: null }

beforeEach(() => { vi.clearAllMocks(); capturedResponse = null })

it('protetta senza claims → redirect /login con next', async () => {
  mockGetClaims.mockResolvedValue(NO_CLAIMS)
  const res = await middleware(req('/lavori'))
  expect(res.status).toBe(307)
  expect(res.headers.get('location')).toContain('/login?next=%2Flavori')
  expect(res.headers.get('server-timing')).toMatch(/auth;dur=\d+/)
})
it('protetta con claims → next, x-pathname sui request headers, server-timing', async () => {
  mockGetClaims.mockResolvedValue(CLAIMS)
  const res = await middleware(req('/lavori'))
  expect(res.status).toBe(200)
  expect(res.headers.get('x-middleware-request-x-pathname') ?? res.headers.get('server-timing')).toBeTruthy()
  expect(res.headers.get('server-timing')).toMatch(/auth;dur=\d+/)
})
it('refresh: cookie riscritti quando il client li setta', async () => {
  mockGetClaims.mockImplementation(async () => {
    capturedResponse!.cookies.set('sb-access-token', 'nuovo', {})
    return CLAIMS
  })
  const res = await middleware(req('/dashboard'))
  expect(res.headers.get('set-cookie')).toContain('sb-access-token=nuovo')
})
it('refresh fallito → redirect login, mai throw', async () => {
  mockGetClaims.mockResolvedValue(NO_CLAIMS)
  await expect(middleware(req('/dashboard'))).resolves.toBeTruthy()
})
// + casi: pubbliche, /auth/callback, autenticato su /login → /dashboard
```

- [ ] **Step 2:** Run: `npx vitest run src/__tests__/middleware.test.ts` → FAIL (middleware usa ancora getUser; x-pathname/Server-Timing assenti).
- [ ] **Step 3: Implementazione** — riscrittura `src/middleware.ts`:

```ts
// RESPONSABILITÀ: redirect visivi UX only — non è un confine di sicurezza.
// L'autorizzazione reale è nel contesto server-side (getLabContext/getFreshLabContext,
// query utenti fresca per-request) + scoping laboratorio_id. R2b′: getClaims()
// verifica ES256 locale (JWKS in cache) — zero rete a token valido; a token
// scaduto passa dal refresh di rete e riscrive i cookie via setAll (invariato).
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { createMiddlewareClient } from '@/lib/supabase/middleware-client'

const PUBLIC_ROUTES = ['/login', '/invite', '/forgot-password', '/reset-password', '/blocked', '/billing', '/portale', '/richiedi', '/ds-v3-catalogo']
const AUTH_CALLBACK = '/auth/callback'

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // x-pathname: il layout (app) non può leggere il pathname (gotcha CLAUDE.md §9);
  // lo inoltriamo come request header per il log strutturato (spec R2 §D-6).
  const requestHeaders = new Headers(request.headers)
  requestHeaders.set('x-pathname', pathname)
  const response = NextResponse.next({ request: { headers: requestHeaders } })

  if (pathname.startsWith(AUTH_CALLBACK)) return response

  const t0 = Date.now()
  const supabase = createMiddlewareClient(request, response)
  const { data } = await supabase.auth.getClaims()
  const authMs = Date.now() - t0
  const claims = data?.claims ?? null

  // Server-Timing anche sui redirect (riserva sre R6b)
  const withTiming = (res: NextResponse) => {
    res.headers.set('Server-Timing', `auth;dur=${authMs}`)
    return res
  }

  const isPublicRoute = PUBLIC_ROUTES.some(r => pathname.startsWith(r))

  if (!claims && !isPublicRoute) {
    const loginUrl = new URL('/login', request.url)
    if (pathname !== '/' && pathname !== '/dashboard') {
      loginUrl.searchParams.set('next', pathname)
    }
    return withTiming(NextResponse.redirect(loginUrl))
  }

  if (
    claims &&
    isPublicRoute &&
    pathname !== '/blocked' &&
    pathname !== '/billing' &&
    !pathname.startsWith('/ds-v3-catalogo')
  ) {
    return withTiming(NextResponse.redirect(new URL('/dashboard', request.url)))
  }

  return withTiming(response)
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|api/|portale/|richiedi/|sw\\.js|manifest\\.json|offline\\.html|icons/|sounds/|fonts/|animations/|.*\\.(?:svg|png|jpg|jpeg|gif|webp|ico|mp3|wav)$).*)',
  ],
}
```

  **Nota redirect+cookie:** i redirect creati qui NON ereditano automaticamente i cookie settati su `response` da `setAll`. Verificare nel test refresh: se il ramo redirect può seguire un refresh (token scaduto→refresh ok→claims presenti→utente su /login→redirect /dashboard), i cookie vanno copiati: `response.cookies.getAll().forEach(c => redirectRes.cookies.set(c.name, c.value, c))` dentro `withTiming` quando `res !== response`. Implementare questa copia.
- [ ] **Step 4:** Run: `npx vitest run src/__tests__/middleware.test.ts` → PASS. `npx tsc --noEmit` → 0.
- [ ] **Step 5:** Commit: `git add src/middleware.ts src/__tests__/middleware.test.ts && git commit -m "perf(auth): R2b' — getClaims() locale nel middleware + x-pathname + Server-Timing"`

---

### Task 5: Layout `(app)` su getLabContext + log strutturato (TDD)

**Files:**
- Modify: `src/app/(app)/layout.tsx`
- Test: `src/app/(app)/__tests__/layout.test.tsx` (nuovo; mock `@/lib/supabase/lab-context` e `next/navigation`)

**Interfaces:**
- Consumes: `getLabContextWithTimings()` (Task 2); `headers()` da `next/headers` per `x-pathname`
- Produces: layout con 3 RT→1; ordine check admin-first; log 1-riga `[layout] {"route":…,"authMs":…,"dbMs":…,"totalMs":…}` emesso PRIMA di ogni redirect.

- [ ] **Step 1: Test che falliscono** — mock di `getLabContextWithTimings` e `redirect` (che in Next lancia — mockare con throw sentinella). Casi:
  1. context null → redirect `/login`
  2. `ruolo: 'admin_sistema'`, `lab: null` → redirect `/admin/labs` (**NON** `/login?error=no_lab` — ordine admin-first, riserva BA R7)
  3. context con `lab: null` (non admin) → redirect `/login?error=no_lab`
  4. lab `blacklist` → `/blocked`; `sospeso` → `/billing`; `scaduto` → `/billing?expired=true`; trial scaduto → `/billing?trial_expired=true`
  5. lab attivo → render (nessun redirect); `console.log` chiamato una volta con stringa contenente `authMs`
  6. il log è emesso ANCHE nel caso redirect (spy su console.log prima del throw del redirect)
- [ ] **Step 2:** Run → FAIL.
- [ ] **Step 3: Implementazione** — sostituire le righe 12-45 di `layout.tsx`:

```tsx
import { headers } from 'next/headers'
import { getLabContextWithTimings } from '@/lib/supabase/lab-context'
// (rimuovere gli import getServerUserClient/getServiceClient)

export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const t0 = performance.now()
  const { context, timings } = await getLabContextWithTimings()
  const hdrs = await headers()
  const route = hdrs.get('x-pathname') ?? 'unknown'
  // Log PRIMA di ogni redirect (redirect() lancia — riserva sre R6c)
  const logLine = () => console.log(`[layout] ${JSON.stringify({
    route, authMs: Math.round(timings.authMs), dbMs: Math.round(timings.dbMs),
    totalMs: Math.round(performance.now() - t0),
  })}`)

  if (!context) { logLine(); redirect('/login') }
  // Ordine VINCOLATO (spec §D-2): admin PRIMA del check lab-null
  if (context.ruolo === 'admin_sistema') { logLine(); redirect('/admin/labs') }
  if (!context.lab || !context.laboratorioId) { logLine(); redirect('/login?error=no_lab') }

  const lab = context.lab
  if (lab.stato === 'blacklist') { logLine(); redirect('/blocked') }
  if (lab.stato === 'sospeso') { logLine(); redirect('/billing') }
  if (lab.stato === 'scaduto') { logLine(); redirect('/billing?expired=true') }
  if (lab.stato === 'trial' && lab.trial_ends_at !== null && new Date(lab.trial_ends_at) < new Date()) {
    logLine(); redirect('/billing?trial_expired=true')
  }

  const isTrialExpiring = lab.stato === 'trial' && lab.trial_ends_at
    ? (new Date(lab.trial_ends_at).getTime() - Date.now()) < 7 * 24 * 60 * 60 * 1000
    : false
  logLine()

  return (
    <RealtimeProvider laboratorioId={context.laboratorioId} ruolo={context.ruolo}>
      {/* … JSX invariato, con: nome={context.nome ?? ''} cognome={context.cognome}
           email={context.email ?? ''} ruolo={context.ruolo} labNome={lab.nome}
           trialEndsAt={lab.trial_ends_at} labStato={lab.stato} … */}
    </RealtimeProvider>
  )
}
```

- [ ] **Step 4:** Run test nuovi + `npx vitest run --silent` (intera suite: i test esistenti del layout, se presenti, vanno adattati ai nuovi mock) → PASS. `npx tsc --noEmit` → 0.
- [ ] **Step 5:** Commit: `perf(auth): R2a' — layout (app) su getLabContext (3 RT→1) + log strutturato + fix N11 layout`

---

### Task 6: Striscia split + dashboard `Promise.all` (TDD)

**Files:**
- Modify: `src/lib/dashboard/striscia.ts` · `src/app/(app)/dashboard/page.tsx`
- Test: estendere il test esistente di striscia se presente (cercare `striscia` in `src/**/__tests__`), altrimenti creare `src/lib/dashboard/__tests__/striscia.test.ts`

**Interfaces:**
- Consumes: `getLabContext()` (Task 2)
- Produces: `fetchIngressiStriscia(svc, labId, ruolo): Promise<Omit<IngressiStriscia, 'pile'>>` — 4 query interne in `Promise.all`, degradi try/catch PER-RAMO (mai tutto-o-niente); `getSegnaleStriscia` resta come wrapper compatibile (fetch+compose) per eventuali altri consumer.

- [ ] **Step 1: Test** — (1) `fetchIngressiStriscia` con ruolo titolare: 4 query partono, risultati composti; (2) una query fallisce → SOLO quel campo degrada a null/0, gli altri arrivano (spy console.error); (3) ruolo tecnico: query fiscali/pagamenti NON eseguite; (4) `scegliSegnale` invariato (test esistenti restano verdi).
- [ ] **Step 2:** Run → FAIL (`fetchIngressiStriscia` non esiste).
- [ ] **Step 3: Implementazione striscia.ts** — refactor di `getSegnaleStriscia` (righe 68-127): estrarre 4 funzioni private che INCLUDONO il proprio try/catch (identici a oggi: stessi messaggi `console.error`, stessi fallback), poi:

```ts
export async function fetchIngressiStriscia(
  svc: SupabaseClient, labId: string, ruolo: string
): Promise<Omit<IngressiStriscia, 'pile'>> {
  const [fatturaScartata, materialeRosso, pagamentoScaduto, ddcOggi] = await Promise.all([
    usaFiscali(ruolo) ? leggiFatturaScartata(svc, labId) : Promise.resolve(null),
    usaFiscali(ruolo) ? leggiMaterialeRosso(svc, labId) : Promise.resolve(null),
    usaPagamenti(ruolo) ? leggiPagamentoScaduto(svc, labId) : Promise.resolve(null),
    leggiDdcOggi(svc, labId),
  ])
  return { fatturaScartata, materialeRosso, pagamentoScaduto, ddcOggi }
}

export async function getSegnaleStriscia(svc: SupabaseClient, labId: string, ruolo: string, pile: PileHome): Promise<SegnaleStriscia> {
  const ingressi = await fetchIngressiStriscia(svc, labId, ruolo)
  return scegliSegnale(ruolo, { ...ingressi, pile: pile.striscia })
}
```

- [ ] **Step 4: dashboard/page.tsx** — sostituire righe 32-44 con:

```tsx
import { getLabContext } from '@/lib/supabase/lab-context'
import { fetchIngressiStriscia, scegliSegnale } from '@/lib/dashboard/striscia'
// dentro DashboardPage:
  const context = await getLabContext()
  if (!context?.laboratorioId) redirect('/login')
  const { ruolo, laboratorioId: labId } = context
  if (!['titolare', 'admin_rete', 'tecnico', 'front_desk'].includes(ruolo)) redirect('/login')

  const svc = getServiceClient()
  const perimetro = await getPerimetroHome(svc, labId, context.userId, ruolo)
  const [pile, ingressi] = await Promise.all([
    getPileHome(svc, labId, perimetro),
    fetchIngressiStriscia(svc, labId, ruolo),
  ])
  const segnale = scegliSegnale(ruolo, { ...ingressi, pile: pile.striscia })
  const nome = context.nome ?? context.email?.split('@')[0] ?? 'Utente'
```

- [ ] **Step 5:** Run: test striscia + suite → PASS. `npx tsc --noEmit` → 0.
- [ ] **Step 6:** Commit: `perf(dashboard): R2c — striscia 4-way Promise.all + pile∥ingressi, contesto deduplicato col layout`

---

### Task 7: Sweep pagine `(app)` → getLabContext (meccanico, lista chiusa)

**Files (Modify — pattern IDENTICO per tutte):** le pagine dal censimento N11 sezione B che fanno il lookup self:
`(app)/ordini/page.tsx:47` · `(app)/impostazioni/page.tsx:108` · `(app)/impostazioni/abbonamento/page.tsx:14` · `(app)/rete/page.tsx:35` · `(app)/rete/invito/[token]/page.tsx:47` · `(app)/rete/[id]/page.tsx:19` · `(app)/clienti/page.tsx:26` · `(app)/clienti/[id]/page.tsx:114` · `(app)/tecnici/page.tsx:29` · `(app)/scadenzario/[cliente_id]/page.tsx:31` · `(app)/fatture/page.tsx:82` · `(app)/fatture/riconciliazioni/page.tsx:24` · `(app)/fatture/[id]/page.tsx:25` · `(app)/lavori/page.tsx:30` · `(app)/lavori/[id]/page.tsx:22` · `(app)/lavori/[id]/modifica/page.tsx:37` · `(app)/lavori/nuovo/page.tsx:22` · `(app)/magazzino/page.tsx:36` · `(app)/magazzino/[id]/page.tsx:17` · `(app)/qualita/page.tsx:40` · `(app)/qualita/rischi/page.tsx:35` · `(app)/qualita/rischi/[id]/page.tsx:24` · `(app)/qualita/psur/page.tsx:19` · `(app)/agenda/page.tsx:64` · `(app)/listino/page.tsx:17` · `(app)/pazienti/page.tsx:25` · `(app)/pazienti/[id]/page.tsx:18` · `(app)/cicli-produzione/page.tsx:17` · `(app)/cicli-produzione/[id]/page.tsx:23` · `(app)/onboarding/page.tsx:12` · `(app)/analytics/page.tsx:140` · `(app)/tutto-il-resto/page.tsx:21` · `(app)/tecnici/[id]/produttivita/page.tsx:32`

**Interfaces:**
- Consumes: `getLabContext()` (Task 2)
- Produces: pagine senza getUser/query utenti duplicati (dedup col layout via `cache()`).

- [ ] **Step 1: Trasformazione (identica per ogni file).** PRIMA:

```tsx
const userClient = await getServerUserClient()
const { data: { user } } = await userClient.auth.getUser()
if (!user) redirect('/login')
const svc = getServiceClient()
const { data: utente } = await svc.from('utenti').select('…').eq('id', user.id)[.is('deleted_at', null)].single()
if (!utente) redirect('/login')
```

  DOPO:

```tsx
const context = await getLabContext()
if (!context?.laboratorioId) redirect('/login')
// campi: context.laboratorioId, context.ruolo, context.nome, context.userId, context.email
const svc = getServiceClient() // resta per le query di dominio
```

  **Regole per-file:** (a) whitelist ruolo esistenti (lavori, lavori/nuovo, tutto-il-resto: `if (!['titolare',…].includes(ruolo)) redirect(…)`) restano IDENTICHE; (b) redirect/return odierni per utente-mancante restano allo stesso valore (`/login`, `/login?error=…`, pagina vuota, null — NON normalizzare, spec §D-2); (c) `abbonamento/page.tsx`: eliminare ANCHE la query `laboratori` separata → select locale embed `utenti.select('laboratorio_id, laboratori(nome, stato, piano, trial_ends_at, stripe_subscription_status, stripe_customer_id)')`? NO — usare `context.laboratorioId` + UNA query `laboratori` locale coi campi extra (piano/stripe_* NON sono nel LabContext e la query singola resta 1 RT); (d) `rete/invito/[token]` legge `email, ruolo` → `context.email`, `context.ruolo`; (e) `onboarding` legge `nome, cognome` → dal context.
- [ ] **Step 2:** Run: `npx vitest run --silent` → suite verde (i test pagina esistenti con mock getUser vanno adattati al mock di `getLabContext` — stesso shape del Task 2). `npx tsc --noEmit` → 0.
- [ ] **Step 3:** Grep di controllo: `grep -rln "auth.getUser()" "src/app/(app)" | sort` → deve restituire SOLO file legittimamente esclusi (nessuno atteso; `billing/page.tsx` è FUORI da `(app)` e si tratta al Task 10).
- [ ] **Step 4:** Commit (uno per gruppo logico, 3-4 commit): `perf(pages): R2a' — pagine <gruppo> su getLabContext (dedup layout, N11)`

---

### Task 8: Admin (layout + pagine + gate) su getFreshLabContext (TDD)

**Files:**
- Modify: `src/app/admin/layout.tsx:15` · `src/app/admin/labs/[id]/live/page.tsx:40` · `src/app/admin/labs/[id]/view/page.tsx:16` · `src/app/billing/page.tsx:19`
- Test: `src/app/admin/__tests__/admin-gate.test.tsx` (nuovo)

**Interfaces:**
- Consumes: `getFreshLabContext()` (Task 2)
- Produces: gate admin con filtro `deleted_at` (★ N11).

- [ ] **Step 1: Test** — admin soft-deleted (context null) → redirect/403; ruolo ≠ admin_sistema → comportamento odierno invariato.
- [ ] **Step 2:** Trasformazione: stesso pattern del Task 7 ma con `getFreshLabContext()` (admin = getUser resta, decisione ratificata). `billing/page.tsx` (legge `laboratorio_id, nome`): `getFreshLabContext()` (pagina di gating abbonamento, conservativo).
- [ ] **Step 3:** Run test + suite → PASS. Commit: `fix(admin): N11 — gate admin/billing su getFreshLabContext (deleted_at filtrato)`

---

### Task 9: GET categoria A (28 handler) → getLabContext + withServerTiming (TDD)

**Files:**
- Create: `src/lib/api/server-timing.ts`
- Modify: i 28 route file dell'allowlist (Task 3) — SOLO i loro handler **GET**
- Test: `src/lib/api/__tests__/server-timing.test.ts` + adattamento test route esistenti

**Interfaces:**
- Consumes: `getLabContextWithTimings()` (Task 2)
- Produces: `withServerTiming(handler: (t: Partial<ContextTimings>) => Promise<NextResponse>): Promise<NextResponse>` — appende `Server-Timing: auth;dur=…, db;dur=…, total;dur=…`.

- [ ] **Step 1: server-timing.ts + test:**

```ts
// src/lib/api/server-timing.ts
import 'server-only'
import type { NextResponse } from 'next/server'
import type { ContextTimings } from '@/lib/supabase/lab-context'

export async function withServerTiming(
  handler: (t: Partial<ContextTimings>) => Promise<NextResponse>
): Promise<NextResponse> {
  const t: Partial<ContextTimings> = {}
  const t0 = performance.now()
  const res = await handler(t)
  const parts: string[] = []
  if (t.authMs !== undefined) parts.push(`auth;dur=${Math.round(t.authMs)}`)
  if (t.dbMs !== undefined) parts.push(`db;dur=${Math.round(t.dbMs)}`)
  parts.push(`total;dur=${Math.round(performance.now() - t0)}`)
  res.headers.set('Server-Timing', parts.join(', '))
  return res
}
```

  Test: handler che valorizza il sink → header con 3 fasi; handler che non lo valorizza → solo `total`.
- [ ] **Step 2: Trasformazione GET categoria A (pattern per tutti i 28).** PRIMA (es. `api/clienti/route.ts:11-28`): getUser→401→svc→utenti→403. DOPO:

```ts
import { getLabContextWithTimings } from '@/lib/supabase/lab-context'
import { withServerTiming } from '@/lib/api/server-timing'

export async function GET(req: Request) {
  return withServerTiming(async (t) => {
    const { context, timings } = await getLabContextWithTimings()
    Object.assign(t, timings)
    if (!context) return NextResponse.json({ error: 'Non autorizzato' }, { status: 401 })
    if (!context.laboratorioId) return NextResponse.json({ error: 'Laboratorio non trovato' }, { status: 403 })
    const labId: string = context.laboratorioId
    // …corpo esistente INVARIATO (svc e query di dominio come oggi);
    // dove serviva utente.ruolo → context.ruolo
  })
}
```

  Messaggi di errore odierni per-file conservati (es. `'Non autorizzato'`/`'Laboratorio non trovato'`). Handler mutanti negli stessi file: NON toccarli qui (Task 10).
- [ ] **Step 3:** Riattivare eventuali assert 401/403 nei test esistenti: la **deviazione dichiarata** (soft-deleted: prima 403/pass → ora 401) va asserita in ALMENO un test per handler campione (clienti GET): mock context null → 401.
- [ ] **Step 4:** Run: suite completa + guard test (i primi 2 della guardia ora esercitati davvero) → PASS. `npx tsc --noEmit` → 0.
- [ ] **Step 5:** Commit (2 commit: helper+metà, poi resto): `perf(api): R2a' — GET categoria A su getLabContext + Server-Timing (N11)`

---

### Task 10: Sweep handler B/C + helper verify* + WebAuthn (meccanico, lista chiusa)

**Files (Modify):** TUTTI i restanti file col boilerplate getUser→utenti (censimento N11 sezione B, ~55 file API): `verify-titolare.ts` · `verify-admin-rete.ts` · route admin/* (8) · fatture/* · pagamenti/* · pec/* · impostazioni/pec/* · stripe/checkout+portal · qualita/psur · scadenzario ×2 · tecnici/[id]/cedolino · lavori/pronti-da-fatturare · impostazioni/nomina-prrc · tutte le mutazioni (POST/PATCH/PUT/DELETE) dei file già toccati al Task 9 · notifications/subscribe · auth/webauthn/register/options+verify · rete/* residue · cicli/[id]* · magazzino/[id]* · pazienti/[id] · lavori/[id]/* mutazioni · listino/[id] mutazioni · clienti/[id]/* mutazioni.

**Interfaces:**
- Consumes: `getFreshLabContext(t?)` (Task 2)
- Produces: N11 chiuso su TUTTA la superficie; 3° test di guardia riattivabile.

- [ ] **Step 1: Trasformazione (pattern unico).** PRIMA: getUser→401 + `svc.from('utenti').select('…').eq('id', user.id).single()`→403. DOPO:

```ts
const context = await getFreshLabContext()
if (!context) return NextResponse.json({ error: 'Non autorizzato' }, { status: 401 })
if (!context.laboratorioId) return NextResponse.json({ error: 'Laboratorio non trovato' }, { status: 403 })
// ruolo: context.ruolo · labId: context.laboratorioId · nome: context.nome · user.id: context.userId
```

  **Regole:** (a) gate ruolo esistenti INVARIATI (whitelist, `admin_sistema`, titolare); (b) route admin che NON richiedono lab: saltare il check `laboratorioId` (solo `!context` → 401 + gate ruolo); (c) `verifyTitolare()`/`verifyAdminRete()`: riscrivere il corpo sopra `getFreshLabContext()` mantenendo firma e valori di ritorno IDENTICI (i consumer non cambiano); (d) WebAuthn `register/options` (legge nome/cognome) e `register/verify` (nessuna query utenti oggi): aggiungere `const context = await getFreshLabContext(); if (!context) → 401` (chiude l'enrollment per soft-deleted — riserva appsec 3); `auth/accept-invite`: NON toccare (eccezione by-design, aggiungere commento `// N11: pre-membership by design — nessun lookup utenti (spec R2 §3)`); (e) messaggi/status per-file conservati (alcuni usano 403 per no-user: conservare SOLO se un test esistente lo blinda, altrimenti standard 401 — annotare nel report ogni deviazione).
- [ ] **Step 2:** Riattivare il 3° test di guardia (togliere `.skip` dal test anti-bypass, Task 3). Run: `npx vitest run src/lib/supabase/__tests__/lab-context-guard.test.ts` → PASS (zero violazioni residue).
- [ ] **Step 3: Security test N11 (spec §6):** in `src/lib/supabase/__tests__/n11-security.test.ts` — soft-deleted (context null da helper) su: 1 GET A (clienti), 1 mutazione B (clienti POST), 1 fiscale C (fatture GET), `verifyTitolare`, `verifyAdminRete`, WebAuthn register → tutti 401/403, MAI 200.
- [ ] **Step 4:** Run: suite completa → PASS. `npx tsc --noEmit` → 0. `grep -rn "from('utenti')" src/app src/lib | grep -v lab-context | grep "eq('id'"` → vuoto (o solo sezione C del censimento: lookup di ALTRI utenti, legittimi).
- [ ] **Step 5:** Commit (3-4 commit per area): `fix(api): N11 — sweep <area> su getFreshLabContext (deleted_at ovunque)`

---

### Task 11: R2c — Promise.all pagine residue

**Files:**
- Modify: `src/app/(app)/magazzino/page.tsx` (righe ~48-61) · `src/app/(app)/qualita/page.tsx` (righe ~49-68) · `src/app/(app)/analytics/page.tsx` (righe ~152-153) · `src/app/(app)/lavori/[id]/page.tsx` (righe ~68-75)

**Interfaces:**
- Consumes: query esistenti dei file (invariate nella forma)
- Produces: gruppi paralleli: magazzino `[articoli, fornitori]` · qualita `[nc, rischi, incidenti]` · analytics `[getTrendMensile, kpiCache]` · lavori/[id] `[firmaDdc, firmeImmagini]`.

- [ ] **Step 1:** Trasformazione per file — es. magazzino:

```tsx
const [articoliRes, fornitoriRes] = await Promise.all([
  svc.from('magazzino').select(/* select ESISTENTE invariata */)…,
  svc.from('fornitori').select(/* invariata */)…,
])
```

  lavori/[id]: le due operazioni firma (DdC condizionale + map immagini) in un unico `Promise.all([lavoro.ddc ? getSignedUrl(…) : Promise.resolve(null), Promise.all(immagini.map(…))])`. Gestione errori per-ramo come oggi (se oggi un fallimento firma non blocca la pagina, conservare col medesimo try/catch).
- [ ] **Step 2:** Run: suite → verde. `npx tsc --noEmit` → 0. Commit: `perf(pages): R2c — Promise.all su loader indipendenti (magazzino, qualita, analytics, scheda lavoro)`

---

### Task 12: R2d — consolidamento embed riconciliazioni (TDD, fixture obbligatorie)

**Files:**
- Modify: `src/lib/fattura/ricevute/queries-riconciliazioni.ts` (gruppo 3: righe ~132-151; gruppo 5: righe ~259-286)
- Test: estendere il test esistente delle queries riconciliazioni (cercarlo con `grep -rln "queries-riconciliazioni" src --include="*.test.*"`)

**Interfaces:**
- Consumes: shape di ritorno ESISTENTI di `fetchStornateConTd04Rifiutato` e `fetchEventiParcheggiati` (INVARIATE)
- Produces: 2 query → 1 per ciascun gruppo.

- [ ] **Step 1: Fixture di parità PRIMA del refactor (spec §D-5):**
  - Gruppo 3: originale stornata con TD04-A `rifiutata` + TD04-B `accettata` → l'originale DEVE restare esclusa (regola anti-loop: serve l'INTERO set dei TD04).
  - Gruppo 5: evento con `fattura_id NULL` → DEVE restare nel risultato (`fatture: null`).
  Scrivere i test con le query ATTUALI → verde (baseline di parità).
- [ ] **Step 2: Refactor gruppo 3** — self-join embed:

```ts
const { data } = await svc.from('fatture')
  .select('id, numero, stornata_at, td04:fatture!fattura_collegata_id(id, numero, stato_sdi)')
  // filtri TOP-LEVEL identici a oggi (laboratorio_id, stornata_at not null, deleted_at)
  // filtri EMBED: SOLO tipo_documento='TD04' e deleted_at IS NULL — MAI stato_sdi (anti-loop)
  .eq('td04.tipo_documento', 'TD04')
  .is('td04.deleted_at', null)
// laboratorio_id sui figli: implicito per invariante same-lab (trigger) — commento nel codice
```

  Match in memoria invariato, ora su `riga.td04` invece della seconda query.
- [ ] **Step 3: Refactor gruppo 5** — embed LEFT (MAI `!inner`):

```ts
const { data } = await svc.from('fatture_sdi_eventi')
  .select('/* colonne esistenti */, fatture(stato_sdi)')
  // filtri top-level identici (laboratorio_id, stato_a null, OR 3 motivi)
```

  Filtro terminalità in memoria come forma esplicita: evento tenuto se `fatture === null` (fattura_id NULL o non risolta) oppure `!['accettata','rifiutata'].includes(fatture.stato_sdi)` — replicare ESATTAMENTE la condizione odierna della riga ~299.
- [ ] **Step 4:** Run: test parità (fixture) → PASS con le query nuove. Suite → verde. Commit: `perf(fatture): R2d — riconciliazioni: self-join TD04 + LEFT embed eventi (4 query→2)`

---

### Task 13: Osservabilità — Speed Insights + perf-audit v2 + cron budget

**Files:**
- Modify: `src/app/layout.tsx` (root) · `scripts/perf-audit.ts` · `package.json` (dep `@vercel/speed-insights`)
- Create: `.github/workflows/perf-budget.yml`

**Interfaces:**
- Produces: `<SpeedInsights/>` nel root layout; `perf-audit.ts` con `PERF_BASE`, `PERF_RUNS` (default 5, primo giro warmup scartato), `PERF_BYPASS` (header `x-vercel-protection-bypass`), exit code 1 se p75 > budget con `PERF_ENFORCE=1`; workflow cron giornaliero.

- [ ] **Step 1:** `npm install @vercel/speed-insights`; in `src/app/layout.tsx`: `import { SpeedInsights } from '@vercel/speed-insights/next'` + `<SpeedInsights />` prima di `</body>`.
- [ ] **Step 2: perf-audit.ts v2** — modifiche: `const BASE = process.env.PERF_BASE ?? 'https://uachelab.com'`; `const RUNS = Number(process.env.PERF_RUNS ?? 5)`; loop run `0..RUNS` con run 0 = warmup scartato; se `process.env.PERF_BYPASS` → `extraHTTPHeaders: { 'x-vercel-protection-bypass': process.env.PERF_BYPASS }` sul context; alla fine calcolo p75 per pagine e API + stampa tabella; `if (process.env.PERF_ENFORCE === '1' && (p75Pages > 300 || p75Api > 250 || loginMs > 2000)) process.exit(1)`. Budget in costanti `BUDGET_TTFB_MS = 300`, `BUDGET_API_MS = 250`, `BUDGET_LOGIN_MS = 2000`.
- [ ] **Step 3: workflow:**

```yaml
# .github/workflows/perf-budget.yml
name: perf-budget
on:
  schedule: [{ cron: '30 5 * * *' }]
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: npm ci && npx playwright install chromium --with-deps
      - run: PERF_ENFORCE=1 npx tsx scripts/perf-audit.ts
```

  (Il fallimento del job genera la mail GitHub — alerting minimo, riserva sre R1.)
- [ ] **Step 4:** Run: `npx tsc --noEmit` → 0; `PERF_RUNS=1 npx tsx scripts/perf-audit.ts --help 2>/dev/null || true` non richiesto — verifica solo type-check + suite. Commit: `feat(obs): Speed Insights + perf-audit v2 (PERF_BASE/RUNS/BYPASS/ENFORCE) + cron budget giornaliero`

---

### Task 14 (MERGE UNIT 2): Migration RPC N12 (SQL, review rafforzata)

**Files:**
- Create: `supabase/migrations/20260717120000_n12_prove_atomiche.sql`

**Interfaces:**
- Produces: `manda_in_prova_atomico(p_lavoro_id uuid, p_laboratorio_id uuid, p_data_rientro date, p_istruzioni text, p_user_id uuid) RETURNS jsonb` e `registra_rientro_atomico(p_lavoro_id uuid, p_laboratorio_id uuid, p_prova_id uuid, p_esito text, p_note text, p_stato_destinazione text, p_user_id uuid) RETURNS jsonb`. ERRCODE: `UA404` not-found/cross-tenant · `UA409` transizione/prova già chiusa.

- [ ] **Step 1: Scrivere la migration** (contenuto COMPLETO — adattare i nomi colonna esatti leggendo `src/app/api/lavori/[id]/prove/route.ts` e `src/lib/lavori/transizioni.ts` PRIMA di scrivere):

```sql
-- N12: atomicità route prove (spec R2 §D-5). SECURITY INVOKER: chiamate SOLO dal
-- service client (bypass RLS come oggi); niente escalation DEFINER.
-- Archi copiati 1:1 dal SERVER (TRANSIZIONI_CONSENTITE in src/lib/lavori/transizioni.ts),
-- NON dalla UI: manda_in_prova: {in_lavorazione,in_ritardo}→in_prova_esterna;
-- rientro: in_prova_esterna→{in_lavorazione,sospeso,annullato} + legacy in_ritardo→in_lavorazione.

CREATE OR REPLACE FUNCTION public.manda_in_prova_atomico(
  p_lavoro_id uuid, p_laboratorio_id uuid, p_data_rientro date,
  p_istruzioni text, p_user_id uuid
) RETURNS jsonb
LANGUAGE plpgsql SECURITY INVOKER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_stato text;
  v_numero int;
  v_prova lavoro_prove%ROWTYPE;
BEGIN
  -- Tenant isolation DENTRO la transazione (riserva appsec ALTA)
  SELECT stato INTO v_stato FROM lavori
   WHERE id = p_lavoro_id AND laboratorio_id = p_laboratorio_id AND deleted_at IS NULL
   FOR UPDATE;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'lavoro non trovato' USING ERRCODE = 'UA404';
  END IF;
  IF v_stato NOT IN ('in_lavorazione', 'in_ritardo') THEN
    RAISE EXCEPTION 'transizione non consentita da %', v_stato USING ERRCODE = 'UA409';
  END IF;

  UPDATE lavori SET stato = 'in_prova_esterna', updated_at = now() WHERE id = p_lavoro_id;

  SELECT COALESCE(MAX(numero_prova), 0) + 1 INTO v_numero
    FROM lavoro_prove WHERE lavoro_id = p_lavoro_id;

  INSERT INTO lavoro_prove (lavoro_id, laboratorio_id, numero_prova,
    data_rientro_prevista, istruzioni, created_by)
  VALUES (p_lavoro_id, p_laboratorio_id, v_numero, p_data_rientro, p_istruzioni, p_user_id)
  RETURNING * INTO v_prova;

  RETURN jsonb_build_object('prova', to_jsonb(v_prova), 'stato', 'in_prova_esterna');
END;
$$;

CREATE OR REPLACE FUNCTION public.registra_rientro_atomico(
  p_lavoro_id uuid, p_laboratorio_id uuid, p_prova_id uuid,
  p_esito text, p_note text, p_stato_destinazione text, p_user_id uuid
) RETURNS jsonb
LANGUAGE plpgsql SECURITY INVOKER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_stato text;
  v_lavoro record;
BEGIN
  SELECT l.stato, l.tecnico_id, l.numero_lavoro INTO v_lavoro FROM lavori l
   WHERE l.id = p_lavoro_id AND l.laboratorio_id = p_laboratorio_id AND l.deleted_at IS NULL
   FOR UPDATE;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'lavoro non trovato' USING ERRCODE = 'UA404';
  END IF;
  v_stato := v_lavoro.stato;
  -- whitelist sorgenti-per-destinazione (parità server, incluso legacy in_ritardo)
  IF NOT (
    (v_stato = 'in_prova_esterna' AND p_stato_destinazione IN ('in_lavorazione','sospeso','annullato'))
    OR (v_stato = 'in_ritardo' AND p_stato_destinazione = 'in_lavorazione')
  ) THEN
    RAISE EXCEPTION 'transizione non consentita da % a %', v_stato, p_stato_destinazione USING ERRCODE = 'UA409';
  END IF;

  UPDATE lavoro_prove
     SET data_rientro_effettiva = now(), esito = p_esito, note_rientro = p_note
   WHERE id = p_prova_id AND lavoro_id = p_lavoro_id AND data_rientro_effettiva IS NULL;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'prova non trovata o già chiusa' USING ERRCODE = 'UA409';
  END IF;

  UPDATE lavori SET stato = p_stato_destinazione, updated_at = now() WHERE id = p_lavoro_id;

  RETURN jsonb_build_object('stato', p_stato_destinazione,
    'tecnico_id', v_lavoro.tecnico_id, 'numero_lavoro', v_lavoro.numero_lavoro);
END;
$$;

REVOKE EXECUTE ON FUNCTION public.manda_in_prova_atomico(uuid,uuid,date,text,uuid) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION public.registra_rientro_atomico(uuid,uuid,uuid,text,text,text,uuid) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.manda_in_prova_atomico(uuid,uuid,date,text,uuid) TO service_role;
GRANT EXECUTE ON FUNCTION public.registra_rientro_atomico(uuid,uuid,uuid,text,text,text,uuid) TO service_role;
```

  ⚠️ **PRIMA di committare:** confrontare nomi colonna (`istruzioni`, `note_rientro`, `esito`, `created_by`, `data_rientro_prevista/effettiva`) e i campi update dello stato con la route REALE e con `database.types.ts` — correggere la SQL dove diverge (il codice sopra è il progetto; la verità sono i file). Il vincolo `prova_numero_unique UNIQUE(lavoro_id, numero_prova)` NON si tocca (backstop).
- [ ] **Step 2:** Review SQL rafforzata inline: rileggere contro spec §D-5 (tenant nel FOR UPDATE ✓, archi completi ✓, ERRCODE ✓, search_path ✓, INVOKER+REVOKE/GRANT ✓, RETURNS con riga prova / tecnico_id+numero_lavoro ✓).
- [ ] **Step 3:** Commit: `feat(db): N12 — RPC transazionali manda_in_prova_atomico/registra_rientro_atomico (migration, apply gated)`
- [ ] **Step 4:** 🛑 **GATE: NON applicare la migration.** Segnalare nel report che l'apply è azione Francesco (poi FASE 6b: `npx supabase gen types typescript --project-id iagibumwjstnveqpjbwq > src/types/database.types.ts` + rimozione eventuale coda CLI + `npx tsc --noEmit`).

---

### Task 15 (MERGE UNIT 2): Route prove sui 2 RPC (TDD, mock RPC)

**Files:**
- Modify: `src/app/api/lavori/[id]/prove/route.ts` (POST, entrambi i rami)
- Test: test esistenti della route prove (adattare) + casi nuovi

**Interfaces:**
- Consumes: `svc.rpc('manda_in_prova_atomico', {...})` / `svc.rpc('registra_rientro_atomico', {...})` (Task 14); `getFreshLabContext()` già in uso dal Task 10
- Produces: POST atomico; mapping errori: `UA404`→404 · `UA409`→409 · `23505`→409 · altro→500. Il mapping esito→stato RESTA in TS (route calcola `p_stato_destinazione` con la stessa logica odierna: `ok|modifiche`→`in_lavorazione`, `sospeso`→`sospeso`, `rifare`→`annullato`).

- [ ] **Step 1: Test che falliscono** — mock `svc.rpc`: (1) `manda_in_prova` felice → 200 `{prova, stato}` shape invariato; (2) rpc error code `UA404` → 404 stesso messaggio odierno; (3) `UA409` → 409; (4) `23505` → 409; (5) rientro felice per OGNI arco: `in_prova_esterna→in_lavorazione/sospeso/annullato` + `in_ritardo→in_lavorazione` (mock rpc ok) → 200 + push chiamata con `tecnico_id`/`numero_lavoro` del payload RPC; (6) rpc `UA409` su rientro → 409 E NESSUN update parziale (il mock di `from('lavoro_prove').update` NON deve essere chiamato — comportamento atteso NUOVO, deviazione dichiarata); (7) errore generico → 500.
- [ ] **Step 2:** Run → FAIL. **Step 3: Implementazione** — nel ramo `manda_in_prova` sostituire righe ~103-127 (transizione+count+insert) con:

```ts
const { data, error } = await svc.rpc('manda_in_prova_atomico', {
  p_lavoro_id: lavoroId, p_laboratorio_id: labId,
  p_data_rientro: dataRientro, p_istruzioni: istruzioni ?? null, p_user_id: context.userId,
})
if (error) return mapRpcError(error)
return NextResponse.json(data) // { prova, stato }
```

  Ramo rientro: calcolo `statoDestinazione` in TS (logica odierna invariata) → `svc.rpc('registra_rientro_atomico', {...})` → push post-commit con `data.tecnico_id`/`data.numero_lavoro` (fire-and-forget, fuori transazione). Helper locale:

```ts
function mapRpcError(error: { code?: string; message?: string }): NextResponse {
  if (error.code === 'UA404') return NextResponse.json({ error: 'Lavoro non trovato o accesso negato' }, { status: 404 })
  if (error.code === 'UA409' || error.code === '23505') return NextResponse.json({ error: error.message ?? 'Operazione non consentita' }, { status: 409 })
  return NextResponse.json({ error: 'Errore interno' }, { status: 500 })
}
```

  Tipizzazione `svc.rpc`: finché i types non sono rigenerati (FASE 6b post-apply), cast esplicito con commento `// tipo rigenerato in FASE 6b post-apply`.
- [ ] **Step 4:** Run: suite → PASS. `npx tsc --noEmit` → 0. Commit: `fix(lavori): N12 — route prove atomica sui RPC (UA404/UA409, race numero_prova eliminata)`

---

### Task 16: FASE 7 completa + riconciliazione finale

- [ ] **Step 1:** `npx tsc --noEmit` → 0 errori (output reale nel report).
- [ ] **Step 2:** `npx vitest run` → tutta la suite verde (conteggio nel report; timeout noti da carico: ri-eseguire i file falliti in isolamento come da prassi).
- [ ] **Step 3:** `npx next build` → success (output reale).
- [ ] **Step 4:** Grep finali: `grep -rn "auth.getUser()" src/app "src/lib" | grep -v lab-context | grep -v webauthn/login | grep -v accept-invite | grep -v test` → SOLO occorrenze legittime documentate (dentro `getFreshLabContext`, login webauthn senza sessione, accept-invite). Guard test attivi tutti e 3.
- [ ] **Step 5:** Commit finale + report di branch per la review whole-branch (FASE 8).

---

## Self-review del piano (eseguita)

- **Copertura spec:** D-1→Task 4 · D-2→Task 2,3,5,7,9,10 · D-3→Task 2,5,8,10 (WebAuthn incluso) · D-4→Task 6,11 · D-5→Task 12,14,15 · D-6→Task 4,5,9,13 · D-7→§10 spec (azioni Francesco, fuori piano) · §7 misura→Task 13 + FASE 9/handoff.
- **Tipi coerenti:** `LabContext`/`ContextTimings` (Task 2) usati identici in 4,5,6,9,10; `withServerTiming` firma unica (Task 9→13); RPC names identici Task 14↔15.
- **Nessun placeholder**; per gli sweep (7, 10) la trasformazione è definita col pattern completo + lista file chiusa + regole per-file (deviazioni ammesse solo se annotate nel report del task).
- **Gate:** 🛑 apply migration (Task 14 Step 4) · 🛑 merge unit 1 e 2 (Francesco) · FASE 9 QA e misura (handoff §4, fuori piano — sessione principale).
