# N13 — Guard `lab.stato` nelle API — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Applicare la matrice ratificata stato-lab × metodo HTTP a tutte le API (helper `assertLabOperativo`), con guardia dedicata per il portale token, test di guardia statico anti-drift, shadow mode e kill-switch.

**Architecture:** Nuovo modulo puro `src/lib/supabase/lab-guard.ts` chiamato a inizio handler (early-return della 403 pronta, niente throw). Nessuna modifica a RLS, middleware o helper di contesto. Allowlist esenzioni esportata e test statico gemello di `lab-context-guard.test.ts` che impone la chiamata in ogni `route.ts` non esente. Portale token: blacklist → 404 generico dentro `risolviClientePortale`/route manuali. Client: interceptor fetch globale montato in `(app)/layout.tsx` che mappa `UA_LAB_*` → `/impostazioni/abbonamento`.

**Tech Stack:** Next.js 16 App Router, Supabase (service client), Vitest, TypeScript strict.

**Documento decisione (LEGGE, non ridiscutere):** `docs/design/decisions/2026-07-17-N13-N14-N11bis-ratifiche.md`

## Global Constraints

- Contratto errore: `403` + body `{ error: string, code: 'UA_LAB_*' }` + header `Cache-Control: no-store`. MAI 402.
- Codici DISTINTI: `UA_LAB_TRIAL_SCADUTO` / `UA_LAB_SOSPESO` / `UA_LAB_SCADUTO` / `UA_LAB_BLACKLIST`; fail-closed generico `UA_LAB_NON_OPERATIVO`.
- Matrice ratificata: trial/attivo tutto ok · trial-scaduto/sospeso/scaduto → mutazioni 403, GET ok · blacklist → tutto 403 GET inclusi · stato sconosciuto → 403 fail-closed · `lab===null` non-admin → 403 fail-closed · `admin_sistema` bypass totale · `admin_rete` = come titolare.
- Kill-switch: env `UA_LAB_GUARD_MODE` ∈ `off | shadow | enforce`; default in codice (`LAB_GUARD_DEFAULT_MODE`) = `'shadow'` per questa ondata — il flip a `'enforce'` è un commit dedicato DOPO la finestra shadow 24-48h (decisione con Francesco).
- Guard NEGLI handler, MAI dentro `getLabContext`/`getFreshLabContext`, MAI in middleware.
- Portale token: blacklist → **404 generico** `{ errore: 'non_trovato' }` (no info-leak); sospeso/scaduto → read consentito (nessun cambiamento).
- Esenzioni (allowlist esportata): `stripe/checkout`, `stripe/portal`, `stripe/webhook`, `auth/*`, `portale/*` (guardia propria), `internal/pec-verify`, `fatture/export` (export GDPR/portabilità).
- Commit frequenti nel worktree dedicato; formato commit `feat(security): …` / `fix(security): …`.
- MAI toccare il working tree principale (contiene N11-bis + N14 non committati).

---

## Censimento (17/07/2026, base per il rollout)

106 `route.ts` totali · 80 con handler mutanti · 25 solo-GET · 1 senza handler (`fatture/[id]` = `export {}`).

Helper per file: `getFreshLabContext` 75 · `getLabContextWithTimings` 28 (GET categoria A, allowlist `lab-context-allowlist.ts`) · `verifyTitolare` 2 (`tecnici/invite`, `tecnici/invite/[id]`) · `verifyAdminRete` 4 (`rete/[id]`, `rete/[id]/inviti`, `rete/[id]/inviti/[invitoId]`, `rete/[id]/membri/[laboratorioId]`) · `verifyAdmin` locale 8 file `admin/*` (ritorna il LabContext completo → guard = bypass no-op, si include per uniformità anti-drift).

GET che usano `getFreshLabContext` (si guardano come GET): `fatture` (GET), `fatture/export` (ESENTE), `impostazioni/nomina-prrc`, `impostazioni/pec/verify-status`, `lavori/pronti-da-fatturare`, `qualita/psur` (GET), `scadenzario`, `scadenzario/[cliente_id]`, `tecnici/[id]/cedolino`, `tecnici/invite` (GET via vTit).

---

### Task 1: Modulo core `lab-guard.ts` (TDD)

**Files:**
- Create: `src/lib/supabase/lab-guard.ts`
- Test: `tests/unit/lab-guard.test.ts`

**Interfaces:**
- Produces: `type LabGuardInput = { ruolo: string; lab: { stato: string; trial_ends_at: string | null } | null }` · `assertLabOperativo(ctx: LabGuardInput | null, method: 'GET'|'HEAD'|'POST'|'PATCH'|'PUT'|'DELETE'): NextResponse | null` · `getLabGuardMode(): 'off'|'shadow'|'enforce'` · `LAB_GUARD_DEFAULT_MODE`
- `LabContext` di `lab-context.ts` soddisfa strutturalmente `LabGuardInput`.

- [ ] **Step 1: Scrivere il test che fallisce**

```typescript
// tests/unit/lab-guard.test.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
import { assertLabOperativo, getLabGuardMode, LAB_GUARD_DEFAULT_MODE } from '@/lib/supabase/lab-guard'

const LAB_ATTIVO = { stato: 'attivo', trial_ends_at: null }
const ctx = (over: Partial<{ ruolo: string; lab: { stato: string; trial_ends_at: string | null } | null }> = {}) => ({
  ruolo: 'titolare',
  lab: LAB_ATTIVO,
  ...over,
})

async function bodyOf(res: Response) {
  return res.json() as Promise<{ error: string; code: string }>
}

describe('assertLabOperativo — matrice ratificata (enforce)', () => {
  beforeEach(() => { process.env.UA_LAB_GUARD_MODE = 'enforce' })
  afterEach(() => { delete process.env.UA_LAB_GUARD_MODE; vi.restoreAllMocks() })

  it('trial e attivo: tutto consentito', () => {
    expect(assertLabOperativo(ctx(), 'GET')).toBeNull()
    expect(assertLabOperativo(ctx(), 'POST')).toBeNull()
    expect(assertLabOperativo(ctx({ lab: { stato: 'trial', trial_ends_at: new Date(Date.now() + 86_400_000).toISOString() } }), 'PATCH')).toBeNull()
    expect(assertLabOperativo(ctx({ lab: { stato: 'trial', trial_ends_at: null } }), 'DELETE')).toBeNull() // null = override admin, mai scade
  })

  it('trial scaduto: GET ok, mutazione 403 UA_LAB_TRIAL_SCADUTO', async () => {
    const scaduto = ctx({ lab: { stato: 'trial', trial_ends_at: new Date(Date.now() - 1000).toISOString() } })
    expect(assertLabOperativo(scaduto, 'GET')).toBeNull()
    const res = assertLabOperativo(scaduto, 'POST')!
    expect(res.status).toBe(403)
    expect((await bodyOf(res)).code).toBe('UA_LAB_TRIAL_SCADUTO')
    expect(res.headers.get('Cache-Control')).toBe('no-store')
  })

  it.each([
    ['sospeso', 'UA_LAB_SOSPESO'],
    ['scaduto', 'UA_LAB_SCADUTO'],
  ])('%s: GET/HEAD ok, mutazioni 403 %s', async (stato, code) => {
    const c = ctx({ lab: { stato, trial_ends_at: null } })
    expect(assertLabOperativo(c, 'GET')).toBeNull()
    expect(assertLabOperativo(c, 'HEAD')).toBeNull()
    for (const m of ['POST', 'PATCH', 'PUT', 'DELETE'] as const) {
      const res = assertLabOperativo(c, m)!
      expect(res.status).toBe(403)
      expect((await bodyOf(res)).code).toBe(code)
    }
  })

  it('blacklist: TUTTO 403, GET inclusi', async () => {
    const c = ctx({ lab: { stato: 'blacklist', trial_ends_at: null } })
    for (const m of ['GET', 'HEAD', 'POST', 'PATCH', 'PUT', 'DELETE'] as const) {
      const res = assertLabOperativo(c, m)!
      expect(res.status).toBe(403)
      expect((await bodyOf(res)).code).toBe('UA_LAB_BLACKLIST')
    }
  })

  it('stato sconosciuto: 403 fail-closed anche in GET', async () => {
    const res = assertLabOperativo(ctx({ lab: { stato: 'stato_futuro', trial_ends_at: null } }), 'GET')!
    expect(res.status).toBe(403)
    expect((await bodyOf(res)).code).toBe('UA_LAB_NON_OPERATIVO')
  })

  it('ctx null o lab null non-admin: 403 fail-closed', async () => {
    expect(assertLabOperativo(null, 'GET')!.status).toBe(403)
    const res = assertLabOperativo(ctx({ lab: null }), 'POST')!
    expect(res.status).toBe(403)
    expect((await bodyOf(res)).code).toBe('UA_LAB_NON_OPERATIVO')
  })

  it('admin_sistema: bypass totale anche con lab null', () => {
    const admin = ctx({ ruolo: 'admin_sistema', lab: null })
    expect(assertLabOperativo(admin, 'DELETE')).toBeNull()
  })

  it('admin_rete: come titolare (nessun bypass)', () => {
    const c = ctx({ ruolo: 'admin_rete', lab: { stato: 'sospeso', trial_ends_at: null } })
    expect(assertLabOperativo(c, 'POST')).not.toBeNull()
    expect(assertLabOperativo(c, 'GET')).toBeNull()
  })
})

describe('assertLabOperativo — modalità', () => {
  afterEach(() => { delete process.env.UA_LAB_GUARD_MODE; vi.restoreAllMocks() })

  it('default senza env = shadow (ondata rollout)', () => {
    delete process.env.UA_LAB_GUARD_MODE
    expect(LAB_GUARD_DEFAULT_MODE).toBe('shadow')
    expect(getLabGuardMode()).toBe('shadow')
  })

  it('env non valida → default', () => {
    process.env.UA_LAB_GUARD_MODE = 'banana'
    expect(getLabGuardMode()).toBe(LAB_GUARD_DEFAULT_MODE)
  })

  it('shadow: would-block loggato, richiesta consentita', () => {
    process.env.UA_LAB_GUARD_MODE = 'shadow'
    const warn = vi.spyOn(console, 'warn').mockImplementation(() => {})
    const res = assertLabOperativo(ctx({ lab: { stato: 'blacklist', trial_ends_at: null } }), 'GET')
    expect(res).toBeNull()
    expect(warn).toHaveBeenCalledTimes(1)
    expect(warn.mock.calls[0].join(' ')).toContain('UA_LAB_BLACKLIST')
  })

  it('off (kill-switch): nessun log, nessun blocco', () => {
    process.env.UA_LAB_GUARD_MODE = 'off'
    const warn = vi.spyOn(console, 'warn').mockImplementation(() => {})
    expect(assertLabOperativo(ctx({ lab: { stato: 'blacklist', trial_ends_at: null } }), 'POST')).toBeNull()
    expect(warn).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Run test → FAIL** — `npx vitest run tests/unit/lab-guard.test.ts` → "Cannot find module '@/lib/supabase/lab-guard'"

- [ ] **Step 3: Implementazione minima**

```typescript
// src/lib/supabase/lab-guard.ts
// N13 (decisione ratificata 17/07/2026): guard applicativa lab.stato × metodo.
// Chiamata a INIZIO handler, early-return della 403 pronta — MAI negli helper
// di contesto (servono anche ai Server Component dove il gate è redirect),
// MAI in middleware (lab.stato non è nei claims).
import { NextResponse } from 'next/server'

export type LabGuardMode = 'off' | 'shadow' | 'enforce'

// Default dell'ondata di rollout: shadow (log-only). Il flip a 'enforce' è un
// commit dedicato dopo la finestra 24-48h; l'env resta il kill-switch.
export const LAB_GUARD_DEFAULT_MODE: LabGuardMode = 'shadow'

export type LabGuardInput = {
  ruolo: string
  lab: { stato: string; trial_ends_at: string | null } | null
}
export type GuardMethod = 'GET' | 'HEAD' | 'POST' | 'PATCH' | 'PUT' | 'DELETE'

export type LabGuardCode =
  | 'UA_LAB_TRIAL_SCADUTO'
  | 'UA_LAB_SOSPESO'
  | 'UA_LAB_SCADUTO'
  | 'UA_LAB_BLACKLIST'
  | 'UA_LAB_NON_OPERATIVO'

const MESSAGGI: Record<LabGuardCode, string> = {
  UA_LAB_TRIAL_SCADUTO: 'Periodo di prova terminato: passa a un piano per continuare a modificare i dati.',
  UA_LAB_SOSPESO: 'Abbonamento sospeso: la modifica dei dati è disabilitata.',
  UA_LAB_SCADUTO: 'Abbonamento scaduto: rinnova il piano per continuare.',
  UA_LAB_BLACKLIST: 'Account disabilitato.',
  UA_LAB_NON_OPERATIVO: 'Laboratorio non disponibile.',
}

export function getLabGuardMode(): LabGuardMode {
  const env = process.env.UA_LAB_GUARD_MODE
  return env === 'off' || env === 'shadow' || env === 'enforce' ? env : LAB_GUARD_DEFAULT_MODE
}

const isRead = (m: GuardMethod) => m === 'GET' || m === 'HEAD'

// Decisione pura (esportata per i test): null = consentito, altrimenti codice.
export function decideLabOperativo(ctx: LabGuardInput | null, method: GuardMethod): LabGuardCode | null {
  if (!ctx) return 'UA_LAB_NON_OPERATIVO' // fail-closed
  if (ctx.ruolo === 'admin_sistema') return null // bypass totale (laboratorio_id NULL by design)
  if (!ctx.lab) return 'UA_LAB_NON_OPERATIVO' // fail-closed: non-admin senza lab
  switch (ctx.lab.stato) {
    case 'attivo':
      return null
    case 'trial': {
      const scaduto = ctx.lab.trial_ends_at !== null && new Date(ctx.lab.trial_ends_at) < new Date()
      return scaduto && !isRead(method) ? 'UA_LAB_TRIAL_SCADUTO' : null
    }
    case 'sospeso':
      return isRead(method) ? null : 'UA_LAB_SOSPESO'
    case 'scaduto':
      return isRead(method) ? null : 'UA_LAB_SCADUTO'
    case 'blacklist':
      return 'UA_LAB_BLACKLIST' // terminale: anche i GET
    default:
      return 'UA_LAB_NON_OPERATIVO' // fail-closed su stati sconosciuti/futuri
  }
}

export function assertLabOperativo(ctx: LabGuardInput | null, method: GuardMethod): NextResponse | null {
  const mode = getLabGuardMode()
  if (mode === 'off') return null
  const code = decideLabOperativo(ctx, method)
  if (!code) return null
  if (mode === 'shadow') {
    console.warn(`[lab-guard] would-block ${JSON.stringify({ code, method, ruolo: ctx?.ruolo ?? null, stato: ctx?.lab?.stato ?? null })}`)
    return null
  }
  return NextResponse.json(
    { error: MESSAGGI[code], code },
    { status: 403, headers: { 'Cache-Control': 'no-store' } }
  )
}
```

- [ ] **Step 4: Run test → PASS** — `npx vitest run tests/unit/lab-guard.test.ts`
- [ ] **Step 5: Commit** — `git add src/lib/supabase/lab-guard.ts tests/unit/lab-guard.test.ts && git commit -m "feat(security): lab-guard core con matrice stati, shadow mode e kill-switch (N13)"`

---

### Task 2: Allowlist esenzioni + test di guardia statico (RED atteso)

**Files:**
- Create: `src/lib/supabase/lab-guard-exempt-routes.ts`
- Test: `tests/unit/lab-guard-static.test.ts`

**Interfaces:**
- Produces: `LAB_GUARD_EXEMPT_ROUTES: readonly string[]` (path relativi a `src/`, stile `lab-context-allowlist.ts`).

- [ ] **Step 1: Allowlist**

```typescript
// src/lib/supabase/lab-guard-exempt-routes.ts
// N13: route ESENTI dalla guard lab.stato — ogni riga ha un perché.
// Il test lab-guard-static.test.ts impone assertLabOperativo( in tutte le altre.
export const LAB_GUARD_EXEMPT_ROUTES = [
  // Billing: devono restare raggiungibili proprio quando il lab NON è operativo
  // (riattivazione abbonamento). Self-check blacklist già presente nei file.
  'app/api/stripe/checkout/route.ts',
  'app/api/stripe/portal/route.ts',
  // Autenticata dalla firma Stripe, non da un utente
  'app/api/stripe/webhook/route.ts',
  // Flussi auth: precedono o costruiscono il contesto lab
  'app/api/auth/accept-invite/route.ts',
  'app/api/auth/webauthn/login/options/route.ts',
  'app/api/auth/webauthn/login/verify/route.ts',
  'app/api/auth/webauthn/register/options/route.ts',
  'app/api/auth/webauthn/register/verify/route.ts',
  // Portale token: guardia propria (blacklist→404 generico) in guardie.ts/route
  'app/api/portale/richiedi/route.ts',
  'app/api/portale/[token]/pin/route.ts',
  'app/api/portale/[token]/fatture/route.ts',
  'app/api/portale/[token]/fatturazione/route.ts',
  'app/api/portale/[token]/situazione/route.ts',
  'app/api/portale/[token]/fatturazione/stampa/route.ts',
  'app/api/portale/[token]/fatturazione/[lavoro_id]/route.ts',
  'app/api/portale/[token]/fatture/[fattura_id]/pdf/route.ts',
  'app/api/portale/[token]/lavori/[lavoro_id]/[documento]/route.ts',
  // Callback interna autenticata da x-internal-secret (constant-time, Task 8)
  'app/api/internal/pec-verify/route.ts',
  // Export GDPR/portabilità: canale in-band ratificato, resta aperto
  'app/api/fatture/export/route.ts',
] as const
```

- [ ] **Step 2: Test statico**

```typescript
// tests/unit/lab-guard-static.test.ts
// Gemello di lab-context-guard.test.ts: anti-drift N13. Ogni route.ts con
// almeno un handler esportato o è in allowlist esenzioni o chiama
// assertLabOperativo(.
import { describe, it, expect } from 'vitest'
import { readFileSync, readdirSync, statSync } from 'node:fs'
import { join, relative } from 'node:path'
import { LAB_GUARD_EXEMPT_ROUTES } from '@/lib/supabase/lab-guard-exempt-routes'

const SRC = join(process.cwd(), 'src')
function walk(dir: string, out: string[] = []): string[] {
  for (const e of readdirSync(dir)) {
    const p = join(dir, e)
    if (statSync(p).isDirectory()) walk(p, out)
    else if (e.endsWith('.ts')) out.push(p)
  }
  return out
}
const rel = (p: string) => relative(SRC, p).replaceAll('\\', '/')
const routes = walk(join(SRC, 'app', 'api')).filter((f) => rel(f).endsWith('route.ts'))

const HANDLER_RE = /export\s+(async\s+)?function\s+(GET|HEAD|POST|PATCH|PUT|DELETE)\b/

describe('guardia statica lab-guard (N13)', () => {
  it('ogni route con handler non esente chiama assertLabOperativo(', () => {
    const violazioni = routes
      .filter((f) => HANDLER_RE.test(readFileSync(f, 'utf8')))
      .map(rel)
      .filter((r) => !LAB_GUARD_EXEMPT_ROUTES.includes(r as never))
      .filter((r) => !readFileSync(join(SRC, r), 'utf8').includes('assertLabOperativo('))
    expect(violazioni).toEqual([])
  })

  it('ogni esenzione punta a un file esistente (niente entry morte)', () => {
    for (const r of LAB_GUARD_EXEMPT_ROUTES) {
      expect(routes.map(rel)).toContain(r)
    }
  })
})
```

- [ ] **Step 3: Run → RED con l'elenco completo delle route da editare** — `npx vitest run tests/unit/lab-guard-static.test.ts`. L'array `violazioni` atteso ≈ 85 file: è la worklist esatta dei Task 4-5.
- [ ] **Step 4: Commit (test RED documentato nel messaggio)** — consentito perché il RED è il driver del rollout: `git add -A && git commit -m "test(security): guardia statica N13 (RED: worklist rollout)"`. In alternativa tenere il commit per fine Task 5 se si preferisce main-verde-sempre nel worktree — a discrezione dell'esecutore, il worktree è isolato.

---

### Task 3: Wrapper — esporre `ruolo` + `lab` (TDD)

**Files:**
- Modify: `src/lib/invito/verify-titolare.ts`
- Modify: `src/lib/rete/verify-admin-rete.ts`
- Test: `tests/unit/lab-guard-wrappers.test.ts`

**Interfaces:**
- Produces: `TitolareContext` e `AdminReteContext` estesi con `ruolo: string` e `lab: { stato: string; trial_ends_at: string | null; nome: string } | null` → soddisfano `LabGuardInput`.
- Consumes: `getFreshLabContext` (invariato).

- [ ] **Step 1: Test che fallisce** — mock di `@/lib/supabase/lab-context` che ritorna un context titolare con `lab`; assert che `verifyTitolare()` e `verifyAdminRete()` propaghino `ruolo` e `lab` identici al context. (Per `verifyAdminRete` mockare anche la query `reti` come nei test esistenti `rete-id-route.test.ts`.)
- [ ] **Step 2: RED** — proprietà assenti.
- [ ] **Step 3: Implementazione**

```typescript
// verify-titolare.ts — diff
export interface TitolareContext {
  userId: string
  laboratorioId: string
  ruolo: string
  lab: { stato: string; trial_ends_at: string | null; nome: string } | null
}

export async function verifyTitolare(): Promise<TitolareContext | null> {
  const context = await getFreshLabContext()
  if (!context || context.ruolo !== 'titolare' || !context.laboratorioId) return null
  return { userId: context.userId, laboratorioId: context.laboratorioId, ruolo: context.ruolo, lab: context.lab }
}
```

`verify-admin-rete.ts`: stesso pattern — aggiungere `ruolo: context.ruolo, lab: context.lab` all'oggetto ritornato e i due campi all'interfaccia `AdminReteContext`.

- [ ] **Step 4: GREEN + `npx tsc --noEmit`**
- [ ] **Step 5: Commit** — `git commit -m "feat(security): verifyTitolare/verifyAdminRete espongono ruolo+lab per lab-guard (N13)"`

---

### Task 4: Rollout guard — GET categoria A (28 file) + route `admin/*` (8 file)

**Files:** i 28 file di `LAB_CONTEXT_ROUTE_ALLOWLIST` (`src/lib/supabase/lab-context-allowlist.ts`) + gli 8 `src/app/api/admin/**/route.ts`.

**Interfaces:** Consumes `assertLabOperativo` (Task 1). Nessuna nuova interfaccia.

Pattern EDIT (identico ovunque, adattare solo il metodo letterale):

```typescript
import { assertLabOperativo } from '@/lib/supabase/lab-guard'
// GET categoria A — subito dopo il check context/laboratorioId esistente:
const guard = assertLabOperativo(context, 'GET')
if (guard) return guard
```

Regole:
1. La chiamata va DOPO l'ottenimento del context e i suoi null-check esistenti (401/403 attuali restano primi), PRIMA di qualsiasi query dati.
2. Nei file con più handler (es. `cicli/route.ts` GET+POST) la chiamata va in OGNI handler, col metodo letterale corrispondente.
3. Nelle route `admin/*` inserire dopo `verifyAdmin()`: `const guard = assertLabOperativo(admin, 'PATCH'); if (guard) return guard` — no-op per admin_sistema (bypass), uniformità anti-drift.
4. In `admin/labs/[id]/hard-delete` il check inline `ruolo==='admin_sistema'` già esiste: guard subito dopo.

- [ ] **Step 1:** Editare i 28 GET categoria A (worklist = `LAB_CONTEXT_ROUTE_ALLOWLIST`).
- [ ] **Step 2:** Editare le 8 route `admin/*`.
- [ ] **Step 3:** `npx vitest run tests/unit/lab-guard-static.test.ts` → violazioni scese di ~36 voci; `npx vitest run` mirato sui test route toccati (`clienti-route`, `cicli-route`, `admin-labs-route`, ecc.) → verdi (default mode shadow ⇒ nessun blocco nei test esistenti).
- [ ] **Step 4: Commit** — `git commit -m "feat(security): lab-guard su GET categoria A e route admin (N13)"`

---

### Task 5: Rollout guard — tutti gli handler mutanti restanti (~57 file) + GET-su-GFC

**Files:** ogni `src/app/api/**/route.ts` residuo nella worklist del test statico (Task 2 Step 3), inclusi: `tecnici/invite*` (via `verifyTitolare` esteso), `rete/*` (via `verifyAdminRete` esteso), i GET-su-GFC elencati nel censimento.

**Interfaces:** Consumes `assertLabOperativo`, wrapper estesi (Task 3).

Stesso pattern del Task 4. Per i wrapper:

```typescript
const tit = await verifyTitolare()
if (!tit) return NextResponse.json({ error: 'Non autorizzato' }, { status: 403 })
const guard = assertLabOperativo(tit, 'POST')
if (guard) return guard
```

- [ ] **Step 1:** Sgranare la worklist del test statico a batch per directory (`lavori/*`, `fatture/*`, `clienti/*`, `impostazioni/*`, `rete/*`, `tecnici/*`, resto). Dopo ogni batch: run del test statico.
- [ ] **Step 2:** Test statico GREEN totale: `npx vitest run tests/unit/lab-guard-static.test.ts`.
- [ ] **Step 3:** Suite completa `npx vitest run` → verde (mode default shadow).
- [ ] **Step 4:** Test comportamentale route-level in enforce (nuovo file `tests/unit/lab-guard-routes-enforce.test.ts`): con `process.env.UA_LAB_GUARD_MODE='enforce'`, (a) `GET /api/clienti` con context blacklist → 403 `UA_LAB_BLACKLIST`; (b) `POST /api/cicli` con context sospeso → 403 `UA_LAB_SOSPESO` PRIMA di qualsiasi query (mock `from` che lancia se chiamato); (c) `POST /api/cicli` con lab trial attivo → passa. Pattern mock identico a `clienti-route.test.ts`.
- [ ] **Step 5: Commit** — `git commit -m "feat(security): lab-guard su tutti gli handler mutanti + GET su context fresh (N13)"`

---

### Task 6: Guardia portale token (TDD)

**Files:**
- Modify: `src/lib/portale/guardie.ts`
- Modify: `src/app/api/portale/[token]/lavori/[lavoro_id]/[documento]/route.ts` (check manuale)
- Modify: `src/app/api/portale/[token]/pin/route.ts` (usa `risolviClientePortale` → copre via guardia comune)
- Modify: `src/app/api/portale/richiedi/route.ts` (check manuale token cliente)
- Test: `tests/unit/portale-lab-guard.test.ts` + aggiornare i mock nei test portale esistenti se il nuovo campo li rompe

**Interfaces:**
- Produces: `ClientePortale` esteso con `lab_stato: string`; `risolviClientePortale` ritorna nuovo esito `{ esito: 'non_disponibile' }` quando il lab è blacklist; `guardieEconomiche` mappa `non_disponibile` → `404 { errore: 'non_trovato' }`.

Comportamento ratificato: **blacklist → 404 generico** (no info-leak a terzi: stessa forma di una risorsa inesistente); sospeso/scaduto → NESSUN cambiamento (read di terzi consentito, incluse le POST del flusso fatturazione/pin che servono l'accesso del dentista ai propri documenti).

- [ ] **Step 1: Test che fallisce** — in `portale-lab-guard.test.ts`: (a) `risolviClientePortale` con lab blacklist → `{ esito: 'non_disponibile' }`; (b) `guardieEconomiche` → `404` body `{ errore: 'non_trovato' }`; (c) lab sospeso → `esito 'ok'` invariato; (d) route documento con lab blacklist → 404.
- [ ] **Step 2: RED**
- [ ] **Step 3: Implementazione** — in `risolviClientePortale`: select diventa `'id, laboratorio_id, studio_nome, portale_token_scade_at, portale_fatturazione_attiva, portale_pin_hash, portale_pin_tentativi, portale_pin_bloccato_fino_a, portale_pin_generation, laboratori(stato)'`; dopo il check scadenza token: `if ((data.laboratori as { stato: string } | null)?.stato === 'blacklist') return { esito: 'non_disponibile' }` (lab embed null → fail-closed: `non_disponibile`); spread esclude `laboratori` e valorizza `lab_stato`. In `guardieEconomiche`: ramo `non_disponibile` → `NextResponse.json({ errore: 'non_trovato' }, { status: 404 })`. Nella route documento e in `portale/richiedi`: aggiungere `laboratori(stato)` alla select esistente e branch 404 esplicito su blacklist/embed-null.
- [ ] **Step 4: GREEN** su `portale-lab-guard.test.ts` + tutti i `portale-*.test.ts` esistenti.
- [ ] **Step 5: Commit** — `git commit -m "feat(security): guardia stato lab nel portale token, blacklist=404 generico (N13)"`

---

### Task 7: Interceptor fetch client PWA (TDD)

**Files:**
- Create: `src/lib/api/lab-guard-client.ts` (logica pura testabile)
- Create: `src/components/layout/LabGuardFetchInterceptor.tsx` (client component, monta la patch)
- Modify: `src/app/(app)/layout.tsx` (mount accanto a `SwRegistration`)
- Test: `tests/unit/lab-guard-client.test.ts`

**Interfaces:**
- Produces: `installLabGuardInterceptor(win: { fetch: typeof fetch; location: { pathname: string; assign(url: string): void } }): () => void` — patcha `win.fetch`, ritorna l'uninstall. `estraiCodiceLabGuard(res: Response): Promise<string | null>` — da un clone della risposta.

Comportamento: solo risposte **same-origin** verso path `/api/` con status `403`; `res.clone().json()` best-effort; se `code` inizia con `UA_LAB_` → `location.assign('/impostazioni/abbonamento')` (il gate del layout (app) poi raffina: blacklist→/blocked, sospeso→/billing…). Anti-loop: nessun redirect se `location.pathname` inizia con `/impostazioni/abbonamento`, `/billing` o `/blocked`. Un solo redirect per pagina (flag modulo). La risposta originale viene comunque ritornata al chiamante.

- [ ] **Step 1: Test che fallisce** — con `win` finto: (a) 403 con `code: 'UA_LAB_SOSPESO'` → `assign('/impostazioni/abbonamento')` chiamato una volta e mai due; (b) 403 senza code / non-JSON → nessun redirect; (c) 200 → nessuna lettura del body clone; (d) pathname `/billing` → nessun redirect; (e) uninstall ripristina il fetch originale.
- [ ] **Step 2: RED**
- [ ] **Step 3: Implementazione** — wrapper attorno a `win.fetch` che inoltra sempre la risposta; ispezione asincrona sul clone solo quando `res.status === 403` e l'URL è same-origin `/api/`. `LabGuardFetchInterceptor` è `'use client'`, `useEffect(() => installLabGuardInterceptor(window), [])` con cleanup, render `null`. Mount in `(app)/layout.tsx` nel JSX radice.
- [ ] **Step 4: GREEN + tsc**
- [ ] **Step 5: Commit** — `git commit -m "feat(security): interceptor fetch PWA per codici UA_LAB_* (N13)"`

---

### Task 8: Accessorie — revoca sessioni su blacklist + timingSafeEqual pec-verify (TDD)

**Files:**
- Modify: `src/app/api/admin/labs/[id]/stato/route.ts`
- Modify: `src/app/api/internal/pec-verify/route.ts`
- Test: `tests/unit/lab-stato-blacklist-revoca.test.ts`, aggiornare/estendere test pec-verify se esistono

**Interfaces:** Consumes `transitionLabStato` (invariato), `getServiceClient`.

Parte A — revoca sessioni (appsec R5): in `stato/route.ts`, DOPO `transitionLabStato` con successo e `stato === 'blacklist'`: lookup `utenti` del lab (`laboratorio_id = id`, `deleted_at IS NULL`) e revoca sessione per ciascuno via API admin GoTrue. Verificare in `node_modules/@supabase/supabase-js` la firma disponibile: se `auth.admin.signOut` accetta solo JWT, usare l'endpoint REST admin `POST {SUPABASE_URL}/auth/v1/admin/users/{userId}/logout` con service key. Best-effort: errori loggati (`console.error('[lab-stato] revoca sessioni blacklist fallita', …)`), la transizione NON fallisce. La guard resta comunque il muro (stato letto fresh a ogni request).

Parte B — constant-time (appsec R7): in `pec-verify/route.ts` sostituire `secret !== process.env.INTERNAL_SECRET` con confronto su digest SHA-256 (normalizza le lunghezze):

```typescript
import { createHash, timingSafeEqual } from 'node:crypto'

function secretValido(candidato: string | null): boolean {
  const atteso = process.env.INTERNAL_SECRET
  if (!candidato || !atteso) return false
  const a = createHash('sha256').update(candidato).digest()
  const b = createHash('sha256').update(atteso).digest()
  return timingSafeEqual(a, b)
}
```

- [ ] **Step 1: Test RED** — (a) transizione a blacklist → chiamata revoca per ogni utente del lab; (b) transizione ad altro stato → nessuna revoca; (c) errore revoca → PATCH risponde comunque `{ success: true }`; (d) pec-verify: secret giusto → 200 path, sbagliato/assente/env-assente → 403.
- [ ] **Step 2: Implementazione → GREEN**
- [ ] **Step 3: Commit** — `git commit -m "fix(security): revoca sessioni su blacklist + confronto constant-time pec-verify (N13)"`

---

### Task 9: FASE 7 — verifica completa nel worktree

- [ ] `npx tsc --noEmit` → 0 errori
- [ ] `npx vitest run` → tutta la suite verde (attesi ~2100+ test, inclusi i nuovi)
- [ ] `npx next build` → build OK
- [ ] Commit di eventuali fix emersi; poi `/code-review` sul diff del branch.

---

## Fuori dal piano (decisioni/azioni Francesco — NON implementare qui)

- Commit del working tree principale (N11-bis + N14).
- Merge del branch N13, deploy, finestra shadow 24-48h su Vercel, flip a `enforce` (commit dedicato che cambia `LAB_GUARD_DEFAULT_MODE`), eventuale env `UA_LAB_GUARD_MODE`.
- Processo GDPR out-of-band in `docs/security/` PRIMA del deploy del blocco-GET-blacklist.
- Collaudo login reale ≤2s + `PERF_BUDGET_LOGIN`.

## Self-review (fatta)

- Copertura spec: matrice completa (Task 1) · scope pieno ~95 edit (Task 4-5, worklist guidata dal test statico) · guardia portale (Task 6) · trial-scaduto (Task 1) · anti-drift (Task 2) · shadow+kill-switch (Task 1) · contratto 403+code+no-store (Task 1) · wrapper (Task 3) · fetch-wrapper client (Task 7) · revoca sessioni + timingSafeEqual (Task 8). Esenzioni ratificate tutte in allowlist (Task 2).
- Tipi coerenti: `LabGuardInput` soddisfatto da `LabContext`, `TitolareContext`, `AdminReteContext` estesi (Task 3); `ClientePortale.lab_stato` (Task 6).
- Nessun placeholder; l'unico punto da verificare sul posto è la firma admin GoTrue (Task 8, istruzione esplicita di verifica in node_modules).
