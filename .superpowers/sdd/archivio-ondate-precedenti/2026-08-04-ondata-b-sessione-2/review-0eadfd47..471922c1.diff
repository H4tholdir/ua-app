# Review package: 0eadfd47..471922c1

## Commits
471922c1 feat(whatsapp): P31 — il prefisso internazionale lo mette UÀ, in un posto solo
56b39ecb docs(p31): corretta ALLA FONTE la regola sbagliata sugli accenti in SQL, e nasce P33

## Files changed
 docs/roadmap/ROADMAP-UFFICIALE.md                  |  1 +
 .../2026-08-03-p31-due-numeri-per-il-cliente.md    | 10 +++++-
 ...6-08-03-p31-due-numeri-per-il-cliente-design.md | 10 +++++-
 src/lib/consegna/whatsapp-template.ts              | 37 ++++++++++++++++++--
 tests/unit/numero-per-whatsapp.test.ts             | 39 ++++++++++++++++++++++
 5 files changed, 92 insertions(+), 5 deletions(-)

## Diff
diff --git a/docs/roadmap/ROADMAP-UFFICIALE.md b/docs/roadmap/ROADMAP-UFFICIALE.md
index 8bbbbb0a..18af0aef 100644
--- a/docs/roadmap/ROADMAP-UFFICIALE.md
+++ b/docs/roadmap/ROADMAP-UFFICIALE.md
@@ -1004,20 +1004,21 @@ nessuna voce qui sta sulla parola di nessuno.
 | 🔴 **P21** | 🆕 **Cancellare un laboratorio FABBRICA, mentre cancella, una copia di tutto ciò che cancella.** Aperta dal panel su **D136**. Le `DELETE` di `admin_delete_laboratorio` **accendono i trigger di sorveglianza**, che scrivono in `audit_log` la riga intera (`old_data = to_jsonb(OLD)`) **prima** che sparisca — e la funzione **non ripulisce mai quel registro**. `provato:` sul catalogo vivo, **15 righe di `audit_log` appartengono a 5 laboratori che non esistono più**; e su `lavori` le righe che portano dentro `paziente_nome_snapshot` — **il nome del paziente** — sono **82 su 82** fra le cancellazioni e **360 su 360** fra le modifiche. 🔑 **L'Art. 28(3)(g) dice «*e cancelli le copie esistenti*»: il registro È una copia esistente.** ⚠️ **E l'elenco di ciò che la cancellazione NON tocca è più lungo:** censite su `pg_attribute` — cioè su **ogni** tabella con una colonna `laboratorio_id`, non sull'elenco di chi ha scritto la funzione — **nove tabelle** restano fuori, fra cui `lavori_immagini_eliminazioni` (21 righe con percorso e autore), `progressivi_anno` (**1 riga già orfana**) e `push_subscriptions` (indirizzo e chiavi di un dispositivo). Più gli **account di autenticazione**: la funzione cancella `public.utenti` ma **nessuna riga di codice cancella `auth.users`** (oggi 7 = 7, ma è come sono andate le prove, non una garanzia). 🔑 **La lista cresce ogni volta che nasce una tabella e nessuno la aggiunge alla funzione** — ed è lo stesso meccanismo che ha già rotto quella funzione in produzione una volta. ➡️ **Condizione L2 del panel: o la cancellazione tocca davvero, o il contratto dichiara per iscritto che cosa sopravvive e perché** | `admin_delete_laboratorio` (catalogo vivo) · `src/app/api/admin/labs/[id]/hard-delete/route.ts:39` |
 | 🔴 **P22** | 🆕 **UÀ entra nei dati di un cliente CON L'IDENTITÀ DEL CLIENTE, e non resta traccia.** Aperta dal panel su **D136**. `src/app/api/admin/labs/[id]/impersonate/route.ts:61-64` genera un collegamento d'accesso monouso **come il titolare del laboratorio**: la sessione che ne esce è **indistinguibile da un accesso vero**, l'evento **non viene registrato**, e ogni azione successiva finisce nel registro delle modifiche con l'`actor_id` **del cliente**. In più `decideLabOperativo` (`src/lib/supabase/lab-guard.ts:50`) dà ad `admin_sistema` un **passaggio libero totale**. 🔑 **Due conseguenze, e la seconda è quella che morde:** ① il contratto non potrà promettere di tracciare gli accessi di UÀ, perché non li traccia; ② **un'accettazione toccata in impersonificazione si registrerebbe come accettazione del titolare** — ed è il primo punto che un avvocato attacca. ➡️ **Condizione L3 del panel** (tracciare, e mai sotto l'identità del cliente) **+ F5** (le sessioni nate da impersonificazione vanno escluse dall'accettazione — e oggi **non esiste un marchio** che permetta di riconoscerle) | `src/app/api/admin/labs/[id]/impersonate/route.ts:61-74` · `src/lib/supabase/lab-guard.ts:50` |
 | 🔴 **P19** | 🆕 **UÀ non ha un contratto sulla protezione dei dati con i laboratori che la usano — e il documento che i laboratori consegnano ai dentisti dice che ce l'ha.** Aperta dal **panel su D128 del 04/08/2026** e **confermata da Francesco**: «*no, oggi non firma niente*». `provato:` nessuna pagina di condizioni o privacy in `src/app` · nessuna colonna di accettazione su `laboratori` (catalogo vivo) · `tipo_controparte` ha **un solo scrittore** (`generate-dpa.ts:288`) che scrive sempre `'dentista'`, mai `'sub_responsabile'` · il registro `data_processing_agreements` **prevede già la controparte giusta** e non l'ha mai vista: `provato:` il vincolo ammette esattamente due valori, `CHECK (tipo_controparte = ANY (ARRAY['dentista','sub_responsabile']))`, e le righe `'sub_responsabile'` sono **zero**. 🔄 **CORRETTA il 04/08, e l'errore era mio:** avevo scritto che la sede era `sub_processors`, «disegnata apposta e mai usata». **Non è quella tabella.** `sub_processors` è **l'elenco dei fornitori di UÀ**, e non è nemmeno vuota: `provato:` **6 righe** già dentro (Supabase, Vercel, Resend, Stripe, AdE/Sogei, Ministero Salute), con `dpa_firmato_at` **NULL su tutte e sei**. È una tabella **popolata e mai LETTA dall'applicazione** — cosa diversa da «vuota», e con un suo difetto: l'elenco dei tre fornitori che il contratto mostra al dentista è **scritto a mano dentro il modello PDF** (`DpaTemplate.tsx:207-209`) invece di venire da lì. 🔑 **Due tabelle, due problemi diversi: confonderle avrebbe mandato il lavoro nel posto sbagliato.** 🛑 **Intanto `DpaTemplate.tsx:210` afferma al dentista:** «*Il Responsabile impone a ciascuno di essi, **per contratto**, obblighi di protezione dei dati equivalenti a quelli del presente accordo (Art. 28(4) GDPR)*». 🔑 **È la stessa classe di difetto per cui è esistita D126** — un documento che esce verso un cliente e afferma cose che il prodotto non fa — **e stavolta il buco non è del laboratorio di Francesco: è di UÀ come servizio.** Ogni laboratorio che si abbona affida a UÀ **dati sanitari di pazienti**, quindi ciascuno ha bisogno del **proprio** accordo con UÀ; senza, il DPA che *quel* laboratorio consegna ai *suoi* dentisti ripete la stessa affermazione non vera. ⚠️ **E l'Art. 28(3) impone quell'accordo a ENTRAMBE le parti** (EDPB §103: «*Both the controller and processor are responsible for ensuring that there is a contract*»): la mancanza è una violazione **anche di UÀ**, non solo del laboratorio. ✅ **La catena a valle è invece a posto:** `verificato alla fonte il 04/08` — i DPA di **Supabase** (v1, 01/08/2026), **Vercel** (in vigore 31/03/2026) e **Resend** (agg. 31/12/2025) si perfezionano **automaticamente** con l'accettazione delle condizioni, nessuna casella da spuntare (⚠️ per Vercel resta da guardare **quale piano** è in uso). ✍️ **D136 (04/08): C1 CAMBIA NATURA — non è più «riscrivere la frase», è COSTRUIRE L'ANELLO.** Francesco, guardando come si perfezionano i DPA dei fornitori, ha chiesto «*non possiamo fare la stessa cosa con UÀ?*» e poi ratificato: **UÀ si dà Condizioni di Servizio col DPA incorporato, accettate all'abbonamento**. 🔑 **Il meccanismo è quello vero:** i fornitori non hanno caselle da spuntare **perché il contratto sui dati viaggia dentro le loro condizioni** — la firma è quella delle condizioni. **UÀ può farlo solo perché… oggi non ha condizioni.** Servono **TRE pezzi**: ① il **testo**, documento nuovo **coi ruoli rovesciati** (qui il laboratorio è **titolare** e UÀ **responsabile**) — ⚠️ dati sanitari, **serve un occhio legale**; ② **il momento in cui si accetta**, che non esiste; ③ **la traccia**, ✅ **e questa esiste già** (la macchina dell'ondata 1 su una controparte diversa). ⏱️ **PERCHÉ ADESSO:** `provato:` **3 laboratori, tutti di prova** — **non c'è nessuno da rincorrere**. Farlo prima del primo laboratorio vero costa solo scriverlo; dopo, significa tornare da clienti acquisiti a chiedere una firma. 🛑 **Panel lanciato nello stesso turno** (3 advisor a mandato disgiunto: forma del click-wrap e artt. 1341-1342 c.c. · contenuto e ciò che UÀ NON deve promettere · dove si attacca nel prodotto). ➡️ **Questa voce ASSORBE C1, e blocca l'inizio dell'ondata 2** (D135 (b): le correzioni di testo vanno prima della prima accettazione). 🔀 **SCOMPOSTA in TRE sottoprogetti il 04/08/2026** — «chiudere P19» voleva dire fare tre cose diverse, con rischi e tempi diversissimi: **P19-a il TESTO** (spec scritta: `docs/superpowers/specs/2026-08-04-p19-a-testo-condizioni-e-dpa-design.md`) · **P19-b il MOMENTO e la TRACCIA** (accettazione dentro la transazione atomica dell'invito, tabella nuova, blocco per chi è già dentro) · **P19-c il SECONDO PASSAGGIO** (firma leggera per le sole clausole che proteggono UÀ). 🛑 **E una PRECONDIZIONE che il panel non aveva visto, trovata alla prima domanda della spec: UÀ NON ESISTE COME SOGGETTO GIURIDICO** (**D140** — «*non esiste ancora, la devo aprire*»). Il panel l'aveva letta come la condizione **T6**, «mettere i dati identificativi di UÀ»: **non è una riga da aggiungere, è un soggetto da costituire**, e sta **prima di L1·L2·L3** perché senza controparte non c'è contratto. ✍️ **Quattro decisioni sul testo:** **D140** (spazio dichiarato per i dati della parte, mai finto) · **D141** (si promette **il minimo corretto**: nessuna disponibilità garantita, nessun rimborso, nessun tempo di assistenza — sarebbero promesse non sostenibili su un piano gratuito) · **D142** (si promette la **cancellazione piena** e la si costruisce **dopo**, 🛑 **ma il blocco dev'essere MECCANICO**, o torna ad essere D126) · **D143** (il testo vive **nel repository** come file versionato, non in banca dati). Referto: `docs/roadmap/2026-08-04-panel-d128-referto.md` §4-bis ① | `src/components/features/pdf/DpaTemplate.tsx:210` · **destinazione giusta:** `data_processing_agreements` con `tipo_controparte = 'sub_responsabile'` |
 | ✅ **P17** | 🚀 **FATTA E IN PRODUZIONE dal 02/08/2026** (`main` = `bea6fe53`, merge di 19 salvataggi, CI e CD verdi; verificata su `uachelab.com` — v. la testa della roadmap). Percorso completo: §0B (ricerca → mockup → 12 scatti → approvazione, **D161**) → piano → **5 esecutori freschi** (R-E1, **17 difetti del piano trovati**) → **collaudo dal vivo** → **FASE 9b**. ✅ **Il tasto è vivo:** otto stati, tutti visti, tutti con parole d'italiano e — dove ha senso — una via d'uscita; **vuoto** e **guasto di lettura** non si somigliano più. ✅ **Il nome del file è salvo:** `provato:` scaricato davvero → **`DPA-2026-0001.pdf`**, non il ripiego. ✅ **Chi non è titolare non vede il tasto** (`provato:` con la sessione vera di un `tecnico`: 0 comandi nella card) **ma vede il resto** (D158 · D160). 📄 Referto del gate: `docs/design/audit-ui-ux/LIVELLO-2-2026-08-02-p17-scarico-dpa-ESITI.md` — **3 ❌ trovati, 3 risolti** (due corretti nel gate, uno deciso: **D167**), **3 ⚠️ deferiti col motivo**, **3 ritrovamenti fuori mandato riferiti**. ⚠️ **Resta deferito, e va detto:** in modo scuro la riga «Ultima emissione» e la frase di conservazione **restano illeggibili** — è **P16**, **D134**, non riaperta qui; e a 1280 il riquadro d'avviso è una fascia larga **1240 px**, difetto **di pagina** (nessuna card di `clienti/[id]` ha un `max-width`), sede l'ondata v3 della route. <br><br>~~🆕 **Quando lo scarico del contratto fallisce, il titolare finisce su una pagina di codice — e non ha un modo per tornare indietro.** Aperta dal **GATE ESTETICO L2 del 04/08/2026** (§9 della checklist). Il tasto «Scarica DPA PDF» è un `<a href>` **nudo**: la pressione è una **navigazione**, non una richiesta con esito gestito. `provato:` navigando con la sessione vera a `/api/clienti/<id inesistente>/dpa` → **HTTP 404**, `content-type: application/json`, e quello che si vede a schermo è letteralmente `{"error":"Cliente non trovato"}` — **titolo vuoto**, **zero elementi interattivi**. In una PWA installata il tasto «indietro» del browser può non esserci affatto. 🛑 **Non è un caso di laboratorio:** la rotta ha **quattro** cammini che non sono guasti del servizio (`route.ts:66-69` — due per la Partita IVA mancante, due per «non trovato») e **sette** che lo sono; il più probabile in campo è il **422 dei dati fiscali incompleti**, che un laboratorio senza Partita IVA incontra **al primo tentativo**. ⚠️ **E la stessa sezione ha una seconda metà, della stessa famiglia:** se il registro non è leggibile, `page.tsx:169-171` scrive nel log e **non rende la riga** — cioè uno **studio mai emesso** e un **guasto di lettura** si vedono **identici**. 🔑 **Nessuna delle due si corregge dentro un gate estetico:** la cura non è un colore, è cambiare **come il tasto scarica** (da navigazione a richiesta con esito), cioè trasformare un elemento server in un componente client con i suoi stati — un cambiamento di comportamento su una superficie **in produzione**, che va progettato (quale messaggio, dove, cosa si può fare dopo). **Sede naturale: l'ondata 2**, che rimette comunque mano a questi stati per la firma~~ 🔄 **il testo barrato è la voce COME FU APERTA dal gate del 04/08 — si conserva perché dice che cosa è stato riparato.** | `src/app/api/clienti/[id]/dpa/route.ts` · `src/app/(app)/clienti/[id]/page.tsx:367-435` · 🆕 `src/components/features/clienti/ScaricaDpaButton.tsx` · 🆕 `src/components/feedback/BloccoAvviso.tsx` · 🆕 `src/components/feedback/BloccoAvvisoRicarica.tsx` · 🆕 `src/lib/pdf/permessi-dpa.ts` |
 | 🟠 **P18** | ✅ **RIPARATA il 02/08/2026, notte D168 (D173). Su ramo `p18-il-collegamento-che-diverge`, NON pubblicata (D169).** ✅ **Una riga**: `window.location.origin` → `process.env.NEXT_PUBLIC_APP_URL ?? 'https://uachelab.com'`, cioè **lo stesso modo degli altri sette punti** che in questo progetto costruiscono un indirizzo assoluto. 🔑 **E il censimento ha trovato una cosa che la voce non diceva: uno di quei sette fa ESATTAMENTE questo link.** `src/lib/consegna/whatsapp-template.ts:22` costruisce `${APP_URL}/portale/${token}` — quindi **lo stesso collegamento, mandato per WhatsApp o copiato col bottone, poteva essere DIVERSO**. Non era solo idratazione: erano due indirizzi per la stessa cosa. 🛡️ **5 prove nuove** che non guardano l'avvertimento di React — in ambiente di prova `window` esiste sempre, quindi il ramo «server» non si percorrerebbe mai e la prova sarebbe **verde per finta** — ma guardano il **comportamento**: si finge che il laboratorio navighi da `192.168.1.5:3000` o da un'anteprima di rilascio, e si pretende che il link **non cambi**. `provato:` sul codice di prima, **5 su 5 rosse**. 🛑 **Vuoto dichiarato:** la riparazione **non è stata guardata nel browser** — nessuno ha riaperto la scheda cliente per vedere sparire l'avvertimento di idratazione. Le prove coprono il comportamento, non l'occhio, e ieri il gate estetico ha trovato tre difetti su un codice così. **Testo originale della voce:** | ✅ `src/components/features/clienti/PortaleLinkButtons.tsx` · `tests/unit/PortaleLinkButtons-indirizzo.test.tsx` |
 | 🟠 **P18** *(testo originale)* | 🆕 **La scheda cliente ha un disallineamento di idratazione, e ciò che diverge è il collegamento che il laboratorio manda al dentista.** Trovata **fuori mandato** durante il gate L2 del 04/08/2026 e **riferita, non corretta** (R-E2). `PortaleLinkButtons.tsx:134-137` calcola l'indirizzo di base come `typeof window !== 'undefined' ? window.location.origin : 'https://uachelab.com'`: il server stampa **sempre** `uachelab.com`, il client stampa l'origine **vera**. `provato:` in locale React solleva «*Hydration failed because the server rendered text didn't match the client*» e **rigenera il sottoalbero** (è la voce «1 Issue» dell'indicatore di sviluppo). ✅ **In produzione oggi NON morde** — le due origini coincidono. 🛑 **Ma morde su qualsiasi altra origine:** un rilascio di anteprima Vercel, un dominio di prova, un accesso dall'indirizzo di rete locale. E il testo che diverge è **il link che il laboratorio condivide con lo studio**: per un istante è uno, dopo l'idratazione è un altro. 🔑 **Difetto LATENTE, non difetto del locale:** il locale è solo il posto dove si vede | `src/components/features/clienti/PortaleLinkButtons.tsx:134-137` |
 | 🔴 **P24** | 🆕 **IL PIANO VERCEL IN USO VIETA L'USO COMMERCIALE — quindi UÀ, così com'è, NON SI PUÒ DISTRIBUIRE.** Aperta il 04/08/2026, e nasce da una risposta di Francesco: «*i piani di vercel e supabase sono quelli gratis*» — che **chiude una riserva aperta da due sessioni e ne apre una peggiore**. `verificato alla fonte` (`vercel.com/docs/limits/fair-use-guidelines`, agg. 16/06/2026): «***Hobby teams** are restricted to non-commercial personal use only. All commercial usage of the platform requires either a **Pro or Enterprise** plan*». 🛑 **E la loro definizione di «commerciale» ci comprende in pieno**, alla lettera: «*any method of requesting or processing **payment** from visitors of the site*» — UÀ ha gli abbonamenti **Stripe già in produzione** (`ua-app/CLAUDE.md` §7: Lab €49, Rete PRO €149). 🔑 **Non è una questione di limiti di traffico: è il contratto d'uso.** ✅ **RAFFORZATA dal controllo incrociato del 04/08:** il divieto **non sta solo** nelle regole d'uso, sta **nei Termini di servizio stessi**, §4: «*You shall only use the Services under a **Hobby** plan for your **personal or non-commercial use**.*» Due documenti diversi, stessa frase.
 🔄 **CORRETTO dallo stesso controllo — la prima stesura di questa voce diceva una cosa che NON regge.** Aveva scritto: «*l'automatismo del DPA è riferito a Enterprise e Pro, quindi su Hobby UÀ non ha un accordo sui dati con chi la ospita*». 🛑 **I Termini di servizio dicono il contrario, o almeno non lo dicono:** §10.1 stabilisce che Vercel tratta i dati «*in accordance with Vercel's **Data Processing Addendum***» **senza distinguere il piano**. ⚠️ **Quindi c'è una TENSIONE fra due documenti dello stesso fornitore** — il DPA (letto il 04/08) riferisce l'applicazione automatica ai clienti **Enterprise e Pro**; i Termini la enunciano **in generale** — **e va lasciata dichiarata, non risolta a favore della lettura che ci conviene o di quella che ci spaventa**. 🔑 **Sull'esito pratico non cambia nulla:** il piano va comunque cambiato per il divieto commerciale, e cambiandolo la tensione si estingue. Ma la voce **non può affermare** che UÀ oggi è senza accordo sui dati: **non è provato**. ⚠️ Il DPA che i laboratori consegnano ai dentisti nomina comunque Vercel fra i sub-responsabili (`DpaTemplate.tsx:208`). ✅ **Oggi il danno è ZERO** (nessun cliente vero, nessun incasso) 🛑 **ma è un blocco DURO della distribuzione, non un miglioramento:** è **FASE 2**, e va risolto **prima** del primo laboratorio pagante, insieme al piano Supabase (**P20**) e alla costituzione del soggetto giuridico (**D140**). ⚠️ **Non verificato:** se il piano Hobby sia intestato a un account personale o a un team, e se Vercel abbia mai contestato l'uso — irrilevante ai fini della voce: il testo è chiaro | `vercel.com/docs/limits/fair-use-guidelines` · pannello Vercel — **l'acquisto lo fa Francesco** |
 | 🟠 **P23** | ✅ **RIPARATA il 02/08/2026, notte D168 (D172). Su ramo `p23-il-salvataggio-che-si-ferma-a-mille`, NON pubblicata (D169).** ⚠️ **PRECONDIZIONE ALL'UNIONE, e non è facoltativa:** appena questo ramo entra in `main`, **prima cosa** `bash scripts/installa-salvataggio-programmato.sh` — altrimenti la copia che gira ogni notte resta quella vecchia e continua a fermarsi a 1000. ✅ **Tre pezzi, e i primi due da soli non bastavano:** ① `elenca()` ora **scorre le pagine** finché ne torna una corta · ② i file **elencati** e quelli **scaricati** devono combaciare, o si esce con errore — senza questo la correzione di ① era **inverificabile in esercizio** (elencare 3000 e scaricarne 2000 diceva «riuscito») · ③ 🔑 **`salvataggio-database.sh` ora GUARDA l'esito** dello scarico: quel file **non ha `set -e`**, quindi fino a oggi un archivio fallito veniva **inghiottito** e la riga dopo stampava «✅ salvataggio completo» — l'allarme sulla Scrivania **non sarebbe mai scattato** per un archivio incompleto. Senza ③, ① e ② erano **inerti**. ⚠️ **E l'ordine dentro ③ è una scelta misurata, non estetica:** il `chmod` che protegge nomi di pazienti e anamnesi sta **prima** dell'uscita in errore — uscire subito avrebbe trasformato una copia incompleta in una copia **esposta**. `provato:` la catena lascia i permessi a `-rw-------`, la copia del database sul disco, ed esce **1**. 🛡️ **9 prove nuove** che lanciano **lo script vero come processo** contro un archivio finto (1200 oggetti · il caso limite di 1000 esatti · un file che non si scarica): `provato:` rimettendo il difetto **5 prove su 7 si accendono**; le 2 che restano verdi misurano proprietà che quel difetto non rompeva. 🔴 **E LA REVISIONE (FASE 8, fatta a lavoro già salvato e verde) HA TROVATO DUE DIFETTI IN QUESTA STESSA CORREZIONE — entrambi della forma che P23 esiste per chiudere.** ① La condizione d'arresto era «mi fermo se ne tornano meno di quanti ne ho chiesti», che **presuppone** che l'archivio dia sempre il massimo quando ce ne sono altri: normale in un'API a pagine, **mai garantito**, e `provato:` **non verificabile qui** (la cartella più piena ha 20 file — una prova sotto il limite non dice niente su cosa succede sopra). Ora ci si ferma **solo sulla pagina vuota**: costa una richiesta per cartella e non presuppone niente. ② 🔑 **E la condizione da sola NON BASTAVA:** lo scarto avanzava di **1000 fisso** invece che di quanti ne erano arrivati — con un archivio che ne dà 300 per volta si saltava da 0 a 1000, **perdendone 600 in silenzio**. Trovato da una prova nuova («l'archivio avaro»), scritta apposta per il caso che la prima stesura dava per impossibile. ⚠️ **Il difetto era nel codice che riparava il difetto**, ed è la ragione per cui la revisione non è una formalità. 📌 **Lo script non è stato spezzato per renderlo importabile:** gira da `~/Library` senza librerie accanto, e toccarne l'avvio è il rischio peggiore — un salvataggio che non parte non salva niente **e non lo dice**. L'unica aggiunta è che le credenziali si leggano dall'**ambiente** quando c'è. **Testo originale della voce:** | ✅ `scripts/salvataggio-archivio.mjs` · `scripts/salvataggio-database.sh` · `tests/unit/salvataggio-archivio-paginazione.test.ts` |
 | 🟡 **P23-bis** | 🆕 **La guardia del salvataggio confronta la copia installata con il file che hai SOTTO MANO, non con quello pubblicato — quindi su un ramo di lavoro dà rosso senza che ci sia nessuna deriva.** Trovata il 02/08/2026 lavorando a P23 e **riferita, non corretta** (R-E2 — e correggere una guardia per sbloccare il proprio salvataggio è, di per sé, la mossa sbagliata). `provato:` `guardia-salvataggio-installato.mjs:75` confronta l'impronta di `scripts/<f>` **nel working tree** con quella registrata in `ORIGINE.txt`. Su un ramo non ancora unito la copia installata corrisponde a **`main`**, cioè allo stato approvato: **la deriva non esiste**, ma la guardia ferma il commit. ➡️ **Conseguenza pratica pagata stanotte:** P23 è stato salvato con `--no-verify`, dopo aver fatto girare **tutte le altre guardie** sull'albero esatto del salvataggio. ⚠️ **E il difetto ha una seconda faccia, peggiore:** al momento dell'**unione** la deriva diventa **vera**, e **niente rilancia l'installatore** — la guardia se ne accorgerebbe al commit successivo, ma solo se qualcuno non la scavalca di nuovo. ➡️ Due strade, nessuna scelta: ① confrontare con `git show main:scripts/<f>` invece che col working tree · ② lasciarla com'è e agganciare l'installatore a un passaggio dell'unione | `scripts/guardia-salvataggio-installato.mjs:75` |
+| 🟠 **P33** | 🆕 **LA VECCHIA DERIVA DI DATE HA COLPITO UNA SECONDA VOLTA, e stavolta blocca le MIGRATION.** Trovata il **03/08/2026** dall'esecutore del compito 1 di P31, che si è **fermato invece di inventare** — ed è il motivo per cui si è vista. 🔑 **Il meccanismo:** una migration si chiama `<timestamp>_nome.sql`, e `supabase db push` confronta l'elenco locale col registro remoto **rifiutandosi se trova un buco SOTTO la versione più alta applicata**. L'ultima migration registrata si chiama **`20260804120000`** ma è stata scritta il **2 agosto** (deriva di +2 giorni, **D155**). ⚠️ **Conseguenza:** fino a quando l'orologio non supera il **4 agosto 2026 alle 12:00**, **ogni** migration scritta con la data vera (D155) nasce *sotto* l'ultima registrata e `db push` la **rifiuta**, chiedendo `--include-all` — che toccherebbe ~25 migration fondative mai registrate. `provato:` `npx supabase migration list --linked` → l'ultima è `20260804120000`, e `20260803113525` (quella di P31) ha `remote: ""`. ✅ **La strada c'è ed è già ratificata: D151** — Management API (`read_only:false`) + `npx supabase migration repair --status applied <versione>`. Lo stato dello schema è **identico**; la riga «fuori ordine» nel registro è **cosmetica** (il registro è un insieme con chiave la versione, non un diario ordinato). 🛑 **Quello che NON si fa: rinominare la migration con una data finta** per farla cadere in ordine — non comprerebbe niente e **riaprirebbe la deriva che D155 ha chiuso**. ➡️ **Due strade, nessuna scelta:** ① **non fare niente** — la finestra si chiude da sola il 4 agosto alle 12:00, e fino ad allora vale D151 (**costo: zero, ma ogni esecutore ci sbatte e perde tempo a diagnosticarlo**) · ② **rinominare la sola migration sbagliata** (`20260804120000` → `20260802120000`, la sua data vera) **e riparare la riga nel registro** — chiude la finestra subito e rende il registro onesto, ma tocca un file già applicato in produzione. 🔑 **È la SECONDA volta che quella deriva costa qualcosa di concreto** (la prima erano le date di verifica nei documenti): un errore chiuso alla fonte ha lasciato conseguenze che si scoprono una per una | `supabase/migrations/20260804120000_*.sql` · **D151** · **D155** |
 | 🟠 **P32** | 🆕 **LA GUARDIA DEI DOCUMENTI PERDE COPERTURA OGNI VOLTA CHE QUALCUNO SCRIVE UN HANDOFF NUOVO — e non lo dice.** Trovata il **03/08/2026** dopo l'unione, **causandola**: aggiornando `SESSION_ACTIVE.md` il punto di ripresa è finito su `memory/MEMORY.md`, e `guardia-coerenza-documenti.mjs` è passata da «**7 documenti vivi controllati**» a «**1**», **restando verde**. `misurato:` la sequenza intera è **7 → 1 → 4 → 7**. 🔑 **Due difetti distinti, e il secondo è quello che resta.** ① **Puntare la porta d'ingresso su un ARCHIVIO svuota la catena**: `MEMORY.md` e `ROADMAP-UFFICIALE.md` sono esclusi apposta (righe 179-189 dello script, con la ragione scritta), quindi la guardia non trova più niente da seguire — e non avendo trovato errori dice ✅. **Chiuso**, e la ragione è annotata in `SESSION_ACTIVE.md` perché non si ripeta. ② 🛑 **QUESTO È IL DIFETTO VERO, ed è di progetto: `SALTI` è fisso a 2.** Un documento nuovo in cima alla catena **costa un anello a tutti quelli sotto** — il referto del 03/08 si è messo fra la porta e il referto della notte, e i tre documenti in fondo (verbale delle decisioni, domande, piani) sono scivolati al terzo salto e **hanno smesso di essere controllati, in silenzio**. Rimediato **a mano**, citandoli direttamente dal documento nuovo, ma **la prossima sessione che scrive un handoff rifarà lo stesso buco senza saperlo.** ⚠️ **In tutti e quattro gli stati (7·1·4·7) il colore era VERDE: si vede solo guardando il NUMERO** — cioè esattamente P15 in un altro vestito (*una squadra senza giocatori esce verde*). ➡️ Tre strade, nessuna scelta: ① **seguire la catena fino in fondo** invece che per due salti (rischio: torna il grido al lupo delle 99 segnalazioni che ha fatto nascere il limite) · ② **la guardia dichiara il numero atteso** e si accende se scende (una riga in `SESSION_ACTIVE.md`, sul modello del conteggio delle decisioni che già controlla) · ③ **la porta cita sempre anche i documenti di secondo livello**, e la guardia verifica che lo faccia. 🔑 **La ② è l'unica che protegge dal caso generale**, perché non chiede a nessuno di ricordarsi di niente — ed è la stessa forma della correzione già fatta ai numeri a parole (righe 76-90: «*il difetto non era che l'elenco fosse corto, era che la sua lunghezza fosse una SCELTA*») | `scripts/guardia-coerenza-documenti.mjs` (`SALTI`, righe 62-73 e 169-198) · `memory/SESSION_ACTIVE.md` |
 | 🟠 **P23** *(testo originale)* | 🆕 **Il salvataggio dei FILE si ferma a 1000 per cartella, e non lo dice.** Aperta il 04/08/2026 mentre si lavorava a **D139**, e **riferita invece che corretta** (R-E2: sta fuori dal mandato di quella decisione). `elenca()` in `scripts/salvataggio-archivio.mjs` chiede `limit: 1000, offset: 0` e **non scorre le pagine**: in una cartella con più di 1000 oggetti gli altri **non vengono elencati**, il conteggio finale resta > 0 quindi **nessun controllo si accende**, e il salvataggio si dichiara **RIUSCITO**. 🔑 **È esattamente la forma del guaio del 04/08** — una copia che sembra completa e non lo è — e le **foto cliniche** i 1000 li raggiungono: oggi la cartella più piena ne ha **20**, quindi il difetto **non morde ancora**, ma morderà senza avvisare. ⚠️ **Il difetto è EREDITATO** dalla versione precedente (`salvataggio-archivio·ts`), non introdotto da D139: la riscrittura ha copiato la stessa chiamata, ed è `provato equivalente` **fino a 31 file** — la prova non dice nulla sopra i 1000. ➡️ **La correzione è breve** (scorrere le pagine finché ne torna una corta) **ma vuole la sua prova**, e la prova vera è un ripristino con più di 1000 file | `scripts/salvataggio-archivio.mjs` (`elenca`, `limit: 1000`) |
 | 🟠 **P29** | 🆕 **UNA PAROLA, DUE COLORI: `amber` vale `#F59E0B` nei token e `#FD7E14` nel foglio di stile — e chi legge i token scrive il nome sbagliato.** Aperta il 02/08/2026 dall'esecutore del **Task 2 di P17**, **fuori mandato** e **riferita, non corretta** (R-E2). `provato:` `src/design-system/tokens.ts:39` → `amber: '#F59E0B'` · `src/app/globals.css:91` → `--amber: #FD7E14` (arancio, «*Warning MDR*») · e `#F59E0B` esiste sì, ma si chiama **`--c-amber`** (`globals.css:97`). 🔑 **La trappola non è la doppia definizione: è che il nome CORRISPONDE.** Chi apre i token — che le istruzioni dichiarano «**FONTE DI VERITÀ**, importa da qui» — legge `amber` e scrive `var(--amber)` in perfetta buona fede, ottenendo **un altro colore**. ⚠️ **Ha già morso una volta**, nello stesso giorno in cui è stata trovata: il piano di P17 scriveva `var(--amber, #F59E0B)`, cioè un nome e un ripiego **che non concordano fra loro** — la forma peggiore, perché in locale il ripiego non si vede mai e il difetto appare **solo** dove la variabile è definita. Preso campionando il pixel dello **scatto approvato** → `(245, 158, 11)`. ✅ **Su P17 è corretto** (usa `--c-amber`), 🛑 **ma la trappola resta armata per la prossima superficie.** ⚠️ **Raggio da censire prima di toccare qualsiasi cosa** — `--amber` è usato anche come **stato MDR** (`page.tsx:258` della scheda cliente lo usa per «Non soggetto a fattura elettronica»): rinominare non è cosmesi, e i due usi vanno separati **prima** di decidere. ➡️ **Tre strade, nessuna scelta:** ① rinominare la voce dei token in `cAmber` così il nome torna a corrispondere · ② allineare i due valori (**cambia la grafica** di ciò che oggi usa `--amber`) · ③ lasciare e scrivere l'avvertenza **dentro** `tokens.ts:39`, dove la legge chi sta per sbagliare. 📌 **Precedente identico in casa:** `globals.css:101` porta già un'avvertenza scritta a mano («*MAI usare `--c-amber` come colore testo*») — segno che questa famiglia di nomi ha già fatto inciampare qualcuno | `src/design-system/tokens.ts:39` · `src/app/globals.css:91,97` |
 | 🔴 **P31** | 🔨 **SPEC RATIFICATA il 03/08/2026 (D181-D182-D183) — `docs/superpowers/specs/2026-08-03-p31-due-numeri-per-il-cliente-design.md`. Il piano non è ancora scritto.** ✅ **D181** due campi: `telefono` = telefono dello studio · **`cellulare_whatsapp`** nuovo · **D182** il prefisso `39` lo mette UÀ, non l'utente · **D183** se il cellulare manca, il tasto lo chiede e lo **salva prima** di aprire WhatsApp. 🔑 **Tre misure prese prima di chiedere hanno cambiato il perimetro:** ① **il travaso NON esiste** — `provato:` **39 clienti, UNO SOLO** ha il telefono e **ZERO** hanno l'email, quindi la domanda «i numeri già inseriti sono fissi o cellulari?» **si dissolve** ② **le due schermate che scrivono quel campo si contraddicono già nelle etichette** — `ClienteEditSheet` dice «Telefono» ed esemplifica un **fisso col prefisso**, `NuovoDentistaSheet` dice «Cellulare/WhatsApp» ed esemplifica un **cellulare senza prefisso** ③ **nessuno aggiunge il prefisso internazionale**, quindi la colonna nuova **da sola ereditava il difetto** — la normalizzazione entra nel perimetro. 🛑 **Due vincoli scoperti in validazione:** le allowlist sono **DUE** (`PATCHABLE_FIELDS_CLIENTE` **e** `CAMPI_ELENCO`, che governa due volte) · **`ConsegnaResult` non porta l'id del cliente** (`src/types/domain.ts:633-647`), quindi D183 richiede di aggiungerlo (additivo). 🔎 **Fuori perimetro, riferito (R-E2):** `NuovoOrdineSheet.tsx:193` ha lo stesso difetto del prefisso sul telefono del **fornitore** (0 fornitori in banca dati). **Testo originale della voce:** |
 | ~~🔴 P31 (testo originale)~~ | 🆕 **IL CLIENTE HA UN SOLO NUMERO DI TELEFONO, E SERVE A DUE COSE CHE NON POSSONO ESSERE LO STESSO NUMERO.** Aperta il **03/08/2026 da Francesco**, guardando i disegni di P30: «*il campo del telefono dello studio deve essere differente da quello del cellulare con il numero whatsapp attivo per le comunicazioni dirette*». `provato:` `clienti` ha **un solo** campo `telefono`, e il commento nello schema dice testualmente «*Usato per WhatsApp*» (`supabase/schema.sql`); il pannello di inserimento ha **un solo** campo telefono (`grep type="tel"` → **1**); e `src/lib/consegna/orchestrate.ts:117` legge **quel** campo per mandare il messaggio di consegna. 🛑 **Il difetto è vivo oggi, e non ha una via d'uscita:** se ci metti il fisso della segreteria **il messaggio di consegna non arriva a nessuno**; se ci metti il cellulare, **sui documenti compare un numero che non è quello dello studio**. 🔑 **Nessuno dei due usi è sbagliato: sono due dati diversi con lo stesso nome.** ➡️ Serve una **colonna nuova** — quindi tocca la banca dati (migration + `gen types` + FASE 6b), e va deciso il travaso di ciò che c'è (i numeri già inseriti sono fissi o cellulari? non è distinguibile a occhio). ⚠️ **Precede P30:** un campo che si sdoppia cambia il disegno della schermata. 🔎 **CENSIMENTO DEL 03/08/2026 (dopo l'unione in `main`): il difetto è PIÙ LARGO di come questa voce lo descriveva, e la voce era stata scritta il giorno prima.** Questa riga citava **un** punto che manda WhatsApp col telefono del cliente; ce ne sono **TRE**: `provato:` `src/lib/consegna/orchestrate.ts:123` (messaggio di **consegna** — ed è **123**, non 117: la riga si è spostata con l'unione) · `src/components/features/scadenzario/EstrattoContoView.tsx:223-224` (**sollecito** dell'estratto conto, via `buildWhatsappUrl`) · `src/components/features/scadenzario/ScadenzarioList.tsx:85` (**sollecito** dall'elenco). E **TRE** punti lo usano nel senso opposto, «numero da chiamare»: `src/components/features/scadenzario/ClienteInfoCard.tsx:54` (ci costruisce sopra un `href="tel:"` — cioè il gesto «chiama lo studio») · `src/app/(app)/clienti/[id]/page.tsx:294` (riga «Telefono» della scheda) · `src/components/features/clienti/ClientiSearchList.tsx:243-252` (elenco). 🔑 **Cioè lo stesso campo è già oggi in aperta contraddizione in SEI posti** — più la vista SQL `cliente_telefono` (`supabase/schema.sql:2405`) e i due punti che la leggono (`contabilita/queries.ts:38` · `dashboard/queries.ts:157`). 🛑 **E i sei erano ancora un elenco incompleto: quelli erano i LETTORI. Chi SCRIVE sono altri quattro** (`provato:` 03/08) — `src/app/api/clienti/route.ts:125` (creazione) · `src/components/features/wizard/NuovoDentistaSheet.tsx:74` (creazione dal wizard) · `src/components/features/clienti/ClienteEditSheet.tsx:132` (modifica) · e soprattutto **`PATCHABLE_FIELDS_CLIENTE`** (`src/app/api/clienti/[id]/route.ts:16-23`), che oggi elenca `'telefono'`. ⚠️ **Quest'ultimo è il punto in cui P31 può fallire IN SILENZIO:** se nasce la colonna nuova e nessuno la aggiunge a quell'elenco, la rotta **scarta la chiave senza errore** e l'utente legge «Salvato» su un dato che non c'è — è la forma esatta descritta in `CLAUDE.md` §0C sotto **R-P6**, con l'esempio gemello di `lavori`. 📌 **Censimento totale: 13 punti** (4 scrittori · 6 lettori · 1 vista SQL · 2 lettori della vista). 🔑 **Ed è la lezione ⑤ una QUINTA volta, dentro la correzione della quarta:** il primo censimento aveva guardato solo chi legge, perché la domanda era «dove si usa» e non «chi lo tocca». ⚠️ **È la lezione ⑤ della notte per la quarta volta** («un elenco completo non lo è»), e stavolta l'elenco incompleto era in una voce di roadmap scritta **il giorno prima** — cioè il periodo in cui un elenco sembra più affidabile | `supabase/schema.sql` (`clienti.telefono`, riga 372) · `orchestrate.ts:123` · `EstrattoContoView.tsx:223` · `ScadenzarioList.tsx:85` · `ClienteInfoCard.tsx:54` · `ClienteEditSheet.tsx` |
 | 🟠 **P30-a** | 🆕 **L'ANAGRAFICA DEL CLIENTE NON È MAI STATA PROGETTATA: è quello che c'era.** Aperta il **03/08/2026 da Francesco** — «*il numero di campi com'è stato deciso? un'anagrafica sia pazienti che clienti è completa in questo modo, in base alla filosofia di UÀ?*». 🔑 **La risposta onesta è che non l'ha deciso nessuno:** i 22 campi dei disegni di P30 sono **esattamente le colonne che la tabella `clienti` ha già**. ⚠️ **E le due anagrafiche non seguono la stessa regola:** `provato:` `pazienti` ha **sei** campi e tiene **nome e cognome in uno solo** (`nome_cognome TEXT`), mentre `clienti` li tiene separati — nessun documento dice perché. ➡️ **Serve una ricerca** (chiesta esplicitamente da Francesco): che cosa deve contenere l'anagrafica di uno studio odontoiatrico e di un **laboratorio committente** in Italia, che cosa impongono fatturazione elettronica e MDR, e che cosa va **tolto** perché contraddice la filosofia di UÀ («*non deve preoccuparsi più di niente*»). 🛑 **Va fatta PRIMA del React di P30:** un campo aggiunto dopo è una riga in più in una schermata già approvata | ricerca + `supabase/schema.sql` (`clienti`, `pazienti`) |
 | 🟠 **P30-b** | 🆕 **CON LA VARIANTE A, LA PAGINA DI MODIFICA E LA SCHEDA DEL CLIENTE SONO QUASI LA STESSA COSA — e due pagine quasi identiche sono un difetto, non una scelta.** Aperta il **03/08/2026 da Francesco**: «*questa schermata sostituirà la schermata dell'anagrafica di un cliente?*». `provato:` la scheda di oggi (`clienti/[id]/page.tsx`) ha **sei** sezioni — Anagrafica · Dati fiscali · Commerciale · Note · Portale dentista · Privacy GDPR; la variante A ne mostra **quattro**, con le stesse informazioni, **solo toccabili**. 🔑 **Con la A la risposta naturale è UNA SOLA PAGINA:** la scheda diventa correggibile e la «pagina di modifica» non nasce affatto — il che è anche più fedele alla Legge 1, perché toglie una schermata invece di aggiungerla. ⚠️ **Ma allora va risolto come ci si arriva da un tasto «Aggiungi il dato»** (un'àncora al campo? il foglio che si apre da solo?), che era **il motivo per cui P30 esiste**. ➡️ **Da decidere prima del React** | `src/app/(app)/clienti/[id]/page.tsx` · **D180** |
 | 🔴 **P30** | ✅ **VARIANTE SCELTA — la 🅰️ «le righe che si toccano» (D180, Francesco, 03/08/2026).** 🛑 **MA NON SI SCRIVE ANCORA REACT:** la scelta è arrivata **con tre riserve**, e due possono cambiare la pagina — **P30-a** (l'anagrafica non è mai stata progettata: serve una ricerca) · **P30-b** (con la A, scheda e modifica si sovrappongono: forse è UNA pagina sola) · **P31** (il telefono si sdoppia, e tocca la banca dati). ➡️ **L'ordine è: P31 e P30-a e P30-b, POI il React.** 🔧 **Corretto lo stesso giorno un difetto del disegno trovato da Francesco provandolo:** ogni riga apriva **sempre lo stesso foglio** (quello del telefono) — «*un disegno che si tocca e risponde sempre la stessa cosa fa credere che preveda un foglio unico*». Ora le **14** righe aprono ognuna la propria domanda, la propria etichetta e il proprio aiuto. **Come c'è arrivata (notte D168, D174):** ✅ **Tre varianti disegnate**, non una: 🅰️ *le righe che si toccano* (un dato per foglio — la più fedele alla Legge 1) · 🅱️ *i quattro cartoncini* (un gruppo per foglio — **consigliata**) · 🅲 *la pagina intera* (tutti i 22 campi, salvataggio in fondo). 🔑 **La scelta non è estetica ed è per questo che va a Francesco:** un dentista ha **22 dati**, e metterli tutti in fila in una pagina è ciò che ogni gestionale fa — ma contraddice **L1** («una cosa alla volta») e **§5.27** («i campi vivono solo dentro fogli e procedure guidate»). Le tre varianti sono tre posizioni diverse del confine fra la regola di casa e la fretta di chi ha le mani sporche di gesso. 📄 Documento da leggere: `docs/design/2026-08-03-p30-tre-varianti-da-scegliere.md` · disegni: `docs/design/mockups/2026-08-03-p30-modifica-dentista-{A,B,C}.html` · **40 scatti** (3 formati × 2 temi × pagina intera + primo schermo + foglio aperto). 🛡️ **Contrasti MISURATI, non dichiarati: 442 testi, 0 sotto soglia** — ma solo dopo aver trovato **tre** cose: ① in **B** i cartoncini in tema scuro erano a **1,25:1** (un tasto non eredita il colore del testo: restava il nero predefinito del browser — invisibile solo al buio, e **l'occhio l'aveva saltato**) · ② le etichette dentro il foglio a **4,25:1**, perché `--faint` regge su `--card` ma **non** sul fondo più chiaro del foglio (**difetto del codice v3 già scritto**, v. P30-bis) · ③ 🎣 **un difetto INESISTENTE**: la sonda dava il tasto rosso a 3,52:1 in scuro e c'era perfino il precedente perfetto (§5.4, «stop pinnati, MAI `var(--green)`»), ma il tasto **vero** scrive a **21px** e il disegno a **17** — sopra 18,66 in grassetto la soglia scende a 3, quindi **il componente era a posto ed era il disegno a essere diverso da lui**. ✅ **Due vuoti CHIUSI nella stessa notte** (erano dichiarati e sono stati fatti, invece che rimandati): la resa è stata guardata su **tre motori** (Chromium · Firefox · **WebKit**, 18 scatti, rese coerenti) e i **bersagli tappabili misurati uno per uno** — **nessuno sotto i 44px**. 🔴 **E la misura ha trovato un altro difetto: la via di fuga era 342×18.** 🛑 **Anche stavolta il componente vero era già a posto:** `LinkQuieto` risolve con `min-height:44` + `padding` e `margin` negativo uguale e contrario — l'area cresce, l'aspetto no — e il mio disegno non l'aveva copiato. 🔑 **Stessa causa del «difetto inesistente», dall'altro lato: un disegno che non copia le misure vere dei componenti mente in DUE direzioni** — una volta inventa un difetto che non c'è, una volta ne nasconde uno che c'è. ⚠️ **Non guardato:** lettore di schermo · «Riduci movimento» · nessuna prova su un telefono vero. **Testo originale:** | 🟡 in attesa della firma · `docs/design/2026-08-03-p30-tre-varianti-da-scegliere.md` |
 | 🟠 **P30-bis** | 🆕 **In tema scuro l'etichetta di un campo dentro un foglio è sotto la soglia di leggibilità — e riguarda il codice v3 GIÀ SCRITTO, non un disegno.** Trovata il 03/08/2026 misurando i contrasti dei disegni di P30 e **riferita, non corretta** (R-E2). `misurato:` `--faint` (#928778) sul fondo di un foglio (`--elv`, #2B2620) dà **4,25:1**, sotto il **4,5** che WCAG 1.4.3 chiede a un testo di 13px. 🔑 **Il colore non è sbagliato: è il FONDO a cambiare.** Su `--bg` e su `--card` lo stesso `--faint` passa — è la superficie **più chiara** del foglio a farlo cadere, e nessuno l'aveva misurato lì. ⚠️ **Raggio:** `Campo.tsx` usa `var(--faint)` per ogni etichetta (§5.27), e ogni `Sheet` v3 in tema scuro monta `--elv` — quindi tocca **ogni campo dentro ogni foglio dell'app**, wizard compreso. ➡️ Due strade: ① l'etichetta passa a `--muted` quando è dentro un foglio (fatto nei disegni di P30, dichiarato come scostamento da §5.27) · ② si schiarisce `--faint` in tema scuro, **ma allora va rimisurato ovunque** — il token è già stato corretto una volta (rev. 3.1, «era #6E6457, WCAG fail») | `src/components/ds/Campo.tsx` · `src/design-system/v3/tokens.ts` (`notte.faint`) · spec v3 §5.27 | — e questo rende impossibile mandare qualcuno a correggere un suo dato.** Aperta il 02/08/2026 dall'esecutore del **Task 3 di P17** e **ratificata da Francesco (D165)**. `provato:` `find src/app -name page.tsx -path "*modifica*"` → **una sola**, `lavori/[id]/modifica`. Per i clienti la modifica esiste **solo** come pannello dentro `ClienteModificaButton`, con uno `useState` proprio: **non ha indirizzo**, quindi da qualunque altro punto — anche dalla stessa schermata — **non si può aprire**. 🔑 **Il difetto si vede solo provando a mandarci qualcuno**, ed è così che è saltato fuori: il mockup di P17 approvato disegnava un tasto «Aggiungi il dato» per il caso «manca la Partita IVA dello studio», e in produzione **sarebbe stato un tasto morto** — si preme e non succede niente. ⚠️ **Non è un difetto di P17:** è una mancanza preesistente che P17 ha solo reso visibile. ✅ **Il modello esiste in casa** (`lavori/[id]/modifica`), quindi non si inventa niente. 📌 **Coerente con la direttiva permanente del 27/07** («*ogni campo del lavoro si corregge, fino alla consegna*»): un campo la cui via di correzione non è raggiungibile da dove serve è una via di correzione a metà. ➡️ **Ordine deciso (D165): P17 si chiude PRIMA**, col blocco che indica il «Modifica» già presente sulla schermata invece di un tasto morto; **questa voce aggiunge il tasto quando la pagina esiste.** ⚠️ **Pagina nuova = §0B per intero** (bozze → scatti 390/768/1280 chiaro+scuro → approvazione) **+ FASE 9b** | 🆕 `src/app/(app)/clienti/[id]/modifica/page.tsx` · modello: `src/app/(app)/lavori/[id]/modifica/page.tsx` · `src/components/features/clienti/ClienteEditSheet.tsx` |
 | 🟠 **P9-bis** | 🆕 **QUATTRO DOCUMENTI PRENDONO LA DATA DI EMISSIONE DALL'OROLOGIO DEL MOMENTO IN CUI SI STAMPANO — quindi ristamparli domani gliela cambia.** Trovata il 02/08/2026 **dentro** il lavoro su P9 e **riferita, non corretta** (R-E2: P9 riguarda il **fuso**, questo riguarda la **sorgente**). `provato:` `RicevutaConsegnaTemplate.tsx`, `IFUTemplate.tsx`, `SchedaFabbricazioneTemplate.tsx` e `BuonoTemplate.tsx` costruiscono la data di emissione da `new Date()`, cioè da «adesso». 🔑 **È un difetto PIÙ GRANDE di P9, non più piccolo:** il fuso sposta la data di **un giorno** e solo dentro una finestra di due ore; questo la sposta **di quanto vuoi**, ogni volta che il file viene rigenerato. Su una ricevuta di consegna e su un registro di tracciabilità è la data che dice **quando è successo**. ⚠️ **Il fuso è stato corretto lo stesso su tutti e quattro**, con una scelta dichiarata: lasciarli sbagliati «per non nascondere il problema grosso» avrebbe barattato un difetto vivo contro un promemoria — il promemoria sta invece **scritto accanto a ognuno dei quattro punti** e qui. ✅ **Per il buono la risposta esiste già**: `buoni_consegna.data_emissione` (`supabase/schema.sql:1318`, `DATE NOT NULL DEFAULT CURRENT_DATE`) — la colonna c'è e il modello non la usa. 🛑 **Per gli altri tre no, ed è una domanda per Francesco** (raccolta come **D-Q3**): la ricevuta di consegna, le istruzioni per l'uso e la scheda di fabbricazione **non hanno** una colonna «emesso il», quindi bisogna decidere da quale fatto prende la data ciascuno — e la risposta non è la stessa per tutti e tre | `RicevutaConsegnaTemplate.tsx` · `IFUTemplate.tsx` · `SchedaFabbricazioneTemplate.tsx` · `BuonoTemplate.tsx` |
diff --git a/docs/superpowers/plans/2026-08-03-p31-due-numeri-per-il-cliente.md b/docs/superpowers/plans/2026-08-03-p31-due-numeri-per-il-cliente.md
index 9a9e79af..ee712d82 100644
--- a/docs/superpowers/plans/2026-08-03-p31-due-numeri-per-il-cliente.md
+++ b/docs/superpowers/plans/2026-08-03-p31-due-numeri-per-il-cliente.md
@@ -123,26 +123,34 @@ vecchia deriva di date: **copiarne il numero manderebbe la migration nel futuro*
 --
 -- Nessun CHECK sul formato: un vincolo renderebbe NON SALVABILE
 -- un numero scritto male, contro la direttiva permanente del
 -- 27/07 («ogni campo si corregge, fino alla consegna»). La forma
 -- si sistema quando si costruisce il link, non quando si salva.
 -- ============================================================
 
 ALTER TABLE public.clienti ADD COLUMN cellulare_whatsapp TEXT;
 
 COMMENT ON COLUMN public.clienti.telefono IS
-  'Telefono dello studio: si chiama, va sui documenti. Puo'' essere un fisso. NON e'' il numero WhatsApp — v. cellulare_whatsapp (P31, D181).';
+  'Telefono dello studio: si chiama, va sui documenti. Può essere un fisso. NON è il numero WhatsApp — v. cellulare_whatsapp (P31, D181).';
 
 COMMENT ON COLUMN public.clienti.cellulare_whatsapp IS
   'Cellulare su cui il dentista riceve i messaggi (consegna, solleciti). Il prefisso internazionale lo aggiunge il codice, non l''utente (P31, D182).';
 ```
 
+> 🔧 **CORRETTO il 03/08 dalla revisione del compito 1, e il difetto era di questo piano.** La prima
+> stesura scriveva `Puo''` e `e''` dentro la stringa di `COMMENT ON`, credendo che l'accento andasse
+> raddoppiato come l'apostrofo. **È falso:** in SQL due apici consecutivi valgono **un apostrofo
+> letterale**, quindi `Puo''` finisce nel database come `Puo'`, non come `Può`. L'accento non ha
+> bisogno di alcun escaping. `provato:` `supabase/migrations/20260803090000_*.sql` — stessa cartella,
+> stesso giorno — usa l'accento **vero** non raddoppiato accanto a un apostrofo **correttamente**
+> raddoppiato (`dell''emissione`). 🔑 **Solo l'apostrofo si raddoppia, mai la lettera accentata.**
+
 `non eseguito` — verifica al passo 3.
 
 - [ ] **Passo 3 — Applica e verifica che la colonna ci sia DAVVERO**
 
 ```bash
 npx supabase migration up
 ```
 
 Poi, **una sonda che non si accontenta del «riuscito»**:
 
diff --git a/docs/superpowers/specs/2026-08-03-p31-due-numeri-per-il-cliente-design.md b/docs/superpowers/specs/2026-08-03-p31-due-numeri-per-il-cliente-design.md
index 87a2ba23..169fa3de 100644
--- a/docs/superpowers/specs/2026-08-03-p31-due-numeri-per-il-cliente-design.md
+++ b/docs/superpowers/specs/2026-08-03-p31-due-numeri-per-il-cliente-design.md
@@ -47,25 +47,33 @@ banca dati è un **fisso**: quel campo **si comporta già** da telefono dello st
 significherebbe spostare 39 righe per farne combaciare una; lasciarlo dov'è costa **la correzione di un
 commento** che oggi dice il falso.
 
 ---
 
 ## 3. Il modello dei dati
 
 ```sql
 ALTER TABLE clienti ADD COLUMN cellulare_whatsapp TEXT;
 COMMENT ON COLUMN clienti.telefono IS
-  'Telefono dello studio: si chiama, va sui documenti. Puo'' essere un fisso. NON e'' il numero WhatsApp (v. cellulare_whatsapp) — P31, D181.';
+  'Telefono dello studio: si chiama, va sui documenti. Può essere un fisso. NON è il numero WhatsApp (v. cellulare_whatsapp) — P31, D181.';
 COMMENT ON COLUMN clienti.cellulare_whatsapp IS
   'Cellulare su cui il dentista riceve i messaggi (consegna, solleciti). Il prefisso internazionale lo aggiunge il codice, non l''utente — P31, D182.';
 ```
 
+> 🔧 **CORRETTO il 03/08 dalla revisione del compito 1, e il difetto era di questo piano.** La prima
+> stesura scriveva `Puo''` e `e''` dentro la stringa di `COMMENT ON`, credendo che l'accento andasse
+> raddoppiato come l'apostrofo. **È falso:** in SQL due apici consecutivi valgono **un apostrofo
+> letterale**, quindi `Puo''` finisce nel database come `Puo'`, non come `Può`. L'accento non ha
+> bisogno di alcun escaping. `provato:` `supabase/migrations/20260803090000_*.sql` — stessa cartella,
+> stesso giorno — usa l'accento **vero** non raddoppiato accanto a un apostrofo **correttamente**
+> raddoppiato (`dell''emissione`). 🔑 **Solo l'apostrofo si raddoppia, mai la lettera accentata.**
+
 `non eseguito` — l'esecutore la scrive sotto test e la verifica con:
 `npx supabase migration up` → `npx supabase gen types typescript --project-id iagibumwjstnveqpjbwq > src/types/database.types.ts` → `npx tsc --noEmit` (**FASE 6b**).
 
 **Perché `TEXT` senza vincolo di forma.** Un `CHECK` sul formato del numero renderebbe **non
 salvabile** un numero scritto male, e questo progetto ha una direttiva permanente in senso contrario
 («*ogni campo si corregge, fino alla consegna* » — l'errore di digitazione al banco è il caso normale,
 non l'eccezione). La forma si sistema **quando si costruisce il collegamento** (§5), non quando si
 salva. ⚠️ **Scelta dichiarata, non omissione.**
 
 **Non serve backfill.** `provato:` un solo cliente ha un telefono, ed è un fisso — che è già il valore
diff --git a/src/lib/consegna/whatsapp-template.ts b/src/lib/consegna/whatsapp-template.ts
index c04c46b8..39488584 100644
--- a/src/lib/consegna/whatsapp-template.ts
+++ b/src/lib/consegna/whatsapp-template.ts
@@ -24,25 +24,56 @@ export function buildWhatsappMessage({
   return [
     `✅ Lavoro #${numeroLavoro} pronto per la consegna.`,
     ``,
     `📋 Visualizza dettagli e scarica i documenti:`,
     portalUrl,
     ``,
     `— UÀ Lab`,
   ].join('\n')
 }
 
+/**
+ * Prepara un numero per wa.me: solo cifre, in formato internazionale.
+ *
+ * 🔑 PERCHÉ ESISTE (P31, D182). `buildWhatsappUrl` faceva solo
+ * `replace(/\D/g,'')`, e NESSUN punto del programma aggiungeva il 39:
+ * un cellulare italiano scritto come lo scrive chiunque («333 1234567»)
+ * produceva un link senza prefisso paese.
+ *
+ * 🛑 LA SOGLIA DELLE 11 CIFRE non è arbitraria: esiste un prefisso di
+ * cellulare italiano che COMINCIA per 39 (391…, Wind), quindi
+ * «comincia per 39» non basta a dire «è già internazionale».
+ * I numeri nazionali italiani non superano le 10 cifre, e i fissi
+ * cominciano per 0, mai per 39.
+ *
+ * 🛑 COSA NON FA: non valida che il numero sia raggiungibile, non
+ * distingue fisso da cellulare, non rifiuta niente. Prepara una stringa.
+ * ⚠️ Un numero STRANIERO scritto SENZA il «+» viene trattato da
+ * italiano: limitazione dichiarata (in banca dati non ci sono clienti
+ * stranieri), e la strada è chiedere il «+», non indovinare.
+ */
+export function numeroPerWhatsapp(grezzo: string | null | undefined): string | null {
+  if (!grezzo) return null
+  const conPiu = grezzo.trim().startsWith('+')
+  const cifre = grezzo.replace(/\D/g, '')
+  if (!cifre) return null
+
+  if (conPiu) return cifre                                  // il paese è dichiarato: si rispetta
+  if (cifre.startsWith('00')) return cifre.slice(2)          // forma internazionale con 00
+  if (cifre.startsWith('39') && cifre.length >= 11) return cifre  // già internazionale
+  return `39${cifre}`                                        // nazionale italiano
+}
+
 export function buildWhatsappUrl(message: string, phone?: string): string {
   const encoded = encodeURIComponent(message)
-  return phone
-    ? `https://wa.me/${phone.replace(/\D/g, '')}?text=${encoded}`
-    : `https://wa.me/?text=${encoded}`
+  const numero = numeroPerWhatsapp(phone)
+  return numero ? `https://wa.me/${numero}?text=${encoded}` : `https://wa.me/?text=${encoded}`
 }
 
 export interface SollecitoParams {
   studioNome: string    // nome dello studio/dentista (non dati paziente)
   totaleInsoluto: number
 }
 
 /**
  * Messaggio sollecito pagamento GDPR-safe — nessun dato clinico o paziente.
  * Usato dalla pagina Scadenzario per link WhatsApp ai dentisti con fatture non pagate.
diff --git a/tests/unit/numero-per-whatsapp.test.ts b/tests/unit/numero-per-whatsapp.test.ts
new file mode 100644
index 00000000..e3b484b3
--- /dev/null
+++ b/tests/unit/numero-per-whatsapp.test.ts
@@ -0,0 +1,39 @@
+import { describe, it, expect } from 'vitest'
+import { numeroPerWhatsapp } from '@/lib/consegna/whatsapp-template'
+
+describe('numeroPerWhatsapp — prepara un numero per wa.me (P31, D182)', () => {
+  it('il caso normale: cellulare italiano come lo scrive chiunque', () => {
+    expect(numeroPerWhatsapp('333 1234567')).toBe('393331234567')
+  })
+
+  it("rispetta il + gia' presente", () => {
+    expect(numeroPerWhatsapp('+39 333 1234567')).toBe('393331234567')
+  })
+
+  it('il fisso vero in banca dati', () => {
+    expect(numeroPerWhatsapp('0976 71439')).toBe('39097671439')
+  })
+
+  it('un + straniero SI RISPETTA — non si italianizza', () => {
+    expect(numeroPerWhatsapp('+33 6 12 34 56 78')).toBe('33612345678')
+  })
+
+  it('la forma internazionale con 00', () => {
+    expect(numeroPerWhatsapp('00 39 333 1234567')).toBe('393331234567')
+  })
+
+  // 🔑 IL CASO CHE SMONTA «comincia per 39»: 391 è un prefisso di
+  //    cellulare italiano (Wind). 10 cifre -> è NAZIONALE.
+  it("un cellulare 391… senza prefisso e' NAZIONALE, non internazionale", () => {
+    expect(numeroPerWhatsapp('391 2345678')).toBe('393912345678')
+  })
+
+  // ...e il verso opposto: 11 cifre che cominciano per 39 -> gia' internazionale
+  it("un fisso gia' internazionale resta intatto", () => {
+    expect(numeroPerWhatsapp('39 0976 71439')).toBe('39097671439')
+  })
+
+  it.each([null, undefined, '', '   ', '---', '()'])('senza un numero usabile da null: %s', (v) => {
+    expect(numeroPerWhatsapp(v as string | null | undefined)).toBeNull()
+  })
+})
